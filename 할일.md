# 할 일

**지금 안 하지만 잊으면 안 되는 것.** 끝난 건 지운다 — 여기 남아 있으면 아직 안 된 것이다.

- 규칙은 [인수인계.md](인수인계.md), DB를 건드린 이력은 [DB-CHANGELOG.md](DB-CHANGELOG.md).
- 마지막 손댄 날: 2026-09-01

---

## 사장님 판단이 필요한 것

| | 무엇 | 왜 멈춰 있나 |
|---|---|---|
| ☐ | **표준 계정과목 이전** | 세무사 확인 대기. 계획은 [docs/표준계정과목-이전계획.md](docs/표준계정과목-이전계획.md), 스크립트는 `scripts/fix-standard-chart.mts` — **아직 돌리지 않았다.** 붙여 주신 표준 목록에 원재료비·부재료비가 없어서 500/505를 어디로 보낼지가 남은 물음이다. |
| ☐ | **박스 재고 모델** | 제조한 박스를 그대로 내보내나, 아니면 낱개로 풀었다 다시 담나. 이 답에 따라 재고 모델이 갈린다. |

## 날짜가 정해진 것

| | 언제 | 무엇 |
|---|---|---|
| ☐ | 2026-12-31 결산 | **첫 거래처 앵커를 박는다.** `npx tsx scripts/stamp-partner-anchor.mts --year=2026 --apply` (기본은 미리보기). 지금은 앵커가 없어 기초일부터 읽는다 — 동작은 같고 읽는 양만 다르다. 미리보기로 이미 확인했다(태백 64곳·92장, 풍회 5곳·8장, 미결 합 = 잔액 전부 일치). |

## 확인해야 할 것

- ☐ **거래명세서 양식을 컴포넌트로 떼는 일 — 아직 못 뗐다.**
  1,036줄 통짜인데 **밖에서 끌어다 쓰는 게 233개**다. 일반전표 창(224개)과 겉보기 숫자는
  비슷한데 성격이 다르다. 일반전표는 그 중 서른이 자기 상태라 통째로 들어갔지만, 여기는
  대부분이 **목록 화면과 진짜로 나눠 쓰는 것**이다(`dateFrom`·`activeOrders`·`partnerOrders`·
  `isVouchered`·`selectedConfirmedIds`…). 지금 통째로 빼면 prop 이 마흔을 넘는다 —
  같은 얽힘에 서명만 길어진다.

  안을 열어 보니 섹션이 갈린다. 안쪽부터 하나씩 떼는 게 맞다.

  | 섹션 | 줄 | 밖에서 쓰는 것 | 성격 |
  |---|---|---|---|
  | 주문/발주 고르기 | 3834–4084 (250) | 많음 | 양식이 아니라 **워크플로** |
  | 품목 선택 팝업 | 4205–4360 (156) | 61 | 자족적 — **셈은 뗐다**(`shared/itemPick`) |
  | 품목 테이블 | 4371–4654 (284) | 94 | 진짜 양식. 다음 차례 |

  순서: ① 품목 테이블의 셈(박스↔낱개·단가 저장 상태·과세 토글)을 순수 모듈로 →
  ② 주문 고르기를 목록 화면 쪽으로 밀어냄 → ③ 그러고 나면 남는 게 양식이다.
- ☐ **일반전표 창의 갈래 안 갈래(상환·급여·보험·세금)는 단추가 없다** — 템플릿을 골라야만 들어간다.
  그게 맞는 설계인지 안 정했다. 템플릿 없이 그 갈래로 끊고 싶은 날이 있으면 막힌다.
- ☐ **반품 전표(총액 음수)가 배분에서 상계되지 않는다.** (인천)청정식품 −693,000이 그 예. 잔액에서는 상계되는데 전표별 FIFO 배분은 못 건드려서, 그 −693,000이 영영 안 쓰인 채 남는다. 화면에는 티가 안 나고(잔액은 맞다) 앵커도 그대로 옮기게 해 뒀지만, **다음 매입에서 저절로 깎여야 맞는 것 아닌가**는 안 정했다.
- ☐ **131 선급금이 대변으로 30,740,650 밀려 있다.** 8/4 `260804-14`가 선급금 50,740,650을
  매입(500)으로 털었는데, 그 앞에 선급금을 세운 전표가 없다. 서 있는 건 8/20 웨일스드림
  20,000,000 하나뿐이다. 수입 대금을 먼저 보낸 전표가 안 끊겼거나, 다른 계정으로 들어갔다.
  **자산 계정이 대변 잔액인 건 말이 안 되니 어느 쪽이든 맞춰야 한다.** (2026-09-01 발견,
  선수금 오기입 고치다 나옴 — 그 건과는 별개다.)
- ☐ **서브타입이 '낱개'로 찍힌 박스 품목 2개** — 시골향참기름/A·특A/흰정사각/1750ml.
  BOM 은 ×10 박스라 하고 단위도 박스인데 서브타입만 낱개다. 개입수는 BOM 에서 오니
  동작엔 영향 없지만, 화면에서 갈래로 거를 때 빠진다.
- ☐ **`unitsPerBoxOf` 의 옛 갈래 셋을 걷어낸다** — BOM 이 근거로 정해졌으니
  `boxSize`(고춧가루 2품목) · 규격 글자 · '향미유면 12'(7품목) 는 임시 다리다.
  고춧가루·향미유에 BOM 을 채우면 이 함수는 `unpackComponent` 의 겉옷만 남는다.
  (향미유 규격 '1.8L' 넣는 건 2026-09-01 에 사장님이 보류하셨다.)
- ☐ **`runOne`의 `busy` 이중 방어** — 단추가 이미 잠겨 있어 화면으로는 시험할 방법이 없다. 코드로만 확인했다.

## 나중에 할 것 — 재고·원가

### ☐ 재고관리를 한 번 갈아엎는다 (2026-09-01 사장님 판단)
훑다 나온 것들이 한 갈래를 가리킨다 — **같은 사실을 여러 군데서 따로 들고 있다.**

| 무엇 | 상태 |
|---|---|
| 개입수 | BOM · 규격 글자 · `boxSize` · '향미유면 12' — 근거가 넷이었다. BOM 우선으로 잡았지만 셋이 임시 다리로 남아 있다 |
| 박스 규격 | 낱개를 고쳐도 안 따라갔다. 전파를 붙였지만 **파생값을 저장하는 구조** 자체가 원인이다 |
| 개입수 12 | 화면 여덟 군데에 손으로 박혀 있었다 |
| 재고 실사 | `boxSizeOf` 가 모르면 12로 짐작한다 — 근거 없이 곱한다 |
| 박스 vs 낱개 | 별개 품목으로 따로 센다(사장님 확인). BOM 은 등가 관계일 뿐 차감 아님 |

**갈아엎을 때 정할 것** — 파생값(규격 글자·개입수)을 저장할 것인가 읽을 때 만들 것인가.
지금은 저장해 두고 전파로 맞추는데, 전파를 한 군데라도 빠뜨리면 조용히 어긋난다.


- ☐ **price → cost 통합** — `Product.price`를 `cost`로. 뜻이 겹치는 필드가 둘이라 어느 쪽이 진짜인지 매번 헷갈린다.
- ☐ **OEM 재고처리** — 완사입/임가공. `procureType` + 외주창고 + Backflushing + 가공비전표. 통째로 미구현.
- ☐ **박스/낱개 재고** — 품목 1개 + 박스규격별 variant 재고 + 공유 낱개. 위 '박스 재고 모델' 판단이 먼저다.
- ☐ **배송·선물세트 kit 차감** — 원료배합 반제품은 Phantom 으로 풀었는데(2026-07-10), kit 쪽은 아직이다.
- ☐ **임가공 경로 재고 경합** — 재고 쓰기 경합을 `adjustItemStock` 으로 여덟 군데 막았는데(2026-08-23) 임가공 경로만 남았다.
- ☐ **원료수불부 원장 분리** — 전 원료를 주문×품목으로 다시 세려다 박스 수량 10배 버그(주문 데이터 오류 + `autoRawMaterialUsage` 가 `stockUnits` 를 안 씀)를 보고 멈췄다.

## 나중에 할 것 — 여러 회사에 파는 준비

지금은 회사 구분이 `companyId` 하나로 걸러지는 정도다. 남의 회사에 팔려면 아래가 필요하다.
자세한 건 [인수인계.md](인수인계.md) 8장.

- ☐ **인증을 바꾼다** — 지금은 **익명 로그인**이라 파이어베이스가 누가 어느 회사인지 모른다. 그래서 보안 규칙으로 회사를 못 가른다. **이게 첫 단추다** — 규칙을 먼저 손대는 건 순서가 틀렸다.
- ☐ **보안 규칙** — 인증을 바꾼 다음.
- ☐ **DB 갈아탈 여지** — 화면이 firebase 를 직접 부르지 않게. `firebaseService` 로 문을 넓혀 뒀고(`fetchWhere`·`subscribeWhere`·`writeMany`·`mutateDoc`·`fetchByIds`) 새 코드는 그 문만 쓴다. 옛 코드가 아직 많다.

## 나중에 할 것 — 코드 다듬기

- ☐ **`fmt` 18벌 정리 — 급하지 않다.** 숫자를 글자로 바꾸는 함수가 파일마다 있다.
  그런데 **11개는 글자 하나 안 틀리고 같고**, 나머지는 일부러 다르다
  (로트는 소수 1자리, 품목원장은 3자리, 원가관리는 뒤에 '원', 롤백요약은 아예 숫자를 낸다).
  갈려도 쉼표가 안 찍힐 뿐 돈이 안 틀리고, 지금 실제로 갈린 것도 없다.
  **화폐 표기 규칙을 바꿀 때** 그때 같이 하면 된다.
- ☐ **`slice(0, 7)` 월 자르기 20파일** — 글자 자르기라 시간대 문제는 없다. 값어치 낮음.

## 나중에 할 것 — 설비

- ☐ **린트 설정** — 지금 5,350건인데 4,870건이 `로컬전용/` 일회용 스크립트다(`.mjs` 에 node globals 가 없어서 `console.log` 마다 오류). TS 파일의 `no-undef` 도 꺼야 한다(손으로 적은 globals 목록 70개도 같이 지운다). **설정 한 곳이고 코드는 안 건드린다.** 5,350 → 약 400이 된다.
- ☐ **react-hooks 경고 37건** — 위를 치우고 나면 보인다. 훑어본 바로는 전부 오탐이었다(클릭 핸들러 안의 `Date.now()` 를 렌더 중이라고 잡는 식). 다시 재 볼 것.
- ☐ **Firebase 에뮬레이터 + E2E** — Playwright 는 깔려만 있고 안 쓴다. 앱이 **운영 DB 에 바로 붙어서** 지금 돌리면 진짜 전표가 쌓인다. 에뮬레이터가 먼저다.

## 알고 두는 것 (고칠 게 아니다)

- **`stmt-hide-20260828`** — 분개가 안 만들어지는 전표 1건. 내가 만든 더미이고 회계에 안 잡힌다. 태백 496건 중 유일하게 걸리는 것이라, 앞으로 이 숫자가 2가 되면 진짜 문제가 생긴 것이다.
- **통장 잔고는 관리하지 않는다** — 103 보통예금이 음수여도 문제가 아니다.
- **원료수불부(`rawDocEntries`)와 원장(`rawMaterialLedger`)은 안 맞춘다** — 서류용과 실물용이라 뜻이 다르다.
