# 할 일

여기에는 **아직 끝나지 않은 일만** 둔다. 완료 이력은 `DB-CHANGELOG.md`, 규칙과 설계 기준은
`인수인계.md`와 `로컬전용/docs/`에서 본다.

## 작업 분담

- **Codex:** 설계, 완료 조건 결정, 코드·DB 검증, 시험 결과 검토, 최종 승인.
- **Claude Code:** 실제 기능 코드와 스크립트 구현. 시작 전에 이 파일과 `인수인계.md`를 읽는다.
- 같은 작업폴더를 동시에 수정하지 않는다. Claude 작업 중에는 Codex가 겹치는 파일을 고치지 않는다.
- 커밋·푸시·배포·DB 적용은 사장님 지시가 있을 때만 한다.

## 지금 마무리할 것

- [ ] **당일 데이터 점검 화면과 오후 8시 보고 기능을 검수 후 커밋·배포한다.**
  - 작업완료 BOM·재고 스냅샷, 입고·원료 이동, 원료 현재고=활성 로트, 전표·서류 품목·용량,
    판매일지·서류수불부를 분리 검증한다.
  - Windows 예약 작업 `태백푸드 일일 데이터 점검`은 매일 20:00 실행하도록 등록됐다.
  - 2026-09-16 사전 실행 결과 오류 0건·확인 필요 0건. 타입검사와 감사 시험 6개 통과.
  - 2026-09-16 20:00 첫 예약 실행은 Windows PowerShell 5의 UTF-8 한글 경로 해석 때문에 실패했다.
    래퍼에서 경로를 ASCII 코드로 안전하게 조립하도록 고쳤고, 20:13 작업 스케줄러 재실행 결과
    `Last Result: 0`, 보고서·JSON·로그 생성까지 확인했다. 다음 자동 실행은 9/17 20:00이다.
  - 현재 점검은 연결 문서·스냅샷의 **존재 여부** 중심이다. 0건이어도 실제 수량 일치를 완전히
    증명한 것으로 표현하지 않는다.
  - 완료: 저장 당시 BOM·생산·자동제조·재고 증감 산술 대조, 원료 trace와 실제 원장 대조,
    여러 품목 입고의 품목별 대조, 전체 원장 연결 검사, 회사별 판매일지와 배송완료 주문 대조.
  - 완료: 전표 원본의 출력 규격 누락과 선물세트 BOM의 구성 없음·삭제·서류품목 누락을 검사하고,
    판매일지와 서류수불부는 실제 BOM이 아닌 서류용 환산 함수로 날짜별 대조한다.
  - 완료: 재고 작업 `processing`은 예약 TTL과 같은 1시간 이상 남은 것만 경고하고, 날짜 근거가
    과거인 지속 오류는 오늘 발생 문제와 다른 구역에 표시한다.
  - 남은 완료 조건: 전체 Vitest → 사장님 지시 때 커밋·배포.

- [ ] **회사 분리 보안의 남은 부분만 마무리한다.**
  - 직원별 메뉴 권한은 아직 없다. 현재 `viewAllowed()`가 항상 참이라 회사 내 메뉴 세분화는 미구현이다.
    사장님 판단이 있을 때까지 구현을 보류한다.
  - 완료: 오피스톡 방에 `participantCompanies`를 저장하고 앱·규칙 검증을 추가했다. 기존 방 6개와
    메시지 78개는 전부 태백 소속으로 교정하고 재조회까지 확인했다. 규칙 배포만 남았다.
  - 새 문서 쓰기 경로의 `companyId` 누락·부모와 다른 회사 연결을 정적 감사한다.
  - 안정화 뒤 App Check를 모니터링 모드 → 강제 모드 순서로 적용한다.
  - Storage 73개는 회사 경로 복사·MD5·문서 참조 전환까지 끝났다. 옛 원본 삭제 여부만 결정한다.

## 우선순위 높은 실제 데이터 문제

- [ ] **임가공 옛 원장·로트 자료를 별도로 정리한다.**
  - 8/11 1,425kg·8/28 712kg 가공입고 원장과 로트 불일치.
  - 9/10 한울유통은 실제 사용 후보 20kg 중 2kg, 9/11 무경유통은 150kg 중 15kg만
    원료수불부에 적힌 후보라 각각 18kg·135kg가 부족하다. **완제품 로트 부족 문제가 아니다.**
    주문·실사 시각을 표로 보여드린 뒤 정정 여부를 판단받는다.
  - 9/10 실사 전 부족분은 실사에 이미 반영됐으므로 소급 추가하지 않는다.
  - 주문·원장·실사 시각 대조는 `로컬전용/일일점검/임가공-옛기록-판단표.md`에 정리했다.
    한울 18kg와 무경 135kg는 9/10 00:48 실사 **이후** 누락이다. 현재 네 재고 축은 이미
    일치하므로 단순 차감하지 말고, 과거 표시용 기록만 보충할지 판단받는다.

- [ ] **전표와 주문이 다른 품목을 가리키는 남은 5줄을 판단한다.**
  - 해피유통(포천) 300↔350ml, 초당노인복지기관 선물세트 2↔3호, 모란식품 특↔골드A/A,
    알이네식품 삭제 품목 `p-150` 연결.
  - 세화식품 표기 차이 1줄은 `p-201`로 연결 완료했다. 나머지는 자동 연결하지 않는다.
  - 실제 DB 대조표: `로컬전용/일일점검/전표-주문-품목불일치-판단표.md`.

## 기능·구조 개선

- [ ] 공용 로그인 화면에 **관리자 앱/직원 앱**을 명확히 표시하고, 로그인 후 현재 회사와
  다른 회사 관리자 계정으로 전환하는 방법을 안내한다. 회사 선택값을 브라우저에 저장해
  바꾸는 방식은 쓰지 않는다.
- [ ] 배송순서의 **한 차로 묶기**를 현재 회사별 배송 설정 구조로 복원한다.
- [ ] `git diff 06626e0 a08a811 -- components/`로 과거 병합에서 사라진 기능을 한 번 더 감사한다.
- [ ] 같은 이름이지만 라벨·캡·BOM이 다른 품목 표시는 현재대로 둔다. 사장님이 다시 요청할 때 진행한다.
- [ ] 회계·전표 모듈 분리를 `로컬전용/docs/전표-원가-원장-재무-통합설계.md` 9~12장대로 계속한다.
- [ ] 직원이 만드는 주문·발주·반품·입고를 한 정규화 명령으로 통과시키고 관리자 전용 구독·번들을 분리한다.
- [ ] 전표번호를 회사·날짜별 서버 트랜잭션으로 원자 발급하고 Functions도 같은 카운터를 쓴다.
- [ ] 앱과 Functions가 날짜·전표번호·재고평가·BOM 원가·자동전표 순수 계산을 같은 파일에서 쓴다.
- [ ] 비원료 입고도 작업번호, before/after 재고, 입고 기록을 한 transaction에 저장한다.
- [ ] 스마트스토어 변경 주문 조회 → 상세 조회 → 거래처·품목 ID 확인 → 사람 확인 → 저장 연동을 구현한다.
- [ ] 안드로이드 글 공유를 Web Share Target으로 오피스톡에 연결하고 첨부 업로드 서비스를 통합한다.

## 원가·재고 모델 장기 작업

- [ ] 매입단가에 단위와 성격(원물·임가공)을 추가하고 품목 원가의 `purchase` 산출 방식을 설계한다.
- [ ] 품목이 삭제됐는데 남은 `partner_item` 16건을 원래 품목 확인 후 정리한다.
- [ ] 박스 개입수·규격 파생값과 옛 호환 필드를 단계적으로 제거한다.
- [ ] `Product.price`를 `cost`로 통합한다.
- [ ] 완사입·임가공을 외주창고·Backflushing·가공비 계정까지 포함한 한 모델로 통합한다.
- [ ] OEM 외주발주를 재개 가능한 job으로 만들고 원료별 결정적 operation ID를 저장한다.
- [ ] OEM 로트번호를 물질+날짜별 서버 카운터로 원자 발급한다.
- [ ] 배송·선물세트 kit 차감을 실제 BOM 구조에 맞게 통합한다.

## 품질·정리

- [ ] React hook 의존성 경고를 회귀시험으로 실제 오류와 오탐으로 나눠 고친다.
- [ ] 죽은 파일·백업 사본·중복 `ViewType`을 실제 import graph로 확인한 뒤 삭제한다.
- [ ] `functions/lib` 생성물을 Git에서 빼고 미사용 패키지·AI 청크를 확인 후 제거한다.
- [ ] deprecated 필드의 쓰기·읽기·DB 잔존량·대체 필드·삭제 버전 표를 만들고 한 벌씩 제거한다.
- [ ] 직접 Firestore 호출을 공통 서비스로 옮기고 정정 스크립트도 공용 재고 명령을 기본으로 사용한다.
- [ ] 린트 환경 설정과 실제 오류를 분리하고 Firebase Emulator 기반 E2E를 붙인다.

## 날짜가 정해진 것

- [ ] **2026-10-30 전:** Cloud Functions Node.js 20 → 22. 런타임만 먼저 올려 함수 6개를 확인하고,
  `firebase-functions` 6.x 업그레이드는 별도 작업으로 한다.
- [ ] **2026-12-31 결산:** 거래처 원장 앵커 생성 스크립트를 dry 후 적용한다.

## 사장님 판단이 필요한 것

1. **옛 Storage 원본 73개:** 문서함 66개·오피스톡 첨부 7개다. 새 회사 경로 복사·MD5·참조
   전환은 끝났다. 8시 보고 작업 뒤 원본/사본/현재 참조를 다시 대조해 목록을 보여드리고 삭제한다.
2. **메뉴 권한:** 구현 여부는 아직 결정하지 않았다. 당일 보고 작업 이후 다시 판단받는다.
3. **표준 계정과목:** 표준 목록에 없는 원재료비·부재료비 500/505를 어느 계정으로 옮길까요?
4. **박스 재고:** 제조한 박스를 그대로 출고합니까, 출고할 때 낱개로 풀어 다시 포장합니까?
5. **낱개 판매단가 7건:** 박스 단가의 1/입수로 들어간 낮은 값을 박스 줄의 값으로 바로잡을까요?
   - 대한농산 탈피 750, 대성농산 탈피 780, 현대유통 A/1750 1,800, 용두쭈구미 A/1750 1,900,
     청정식품 특A/1800 1,900, 대승종합 볶음검정참깨 1,270, 대성농산 볶음검정참깨 1,100.
6. **과세·면세 11건:** 같은 거래처·같은 물건의 박스와 낱개 과세구분 중 어느 쪽이 맞습니까?
   - 세화·유원, 대농, 엠에스·고창·진보, 가득찬, 거산, 대왕, 무경·짠지네.
7. **임가공 원료수불부 153kg:** 한울·무경 주문, 당시 기록, 실사 시각을 먼저 표로 보여드린 뒤
   정정 사용기록을 추가할지 판단받는다. 재고나 완제품 로트를 바로 더하는 일이 아니다.
8. **전표·주문 불일치 5건:** 주문 줄과 전표 줄을 나란히 보여드린 뒤 실제 납품 기준을 판단받는다.
9. **동명이품 표시:** 현재 상태를 유지한다. 다시 요청하기 전에는 바꾸지 않는다.
10. **반품 음수 전표:** 전표별 FIFO 배분에서 다음 매입 잔액과 자동 상계할까요?
11. **131 선급금 대변 30,740,650원:** 누락된 선급 전표를 찾을지, 8/4 매입 전표 계정을 고칠지 확인이 필요합니다.
12. **직원→관리자 업무 경계:** 직원이 직접 만드는 것은 주문·발주·반품·입고·재고실사 중 어디까지입니까?
13. **정정 스크립트:** 모든 재고 정정도 공용 원자 명령을 의무화하고, 구조 이관만 예외로 둘까요?

## 고칠 일이 아닌 운영 기준

- 판매일지·서류수불부는 실제 BOM과 다른 **서류용 환산 비율**로 검증한다.
- 통장 잔고는 앱에서 관리하지 않는다. 103 보통예금 음수만으로 오류 판정하지 않는다.
- 임가공 완제품 로트는 완제품 쪽에서 차감하고 원료수불부에는 원료 사용 이력을 남긴다.
- `stmt-hide-20260828`은 회계에 안 잡히는 더미 전표 1건이다. 이 숫자가 늘면 오류다.
