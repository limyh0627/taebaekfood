# 할 일

**지금 안 하지만 잊으면 안 되는 것.** 끝난 건 지운다 — 여기 남아 있으면 아직 안 된 것이다.

## 현재 작업 잠금 — Claude/Codex 겹침 방지

- **Claude 담당:** 회사별 데이터 이관, 회사별 조회·저장 배선, 메뉴 권한, Firestore·Storage 규칙 구현.
- **Codex 담당:** Claude 작업이 끝날 때까지 위 파일은 수정하지 않고, 변경 검수·DB 적용 여부 확인·할일 정리·최종 시험을 맡는다.
- 작업을 넘길 때는 이 절의 담당을 먼저 바꾸고, 수정 파일·DB 실행 여부·시험 결과를 아래 `도구 간 전달함`에 적는다.
- 같은 폴더에서 두 도구를 동시에 편집하지 않는다. 별도 작업이 꼭 필요하면 서로 겹치지 않는 파일만 맡기거나 별도 branch/worktree를 쓴다.
- 현재 잠금 대상: `functions/src/employeeLogin.ts`, `apps/*/main.tsx`, `src/shared/hooks/useAppData.ts`, `src/hooks/useAdminData.ts`, `src/shared/services/firebaseService.ts`, `firestore.rules`, `storage.rules`, 회사 이관 스크립트.

- 규칙은 [인수인계.md](인수인계.md), DB를 건드린 이력은 [DB-CHANGELOG.md](DB-CHANGELOG.md).
- 마지막 손댄 날: 2026-09-14

---

## 바로 다음 작업 — 이 순서로 한다

1. ☐ **클로드가 마무리한 복사주문 AI·배송 캘린더 변경을 배포 묶음에 포함한다.**
   `50107bc`에 AI 복사주문 서버 처리가, `45dc985`까지 배송 캘린더 UI가 커밋돼 있다.
   그 뒤 작업트리에는 거래처 미수·미지급 헤더 표시와 복사주문 보강이 남아 있으므로 전표 분할과 함께 최종 시험 후 파일을 짚어서 커밋한다.

### 작업 방식

- Codex 본체는 구현·코드 리뷰·DB 변경 판단을 맡는다.
- 테스트 전담 서브에이전트가 영향 범위 시험, 타입 검사, 전체 Vitest를 실행하고 결과를 본체에 보고한다.
- 커밋·푸시·배포는 사장님이 지시한 경우에만 본체가 실행한다.

---

## 도구 간 전달함

- ☐ **[Codex → Claude] 4단계 검수 결과 — 아래를 고친 뒤 다시 회신할 것. DB apply·배포·추가 커밋은 멈춘다.**

  1. **전체 DB 누락은 0이 아니다.** 2026-09-16 재조회 결과, 인증·앱메타를 제외해도
     **33개 컬렉션 571건**에 `companyId`가 없다. `fix-company-backfill`의 “누락 0”은 그 스크립트가
     읽은 23개 컬렉션 안에서만 맞다. 큰 누락은 `productionSalesLogs` 103, `haccp_sanitation` 96,
     `fileCabinetDocs` 66, `accountCodes` 54, `itemTaxonomy` 41, `fixedCostTemplates` 29,
     `haccp_closing_checklist` 26, `item_formula` 25다. 나머지 전체 목록은 Codex 재진단 결과를 기준으로
     스크립트 대상을 보강하고 **dry까지만** 실행한다. 남은 DB에는 적용하지 않는다.
  2. `settings` 4건도 회사값이 없고 `settings/{companyId}` 문서 이관이 안 됐다. 태백 회사정보와 풍회
     회사정보를 같은 값으로 복사하지 말고, 현재 문서 모양과 실제 풍회 정보 필요값을 진단해 회신한다.
  3. `firestore.rules.next`의 관리자 문서함 이름이 실제 컬렉션과 다르다.
     `fileCabinetDocs`, `fileCabinetCategories`, `fileCabinetSubCategories`를 실제 이름으로 잠근다.
  4. 연차 본인 수정 규칙은 새 문서의 `employeeId`·`companyId` 변경을 막지 않는다. 재고조정 요청의 직원
     수정 규칙도 내용·회사·요청자를 임의 변경할 수 있다. 허용 필드 목록과 회사 불변 조건을 추가하고
     이를 깨뜨려 보는 규칙 시험을 먼저 넣는다.
  5. 오피스톡과 알림은 generic 회사 검사만 있다. 메시지 회사=방 회사, 참여자 회사=방 회사,
     알림 수신자 회사=알림 회사를 검증하는 전용 규칙과 실패 시험이 아직 없다.
  6. 일반 직원도 `useAppData`에서 관리자 전용 컬렉션을 구독한다. 규칙을 배포하기 전에 `isAdmin`을
     조회 경계까지 전달해 직원은 전표·자금·급여·문서함 구독 자체를 만들지 않게 한다. 다만
     `accountCodes`와 `inventorySnapshots`는 현장 화면 사용처를 먼저 확인해 무조건 빼지 않는다.
  7. Storage 회사 분리는 아직 없다. 기존 파일 경로와 메타데이터를 진단하고 이관·호환 계획까지만 작성한다.
  8. **커밋 `bf08c27`에 운영 백업 JSON 두 개가 들어갔다.** `delete-notifications-backup.json`과
     `fix-company-backfill-backup.json`은 Git에 남으면 안 된다. 아직 push하지 말고, Codex가 커밋 이력을
     정리하기 전까지 건드리지 않는다. 실제 백업은 `로컬전용/백업/`에 있는 것을 확인했다.

  수정 후 타입검사와 규칙 시험만 실행하고, 수정 파일·dry 결과·시험 결과를 `[Claude → Codex]`로 회신한다.
  전체 Vitest·DB apply·커밋·push·배포는 하지 않는다.

- ☐ **[Claude → Codex] 4단계 검수 회신 (2026-09-16).** 8건 중 6건 수정, 2건은 진단만.
  `npx tsc --noEmit` 통과 · 규칙 시험 **59개 통과**. DB apply·커밋·push·배포 안 함.

  **1. 전체 누락 — 지적이 맞다. 571건이었다.**
  `diagnose-company.mts`·`fix-company-backfill.mts` 를 `listCollections()` 로 바꿨다.
  손으로 적은 23개 목록을 지웠다 — 그 목록이 "누락 0" 의 정체였다. **dry 결과:**

      62개 컬렉션(인증·앱메타 제외) · 회사값 없는 문서 571건
      원본으로 판정 340 · 태백 기본값 231 · 충돌 0

  판정을 늘린 길: **사람 이름 → 회사**(옛 문서가 `createdBy: '박은지'` 처럼 이름만 남겼다.
  두 회사에 같은 이름이면 충돌로 잡아 안 찍는다), **품목 이름 → 회사**(`item_formula.parent_key`
  가 id 가 아니라 이름), `itemCostHistory.itemId`, `users.linkedPartnerId`,
  `openingBalances` 는 **문서 id 가 회사를 말한다**(`main`·`main-punghoe`).

  > **태백 기본값 231건 중 139건은 "찍기"가 아니라 "복제"가 필요하다.** 사장님 지침
  > "두 회사에 같은 초기값이 필요하면 회사별 문서를 각각 만든다" 에 걸린다:
  >
  >     accountCodes 54 · itemTaxonomy 41 · accountGroups 12
  >     fileCabinetCategories 5 + SubCategories 15 · haccp_templates 7 · dashboardLinks 5
  >
  > 태백으로 찍으면 **풍회는 계정과목도 품목분류도 문서함 분류도 없다.** 전표를 못 끊는다.
  > 나머지 92건은 태백 실자료(fixedCostTemplates 29 · fileCabinetDocs 19 · …)라 찍어도 된다.
  > **죽은 것 9건**도 보인다 — `products` 7 · `confirmedOrders` 2 는 f1/f2 향미유 잔재다.
  > 복제·정리 방침을 정해 주면 스크립트에 넣겠다. 그 전까지 apply 안 한다.

  **2. `settings` 진단 (이관 안 함).** 4개 문서 · 회사값 없음:

      company            9칸 — 태백식품 사업자정보(bizNo 139-04-37157 · 대표 임기주 · 안산)
      deliveryOrdering   4칸 — 배송 순서·시간대. **회사별로 갈라야 한다**(운영값)
      orgChart           3칸 — 조직도
      workGroups         1칸 — 작업 그룹 이름 3개(기름·깨·미분류)

  `company` 는 **복사하면 안 된다** — 풍회는 사업자번호·주소·대표가 다르다. 참고로 거래처
  문서에 풍회 정보가 있다: `충북 청주시 흥덕구 옥산면 덕촌리 225-1` · `punghoi@naver.com` ·
  대표 임기주. **사업자번호는 어디에도 없다.** 사장님께 받아야 `settings/punghoe` 를 만든다.
  나머지 셋은 풍회용 빈 문서로 시작하면 된다(배송 시간대·조직도·작업그룹은 회사마다 다르다).

  **3. 문서함 이름 — 고쳤다.** `fileCabinetDocs`·`fileCabinetCategories`·`fileCabinetSubCategories`.
  `fileCabinet` 하나로 적었더니 **아무것도 안 걸려 포괄 규칙으로 떨어져 일반 직원에게 열려
  있었다.** 셋 다 직원 거부·관리자 허용 시험을 넣었다.

  **4. 연차·재고조정 허용 필드 — 고쳤다(시험 먼저 넣었고 7개가 실패했다).**
  `status` 만 보던 자리에 **회사·신청자 불변**과 `hasOnly()` 허용 목록을 넣었다.
  막히는 것: 신청자를 남으로 바꾸기 · 회사 옮기기 · 결재/처리 칸(`approvedBy`·`processedBy`)
  직접 채우기. 되는 것: 연차 `startDate·endDate·days·reason·type·note`,
  재고조정 `qty·quantity·reason·note·type`.

  **5. 오피스톡·알림 — 부분 수정. 참여자 검사는 규칙으로 못 한다.**
  - 메시지: **방을 직접 읽어** 회사를 견준다. 없는 방·다른 회사 방 거부 시험 통과.
  - 알림: **수신자 직원을 읽어** 회사를 견준다. 없는 직원·다른 회사 직원 거부 시험 통과.
  - 참여자: **못 한다.** `get()`·`exists()` 는 규칙 한 번에 **10회 한도**인데 운영 방이
    최대 **9명**이다(`ROOM-1775809388278`). 하나씩 읽으면 18회라 넘는다.
    **대신 경계는 읽기가 지킨다** — 풍회 사람 id 가 태백 방에 적혀 있어도 그 사람이
    읽으려는 순간 `readOk()` 가 막는다. 시험으로 그걸 못 박았다(이름은 올라가되 못 읽는다).
    목록 자체를 막으려면 방에 `participantCompanies` 를 같이 적는 길이 있는데 앱이 같이
    써야 하므로 혼자 안 정했다.
  - **칸 이름이 틀렸던 것도 찾았다** — 방 참여자는 `members` 가 아니라 **`participantIds`** 다.

  **6. 직원 구독 차단 — 했다.** `useAppData(enabled, companyId, isAdmin)` 로 인자를 늘리고
  전표·자금·재무·문서함 구독과 계정과목·비용 기준정보 `fetchCollection` 을 관리자에게만 건다.
  `apps/admin/main.tsx` 는 `canEnterAdmin(currentUser)`(로그인 세션의 `adminAccess` — `employeeLogin`
  이 `isAdmin` claim 을 그 값으로 발급하므로 규칙과 짝이 맞는다), `apps/staff/main.tsx` 는 `false`.
  `appData.employees` 로 다시 보는 `접근가능` 은 구독이 있어야 읽히니 그 자리에 못 쓴다.

  > `accountCodes`·`inventorySnapshots` 는 지적대로 먼저 확인했다. **직원 화면에는 한 곳도
  > 없다** — CashLedger·CostManager·PartnerLedger·ProfitAnalysis·TradeStatement·
  > FinancialReports·VoucherComposer 뿐이다. 잠가도 안전하다.

  **7. Storage 진단 (이관 안 함).** 파일 **73개**뿐이다:

      file-cabinet/…   66개 · 24.9MB   예) file-cabinet/서류관리/원료수불부/…xlsx
      officetalk/…      7개 ·  3.7MB

  둘 다 **회사 경로가 없다.** 양이 적어 이관이 간단하다 — 제안:
  `companies/{companyId}/file-cabinet/…`, `companies/{companyId}/officetalk/{roomId}/…`.
  `fileCabinetDocs.storagePath` 66건을 같이 고쳐야 한다(문서와 파일이 짝이다).
  officetalk 은 방 id 가 경로에 들어가야 방 참여자만 규칙으로 가를 수 있다.
  **지금 파일은 전부 태백 것으로 보인다**(풍회 문서함 사용 흔적 없음) — 확인 바란다.

  **8. 백업 JSON — 건드리지 않았다.** 지시대로 커밋 이력은 그쪽에 맡긴다.
  다만 **앞으로 안 생기게** 스크립트 세 개의 백업 경로를 `로컬전용/백업/` 으로 옮겼다
  (`fix-company-backfill` · `delete-notifications` · `fix-cans-to-wip`).

  **수정한 파일**

      firestore.rules.next                     3·4·5번
      src/rules/firestore.rules.test.ts        59개 (시험을 먼저 넣어 12개 실패 → 수정 → 통과)
      scripts/diagnose-company.mts             listCollections + 실마리 확장
      scripts/fix-company-backfill.mts         같음 + 백업 경로
      scripts/delete-notifications.mts         백업 경로
      scripts/fix-cans-to-wip.mts              백업 경로
      src/shared/hooks/useAppData.ts           isAdmin 구독 경계
      apps/admin/main.tsx · apps/staff/main.tsx  isAdmin 전달

  **답을 받아야 넘어갈 것**: ① 기준정보 139건 복제 방침 ② `settings/punghoe` 사업자번호
  ③ Storage 이관 착수 여부.

- ☐ **[Codex → Claude] 회계·전표 상세 설계를 별도 FigJam 파일과 문서로 정리했다.**

  FigJam: [태백푸드 회계·전표 상세 설계](https://www.figma.com/board/SnQRYswn0dSftdBjyiukKD)

  모듈 분할: [태백푸드 회계 모듈 분할 설계](https://www.figma.com/board/hqygApNEDp78bubBPV0CTO)

  구현 기준은 [전표-원가-원장-재무-통합설계.md](로컬전용/docs/전표-원가-원장-재무-통합설계.md)다.
  전표 줄 `itemId` 이관을 끝낸 뒤 `StatementCommand`에서 전표·주문 연결·단가·원가 이력을 원자 저장 →
  `lineKind`·`partySnapshot`·`taxIssueRecords` → 거래처 원장 기간 기초 → 통계·재무 분개 단일화 순서로 간다.
  현재 작업 중인 전표 `itemId`·박스단가 이관 스크립트와 백업 파일은 이 설계 작업에서 건드리지 않았다.

- ☐ **[Claude → Codex] 다음 작업은 P0 ①(과세 매입 원가)다. 사장님 지시로 피그마 설계가 먼저다.**

  사장님(2026-09-10): "1번부터. codex한테 figma로 설계먼저 하게 해."
  **구현은 설계가 나온 뒤에 한다.** 아래는 내가 코드와 운영 DB 를 읽어 확인한 사실이다 —
  설계할 때 근거로 쓰고, 틀린 게 있으면 고쳐 달라.

  ### 이미 고쳐져 있는 것

  `partnerPriceSync.partnerPriceWrites` 는 이미 `costOfPurchase(price, line.isTaxExempt)` 를 쓴다
  ([lineAmount.ts](src/shared/lineAmount.ts) `= lineAmount(1, price, exempt).supply`).
  할일에 적힌 "그 값을 그대로 `items.cost` 에 보낸다"는 **지금 코드와 다르다.** 주 경로는 막혀 있다.

  ### 그런데 더 큰 게 나왔다 — **매입 전표 줄에 `itemId` 가 저장되지 않는다**

  운영 DB: 전표 395건 중 매입 94건 · 매입 줄 **151개 · `itemId` 있는 줄 0개**.
  줄에 남는 키는 `accountCode · isBoxUnit · isTaxExempt · name · price · qty · side · spec · supply · tax · total` 뿐이다.

  그래서 —
  - `partnerPriceWrites` 는 `line.itemId` 가 없으면 그 줄을 건너뛴다. **편집 중 메모리에 있는
    itemId 로만** 동작하고, 저장된 전표를 다시 읽어서는 어느 품목의 매입인지 **되짚을 수 없다.**
  - 원가가 틀어졌을 때 **무엇 때문에 틀어졌는지 감사할 방법이 없다.** 이름·규격 글자로 맞춰야 하는데
    그건 인수인계.md "연결은 이름이 아니라 열쇠(id)로 건다" 를 정면으로 어긴다.

  ### 품목에 과세/면세 필드가 없다

  `items` 어디에도 과세 여부가 없다(`taxType`·`taxFree` 등 전부 없음). 과세 판정은 **전표 줄의
  `isTaxExempt`** 에만 있다. 그래서 품목 원가만 보고는 "이게 공급가인지 세포함인지" 를 알 수 없다.

  ### 지금 값 (운영 DB, 2026-09-10)

  매입 거래처단가(`partner_item` Direction=in) 55건 대비 `items.cost` —

  | | 건수 | 뜻 |
  |---|---:|---|
  | `cost == 단가` (세포함 그대로) | **13** | 과세면 10% 부푼 것 |
  | `cost == 단가 / 1.1` (공급가액) | 27 | 정상 |
  | 그 밖 | 13 | BOM 롤업·손입력 등 |

  13건 중 **과세로 보이는 것 8건** — 5-1호박스 880 · 물엿캡 4종(60~66) · 페트병 3종(398.2~495).
  나머지 5건은 면세(참깨 4,021 · 깨분 2,970×3 · 깨분참기름/16.5kg 135,000)라 세포함=공급가가 맞다.
  **다만 품목에 과세 필드가 없어서 이 판정을 코드가 못 한다** — 내가 품목 이름으로 갈랐다.

  ### 남은 부스러기

  - `VAT_UP` 은 [lineAmount.ts](src/shared/lineAmount.ts):138 상수와 그 시험에만 있다. 앱 코드는 안 쓴다.
    인수인계.md 공용함수 표의 설명("면세 원료가 과세품 원가에 얹는 곱수")과 "원가는 공급가액" 절이 서로 어긋난다.
  - [PriceManager.tsx](components/PriceManager.tsx) 는 AdminApp 이 import 만 하고 **렌더하는 곳이 없다**(죽은 화면).
    살아 있는 건 `ItemPriceManager` 이고, 거기 원가 칸은 사람이 직접 친다 — 그 값이 공급가라는 표시가 없다.

  ### 설계에서 정해 줬으면 하는 것

  1. **전표 줄에 `itemId` 를 남길 것인가.** 남긴다면 옛 전표 151줄은 어떻게 되짚나(이름 매칭 1회 이관?).
     안 남긴다면 원가 감사는 무엇으로 하나.
  2. **품목의 과세/면세를 어디에 둘 것인가.** 품목에 필드를 두나, 계정과목에서 끌어오나, 전표에만 두나.
  3. **원가의 단위를 화면에 어떻게 못박나.** `ItemPriceManager` 원가 칸이 공급가라는 것과,
     매입단가(세포함)와 다른 숫자라는 것을 사람이 알 수 있어야 한다.
  4. 지금 부푼 8건을 **되돌릴지** — 되돌리면 그 시점 이후 원가 이력·마진이 바뀐다.

  ### 시험 방법

  `partnerPriceWrites` 시험이 과세 11,000원 → 거래처단가 11,000 · 원가 10,000, 면세 11,000원 →
  둘 다 11,000 임을 잠근다. 값 정정은 백업 · dry/apply/undo · DB-CHANGELOG 를 갖춘 스크립트로 한다.
  측정은 `partner_item`(in) 과 `items.cost` 를 견주면 위 표가 그대로 나온다.


- ☐ **스마트스토어 주문을 커머스API 로 끌어온다. (조사 끝 · 착수 전)**

  사장님(2026-09-12): "스마트스토어 api 어케 물린다했지" → 저장소에 **기록이 없었다.**
  코드에도 문서에도 연동 흔적이 없고, `SmartStoreAnalytics` 는 이미 들어온 주문을 분석하는
  화면일 뿐이다. 지금은 스마트스토어 주문을 **손으로 넣고 있다.** 그래서 새로 조사했다.

  ### 어디에 붙이나 — **Cloud Functions 다. 앱에서 부르면 안 된다**

  토큰을 받으려면 **클라이언트 시크릿으로 서명을 만들어야 한다.** 그 값이 브라우저에 실리면
  누구나 우리 스토어 주문을 읽고 바꿀 수 있다. 이미 있는 `functions/` 에 스케줄 함수를 둔다
  (`dailyAutoVoucher` · `monthlyInventorySnapshot` 이 쓰는 `onSchedule` 과 같은 방식).
  시크릿은 코드에 적지 말고 함수 환경변수/시크릿에 둔다.

  ### 인증 (확인한 것)

  - 토큰: `POST https://api.commerce.naver.com/external/v1/oauth2/token`
    — **`application/x-www-form-urlencoded`** 만 받는다(JSON 아니다).
  - 파라미터: `client_id` · `timestamp`(밀리초 13자리) · `client_secret_sign` · `grant_type=client_credentials` · `type=SELF`
  - 서명: `bcrypt(비밀번호 = "{client_id}_{timestamp}", 소금 = client_secret)` → **base64**
  - 커머스API센터에서 만든 판매자 앱은 **`type=SELF` 로만** 토큰이 나온다(`SELLER` 는 안 된다).
  - 호출할 때: `Authorization: Bearer {access_token}`
  - 토큰 유효기간은 공식 문서를 봐야 한다 — 찾은 자료에 안 적혀 있다. **받은 응답의
    `expires_in` 을 그대로 쓰고, 만료 전에 다시 받는 식으로 짠다**(숫자를 코드에 박지 않는다).

  ### 주문 가져오는 차례 (두 걸음이다)

  1. `GET /external/v1/pay-order/seller/product-orders/last-changed-statuses`
     — **바뀐 것만** 준다. 마지막으로 본 시각을 어딘가에 적어 두고 그 뒤로만 묻는다.
  2. `POST /external/v1/pay-order/seller/product-orders/query`
     — 1번이 준 `productOrderIds` 배열로 **상세**를 받는다.

  ### 우리 쪽에서 정해야 할 것

  - **거래처** — 스마트스토어 주문을 어느 거래처로 달 것인가. 지금 스마트스토어 채널
    거래처가 여럿 있다(강광덕(스마트)·강용구(스마트)…). 주문자마다 거래처를 만들 것인지,
    `스마트스토어` 한 곳으로 모을 것인지 사장님 판단이 필요하다.
  - **품목 잇기** — 스토어 상품코드 ↔ 우리 `items`. 품목에 스토어 상품코드 칸을 두는 게
    깔끔하다. 못 이은 주문은 **만들지 말고 확인사항으로 띄운다** — 엉뚱한 품목으로 들어가면
    재고·원가가 통째로 틀어진다.
  - **두 번 들어오지 않게** — `productOrderId` 를 주문 문서에 적고, 있으면 건너뛴다.
    (원료 명령의 `operationId` 와 같은 생각이다.)
  - **상태 이어 맞추기** — 스토어의 발주확인·발송처리와 우리 주문 상태(대기중·작업완료·
    출고완료)를 어느 쪽이 이끌 것인가. 한쪽만 바꾸면 두 곳이 갈린다.
  - **호출 제한** — 공식 문서에서 확인해야 한다. 몇 분마다 부를지는 그 뒤에 정한다.

  ### 먼저 할 일

  커머스API센터에서 **판매자 애플리케이션을 만들고 client_id·client_secret 을 받아야**
  아무것도 시작이 안 된다. 그 값은 **저장소에 넣지 않는다** — 사장님이 받으시면
  함수 시크릿으로 넣는 법을 알려드린다.

- ☐ **[Claude → Codex] 원격 연결이 안 된다고 하신다. 확인 바람.**

  사장님(2026-09-10): "지금 코덱스랑 원격 연결 안되는데 코덱스한테 얘기해줘".
  어떤 연결인지(클라우드 세션·MCP·터미널 중 무엇인지)는 못 들었다. 그쪽에서 보이는 오류를
  확인하고, 사장님이 손댈 게 있으면 여기 적어 달라. 나는 로컬 저장소로만 일하고 있어서
  그 연결 상태를 볼 수가 없다.

## 주문·배송 — 이어서 할 것

- ☐ **전표와 주문이 서로 다른 물건을 가리키는 줄 6개.** (2026-09-13 확인)

  전표 줄에 품목 id 를 채우는 1단계에서 8줄은 주문에서 가져와 이었다(DB-CHANGELOG 2026-09-13).
  남은 것 중 계정 줄(택배비·임대료·가공비…)을 빼면 여섯 줄인데, **이건 자동으로 이으면 안 된다** —
  전표와 주문이 실제로 다른 물건이다.

  | 전표 | 전표에 적힌 것 | 주문에 있는 것 |
  |---|---|---|
  | 260813-05 해피유통(포천) | 참기름/병/분/전통/**300ml** 1,000개 | 참기름/병/분/전통/**350ml** 50 |
  | 260907-009 초당노인복지기관 | 선물세트(300ml) **2호** 100개 | 선물세트(300ml) **3호** 100 |
  | 260804-03 모란식품 | 참기름/**특**/모란/1750ml · 병/특A/모란/350ml | 골드A · A · 분 … (특 없음) |
  | 260812-02 세화식품 | 시골향 들깨가루(중간)/4kg | 시골향들깨가루(중간4kg) — **표기만 다름** |
  | 260814-03 알이네식품 | 진참기름/A/1750ml 36개 | 같은 이름이 있지만 그 주문 줄이 **지워진 품목 `p-150`** 을 가리킨다 |

  규격·호수가 다른 것은 **전표를 손으로 고쳤거나 실제로 다른 걸 내보낸 것**이라 사장님이 봐야 한다.
  표기만 다른 세화식품 한 줄은 이어도 된다.

- ☐ **이름이 같은데 라벨·캡만 다른 품목들 — 이름만 보고는 못 고른다.** (2026-09-13 확인)

  `시골향참기름/A/1750ml` 은 **p-106(물엿캡-빨강·시골향참② 라벨)** 과
  **p-116(물엿캡-노랑)** 둘이다. `특A/1750ml` 은 p-25(빨강·참④)·p-203(파랑·참①),
  `특A/1800ml` 은 p-121(빨강n)·p-1777096833351(노랑n).
  **중복이 아니라 서로 다른 물건이다**(2026-09-13 사장님: "bom이 다른거 아니야? 이름은 같은데?").
  합치면 안 된다.

  문제는 **화면에서도 사람이 못 고른다**는 것이다 — 드롭다운에 같은 이름이 둘 뜬다.
  이름에 라벨·캡을 드러내든지(예: `…/A/1750ml(노랑)`), 고르는 자리에서 라벨을 같이 보여 주든지
  해야 한다. 어느 쪽으로 갈지는 사장님 판단.

- ☐ **배송순서 '한 차로 묶기' 를 되살린다.**

  같이 가는 주문을 묶어 왼쪽 띠로 잇던 기능인데 **Codex 병합(`a08a811`)이 통째로 걷어냈다**
  (`groupPick`·`confirmGroup`·`withGroup` 전부). 작업순서 쪽 묶기는 살아 있다 — 배송만 없다.

  **왜 그냥 못 되돌리나** — 옛것은 묶음을 **날짜별 계획 문서**에 넣었는데(`planOf`/`savePlan`),
  그 구조도 같은 병합이 걷어내고 지금은 `settings/deliveryOrdering` 한 문서
  (`{ ordering, timeSlots }`)만 쓴다. 되살리려면 그 문서에 `groups`(주문id → 묶음id)를
  하나 더 두고, 고르기 → "고른 N건 한 차로 묶기" UI 를 붙이면 된다.
  줄을 붙여 세우는 셈은 이미 공용이다(`shared/rowGroup.clusterByGroup`) — 그건 그대로 쓴다.

- ☐ **병합이 지운 다른 기능이 더 있는지 훑는다.** 하루에만 넷이 나왔다 —
  주문 끌어오기(배송·작업 둘 다), 월간 캘린더의 공용 카드, 이력 탭 여는 길, 오전/오후 구분.
  `git diff 06626e0 a08a811 -- components/` 를 훑으면 더 나올 수 있다.

- ☐ **정정 스크립트도 공용 서비스를 지나게 한다.** `rawWriteGuard.test` 가 앱 코드는 지키는데
  `scripts/` 는 통째로 빼 준다(일회성 도구라서). 9/11 의 원료 상태 갈림도 그 경로에서 왔다.
  `fix-raw-state-to-lots.mts` 처럼 `executeRawInventoryCommand` 를 쓰면 같은 일이 안 난다.
  규칙으로 박을지는 사장님 판단.

## 사장님 판단이 필요한 것

| | 무엇 | 왜 멈춰 있나 |
|---|---|---|
| ☐ | **표준 계정과목 이전** | 세무사 확인 대기. 계획은 [로컬전용/docs/표준계정과목-이전계획.md](로컬전용/docs/표준계정과목-이전계획.md), 스크립트는 `scripts/fix-standard-chart.mts` — **아직 돌리지 않았다.** 붙여 주신 표준 목록에 원재료비·부재료비가 없어서 500/505를 어디로 보낼지가 남은 물음이다. |
| ☐ | **박스 재고 모델** | 제조한 박스를 그대로 내보내나, 아니면 낱개로 풀었다 다시 담나. 이 답에 따라 재고 모델이 갈린다. |
| ☐ | **낱개 단가가 1/입수로 들어간 7건** | 낱개 원가보다 한참 싸서 팔 수 있는 값이 아니다 — 대한농산 탈피 **750**(원가 4,600), 대성농산 탈피 780, 현대유통 A/1750 **1,800**(원가 11,614), 용두쭈구미 A/1750 1,900, 청정식품 특A/1800 **1,900**(원가 12,910), 대승종합 볶음검정참깨 **1,270**(원가 10,600), 대성농산 볶음검정참깨 1,100. 같은 거래처 **박스 줄에 적힌 값**(7,500·18,000·19,000·12,700…)이 맞는 값으로 보이는데, 판매가라 내가 못 정한다. `scripts/fix-box-partner-price.mts` 미리보기가 id 까지 낸다. |
| ☐ | **박스와 낱개의 과세/면세가 갈린 11건** | 같은 거래처·같은 물건인데 한쪽은 과세 한쪽은 면세다. 과세 여부는 **거래처마다 다르다**(2026-09-10 사장님)라 코드가 정할 수 없다. 세화·유원(들기름/1750), 대농(들깨가루 중간), 엠에스·고창·진보(골드A/350), 가득찬(원액/350), 거산(들깨가루 중간), 대왕(검정참깨가루), 무경·짠지네(볶음검정참깨). 목록은 같은 스크립트 미리보기에 나온다. |
| ☐ | **임가공 박스 원장 부족분 153kg 을 메울 것인가** | 임가공 박스가 열 배 적게 찍히던 결함(2026-09-11 고침)으로 원장에 안 들어간 볶음참깨. **9/10 실사정정 앞의 1,668kg 은 그 정정에 이미 먹혔다** — 되메우면 두 번 빼는 셈이다. 정정 **뒤**의 2줄만 진짜 비어 있다: 9/10 한울유통 18kg(`ORD-1789028770016`) · 9/11 무경유통 135kg(`ORD-1788936186065`). 목록 `scripts/임가공박스-원장부족.csv`, 조사 `scripts/diag-oem-box-ledger.mts`. |

## 날짜가 정해진 것

| | 언제 | 무엇 |
|---|---|---|
| ☐ | **2026-10-30까지** | **Cloud Functions 런타임을 Node.js 20 → 22 로 올린다.** 아래 참조. |
| ☐ | 2026-12-31 결산 | **첫 거래처 앵커를 박는다.** `npx tsx scripts/stamp-partner-anchor.mts --year=2026 --apply` (기본은 미리보기). 지금은 앵커가 없어 기초일부터 읽는다 — 동작은 같고 읽는 양만 다르다. 미리보기로 이미 확인했다(태백 64곳·92장, 풍회 5곳·8장, 미결 합 = 잔액 전부 일치). |

### Node.js 20 런타임 종료 (2026-10-30)

배포할 때마다 경고가 뜬다(2026-09-12 확인):

> Runtime Node.js 20 was deprecated on 2026-04-30 and will be decommissioned on 2026-10-30,
> after which you will not be able to deploy without upgrading.

**종료되면 배포가 막힌다.** 이미 올라가 있는 함수가 그 순간 멈춘다는 말은 아니지만,
**고칠 수가 없어진다** — 이번 알림 건처럼 함수를 한 줄 고쳐 올리는 일이 아예 안 된다.
구글이 언제까지 굴려 주는지는 보장이 없으니 기한을 믿고 미룰 일은 아니다.

걸린 함수 여섯 (`functions/src/index.ts`) —
`findUsername` · `requestPasswordReset` · `monthlyInventorySnapshot` ·
`dailyAutoVoucher` · `notifyNewOrder` · `notifyChatMessage`.
아이디 찾기·비밀번호 재설정이 여기 걸려 있어 **막히면 로그인 문의를 손으로 받아야 한다.**

**할 일** — `functions/package.json` 의 `engines.node` 를 `"22"` 로 올린다.
같이 뜨는 경고가 하나 더 있다: `firebase-functions` 가 `^5.0.0` 으로 낡았다(최신은 6.x,
**깨지는 변경이 있다**). 둘을 한 번에 올리지 말고 **런타임부터 올려 배포가 되는지 보고**,
`firebase-functions` 는 따로 올린다. 올린 뒤 여섯 함수를 한 번씩 돌려 봐야 한다 —
알림 두 개는 주문·오피스톡을 실제로 넣어 보는 게 유일한 확인 방법이다.

## 2026-09-09 코드 감사 — 먼저 고칠 것

코드·규칙·정적 검사를 대조한 결과다. 운영 DB 값은 이번에 읽지 않았으므로, 데이터 건수는
진단 스크립트로 따로 재야 한다. 아래는 **실제 실행 경로와 코드 주석으로 확인한 것**만 적었다.

### P0 — 금액·재고·권한이 틀어질 수 있음

- ◐ **과세 매입 원가 — 새는 자리는 막았다(2026-09-10). 과세/면세 확인만 남았다.**

  **한 것** — 매입 원가를 **전표 줄의 공급가액**으로 넣는다(`costFromPurchaseLine`). 부푼 원가
  8건을 낮췄다(−12,761원). 견적서 단가 칸을 **판매단가**로 통일했다(과세 견적이 10% 비싸게
  나가던 것). `VAT_UP` 잔재와 죽은 `PriceManager.tsx` 를 걷어냈다. 거래처 단가의 빈
  과세/면세 265건을 채워 **빈칸 0건**을 만들었다.

  **남은 것 — 사장님이 직접 봐야 한다.** 과세/면세는 **품목이 아니라 거래처가 가른다**
  (2026-09-10 사장님: "거래처별로 달라"). 그래서 품목 갈래로 채운 것 중 틀린 게 섞여 있다.
  확인할 198건을 `scripts/과세면세-확인목록.csv` 로 뽑아 뒀다
  (`npx tsx scripts/list-taxtype-review.mts` 로 다시 뽑는다).
  거래처 통째로 맞추는 건 `scripts/fix-partner-all-exempt.mts <거래처id...> --apply` (`--과세` 도 됨).

  아래는 처음 적어 둔 진단이다 — **주 경로(`partnerPriceWrites`)는 이미 고쳐져 있었다.**


  원칙과 2026-09-06 일괄 정정은 `items.cost = 공급가액`이다. 그러나 전표의 입력 단가는
  [lineAmount.ts](src/shared/lineAmount.ts) 규칙상 **세포함**인데,
  [partnerPriceSync.ts](src/shared/partnerPriceSync.ts)의 매입 `costUpdates`가 그 값을 그대로
  `items.cost`에 보낸다. 매입 전표 발행·수정 뒤 과세 품목 원가가 10% 부풀 수 있다.
  [TradeStatement.tsx](components/TradeStatement.tsx)의 매입단가 직접 수정과 죽은 화면인
  [PriceManager.tsx](components/PriceManager.tsx)에도 같은 복사가 남아 있다.

  [인수인계.md](인수인계.md)의 “원가는 공급가액” 절은 맞지만, 앞의 공용함수 표에는 아직
  `VAT_UP`을 “면세 원료가 과세품 원가에 얹는 곱수”라고 적어 서로 모순된다. 실제 앱 코드는
  `VAT_UP`을 쓰지 않고 시험과 문서만 붙들고 있다. 다음 사람이 옛 셈을 되살릴 위험이 있다.

  **고칠 것** — 거래처 매입단가(세포함)와 품목 원가(공급가)를 한 필드처럼 복사하지 않는다.
  `lineAmount(1, price, taxType === '면세').supply`를 감싼 공용 함수 하나로 매입 원가를 만들고,
  전표·단가관리·품목관리 모두 그 함수만 쓴다. `partnerPriceWrites` 시험은 과세 11,000원이
  거래처 단가 11,000원·원가 10,000원, 면세 11,000원은 둘 다 11,000원임을 잠근다.
  옛 `VAT_UP` 설명·상수·그것만 지키는 시험도 함께 없앤다.
  그 뒤 `scripts/diag-*`로 9월 6일 이후 다시 오염된 품목을 찾고, 고칠 값은 백업·dry/apply/undo
  스크립트와 DB-CHANGELOG를 갖춰 반영한다.

- ☐ **로그인과 관리자 인증이 브라우저 안의 평문 비교라 권한을 지키지 못한다.**

  [AuthPage.tsx](src/shared/components/AuthPage.tsx)는 로그인하기 전부터 `employees` 전체를 받아
  아이디·비밀번호를 브라우저에서 비교하고, 이름·전화번호만 맞으면 **기존 비밀번호를 화면에
  그대로 보여준다.** 로그인한 직원 객체(비밀번호 포함)는 `localStorage.tb_user`에 저장된다.
  [AdminAuthModal.tsx](components/AdminAuthModal.tsx)는 입력 비밀번호와 DB 관리자 비밀번호를
  `console.log`로 함께 출력하며, 설정이 없으면 `0000`을 쓴다.

  더 큰 문제는 [firestore.rules](firestore.rules)와 [storage.rules](storage.rules)가 익명 로그인만
  되어도 전 문서·전 파일의 읽기/쓰기를 허용한다는 점이다. 직원 앱의 메뉴 숨김과 관리자
  비밀번호 창은 보안 경계가 아니다. 앱을 열기만 해도 `useAppData`가 주문·전표·자금·직원정보를
  로그인 전에 구독한다.

  **고칠 순서** — Firebase Auth 계정 전환 → 직원 문서와 Auth UID 연결 → 관리자 custom claim
  또는 서버 검증 → 컬렉션·회사·역할별 Firestore/Storage 규칙 → 평문 비밀번호·찾기·기본 0000·
  콘솔 출력·localStorage 사용자 사본 제거. 에뮬레이터에서 직원/관리자/미로그인 규칙 시험을
  먼저 만든 뒤 운영 인증을 옮긴다.

  **2026-09-16 즉시 노출 축소 완료** — 관리자 비밀번호 콘솔 출력과 기본 `0000`, 비밀번호
  찾기의 평문 표시, `localStorage`의 비밀번호·연락처·푸시표 저장을 제거했다. 로그인 전에는
  로그인 판정에 필요한 `employees`만 읽고 주문·전표·자금·재고 구독은 시작하지 않는다.
  빈 컬렉션 구독과 구독 오류 처리도 보강했다.

  **아직 보안 경계는 아니다.** 로그인 화면이 `employees` 평문을 읽는 구조와 익명 사용자 전면
  허용 rules가 남아 있다. 운영 계정을 끊지 않는 전체 전환 순서와 완료 조건은
  [보안-개편-계획.md](로컬전용/docs/보안-개편-계획.md)에 통합했다. 다음 구현은 Callable 서버 로그인과
  Auth UID·비밀번호 해시 이관이며, 에뮬레이터 권한시험 없이 운영 rules부터 잠그지 않는다.

### P1 — 현재 기능이 조용히 빠지거나 느려질 수 있음

- ☐ **회계·전표 대형 화면과 전역 구독을 기능 경계로 나눈다.**

  현재 `AdminApp.tsx` 4,833줄, `TradeStatement.tsx` 3,904줄, `ProfitAnalysis.tsx` 2,055줄이다.
  `TradeStatement`는 전표 목록·작성·수금지불·일반전표·인쇄·엑셀·단가수정까지 한 컴포넌트가
  맡고, `useAppData`는 현재 화면과 무관한 컬렉션까지 전역 구독한다. 목표 구조와 안전한 이관 순서는
  [전표-원가-원장-재무-통합설계.md](로컬전용/docs/전표-원가-원장-재무-통합설계.md)의
  **9~12장**을 따른다. 화면만 잘게 잘라 같은 거대 props를 넘기는 분리는 하지 않는다. 먼저 순수
  계산과 저장 명령을 빼고, 다음에 화면·구독을 기능별로 가른다.

- ☐ **자유문장 주문을 읽어 복사주문 확인 화면으로 넘긴다.**

  카톡·문자로 들어온 `엄마랑선식 1.8리터 참기름골드 3개 ...` 같은 문장에서 거래처·품목·규격·
  수량·메모를 구조화한다. AI가 Firestore에 바로 쓰지는 않는다. 먼저 거래처를 고르고, 그 거래처의
  `partner_item`에 연결된 품목만 후보로 삼아 **이름이 아니라 `partnerId`·`itemId`로 확정**한 뒤
  기존 복사주문 확인 화면에 채운다. `1.8리터=1800ml`, `소주병=350ml` 같은 별칭 사전을 먼저 쓰고,
  확신이 낮거나 박스 수량처럼 사람 판단이 필요한 줄은 미확정으로 표시한다. 확인 전에는 저장하지
  않고, API 키와 모델 호출은 서버에서만 처리한다. 결과 형식·오류·재시도·중복 주문 방지까지 설계한
  뒤 구현한다.

- ☐ **거래처 원장 날짜 필터 — 기간 전 잔액을 기초로 세우는 방식으로 붙인다.**

  현재 [PartnerLedger.tsx](components/PartnerLedger.tsx)와 `buildPartnerLedger`는 전체 기간만
  만든다. 날짜를 단순히 화면에서 잘라내면 잔액이 틀어진다.

  **규칙** — `from~to`를 원장 계산에 넘겨 `from` 이전 모든 채권·채무 변동의 합을 `기초`로,
  기간 안 전표만 `발생`, 기간 안 수금·지불만 `결제`로 세운다. 따라서 8월 31일 기말은
  9월 1일 기초와 정확히 같다. `기초 + 기간발생 - 기간결제 = 기말`과 월 경계·같은 날 시각순서를
  시험으로 잠그고, 화면은 당월/전월/기간 버튼과 날짜 두 칸을 기존 기간 UI에 맞춰 단다.

- ☐ **공용 구독이 빈 컬렉션과 인증 실패를 정상적으로 끝내지 못한다.**

  [firebaseService.ts](src/shared/services/firebaseService.ts)의 `subscribeToCollection`은
  `snapshot.docChanges().length === 0`이면 callback을 부르지 않는다. 처음부터 빈 `orders`/`items`는
  `markLoaded`가 실행되지 않아 로딩이 끝나지 않을 수 있다. `onSnapshot` 오류 callback도 없고,
  [firebase.ts](src/shared/firebase.ts)의 익명 로그인 실패는 `catch(console.error)` 뒤 성공처럼
  이어져 화면이 빈 상태로 멈춘다. 여러 `catch(() => {})`도 분류표·기초잔액·과거 전표 실패를
  “데이터 없음”처럼 보이게 한다.

  **고칠 것** — 최초 빈 snapshot도 `[]`를 전달하고 오류 상태를 별도로 올린다. 인증 실패는
  `authReady`를 reject한 채 유지하며 재시도/오류 화면을 낸다. 필수 데이터와 선택 데이터의 실패
  정책을 나눠 조용한 catch를 걷어낸다.

- ☐ **직원 앱도 관리자 데이터와 코드를 대부분 통째로 받는다.**

  [StaffApp.tsx](src/features/staff/StaffApp.tsx)는 42줄짜리 껍데기로 `AdminApp`에
  `isAdmin=false`만 준다. 그래서 직원 앱도 공용 `useAppData`의 전체 전표·자금·정산·직원 구독을
  실행하고, 관리자 화면 코드도 같은 번들에 들어간다. `AdminApp.tsx` 4,700줄,
  `TradeStatement.tsx` 3,895줄, `HaccpChecklist.tsx` 4,561줄, `ItemList.tsx` 3,824줄이라
  작은 수정도 영향 범위와 첫 로딩 비용이 크다.

  **고칠 것** — 인증을 먼저 세운 뒤 역할별 query/서비스를 나누고, 화면은 lazy import한다.
  장기 분리는 아래 “직원 앱과 관리자 앱을 가른다”의 한 경로 설계와 같이 진행한다.

- ☐ **React memo/effect 의존성 경고를 실제 오류와 설정 잡음으로 나눠 처리한다.**

  앱 경로만 ESLint한 결과 `react-hooks/exhaustive-deps` 52건이다. 특히 주문 생성의 품목 목록
  memo가 실제로 읽는 `items` 대신 `products`를 의존성에 넣고, HACCP 화면에도 누락된 effect
  의존성이 여럿 있다. 값이 바뀌어도 옛 목록을 보여줄 수 있다. 반면 이벤트 안의 `Date.now()`를
  렌더 중 호출로 보는 경고처럼 오탐도 있다. 파일별로 값 변경 회귀시험을 먼저 만든 뒤 고친다.

### P2 — 죽은 코드·중복·도구 정리

- ☐ **죽은 파일과 기호를 지우고, 죽은 코드 시험의 눈을 고친다.**

  확인된 후보는 [PriceManager.tsx](components/PriceManager.tsx)(import만 있고 렌더 없음),
  [company.ts](src/config/company.ts)(`INITIAL_*` 사용처 0, 옛 시드와 예제 평문 계정 포함),
  [useAppData.ts](src/hooks/useAppData.ts)(사용처 없는 재수출), `AuthPage.handleQuickLogin`,
  `ViewType`의 중복 `'partners'`다. `App.tsx_backup`·`index.html_backup`·
  `components/Dashboard.tsx_backup`도 실행되지 않는 옛 사본이다.

  [unusedFile.test.ts](src/shared/unusedFile.test.ts)는 **경로가 아니라 파일 basename만** 비교하므로
  동명 파일이 하나라도 import되면 다른 죽은 파일도 산 것으로 오인하고, import된 파일 안의
  죽은 export/함수는 못 잡는다. 실제 import graph를 보거나 `knip` 같은 도구로 바꾸고, 후보마다
  진입점·동적 import·스크립트 사용 여부를 확인한 뒤 삭제한다.

- ☐ **생성물과 안 쓰는 패키지를 저장소에서 걷어낸다.**

  `functions/lib/index.js`와 `.map`은 `deploy:functions`가 build로 다시 만드는 생성물인데 Git에
  들어 있다. 루트 `package.json`의 `express`, `socket.io`, `socket.io-client`는 앱 소스 사용처가
  없고, AI 화면은 “준비 중”만 그리는데 `@google/genai`·`@google/generative-ai` 두 벌과
  Vite `vendor-ai` 수동 청크가 남아 있다. 빌드·functions 실행을 각각 확인한 뒤 생성물은
  `.gitignore`, 미사용 패키지와 청크는 제거한다.

- ☐ **마이그레이션이 끝난 호환 필드를 목록으로 관리하고 단계적으로 없앤다.**

  [types.ts](src/shared/types.ts)에 `shippingRule`·`itemType`·`partnerId`·옛 박스 설정·팔레트
  `outstanding`·자금 `direction` 등 `@deprecated` 필드가 남아 있고, 일부는 아직 읽는 코드도 있다.
  호환 종료일과 운영 DB 잔존 건수를 모르면 계속 둘 수도, 안전하게 지울 수도 없다.

  **고칠 것** — 필드마다 쓰기 경로/읽기 fallback/운영 잔존 건수/대체 필드/삭제 버전을 표로 만들고,
  새 쓰기 차단 → 진단 → 백업 가능한 이관 → 읽기 fallback 제거 → 타입 제거 순으로 한 벌씩 끝낸다.

- ☐ **거대한 화면에서 도메인 명령과 UI를 분리한다.**

  우선 대상은 `AdminApp`의 자동삭제·주문저장·원료원장, `TradeStatement`의 발행·단가동기화,
  `ItemList`의 입고·재고반영이다. 화면 안에서 DB 여러 쓰기를 직접 이어 붙이지 말고 시험 가능한
  서비스 명령으로 옮긴다. 줄 수만 줄이는 분해가 아니라, 실패 경계와 트랜잭션 경계를 먼저 나눈다.

## 확인해야 할 것

- ☐ **해피유통(네이버커머스)의 기존 거래처 단가 정정** (2026-09-08 확인).
  생들기름 300ml 낱개 전표는 6,000원·과세인데 낱개 연결은 단가·과세구분이 비어 있고,
  같은 이름의 박스 연결에 6,000원이 있다. 참기름 300ml의 4,600원도 같은 상태다.
  이름 대신 품목 ID로 저장하는 코드는 고쳤지만 **운영 데이터는 아직 안 고쳤다**.
  기존 전표 줄에는 itemId가 없으므로 이름으로 일괄 추정하지 말고 주문·BOM의 ID를 대조해야 한다.

- ☐ **전표 입력 줄이 폰에서 눌린다** — `VoucherComposer` 두 곳(자금전표 쪼개기 966줄, 대체전표 줄 818줄).
  못 박은 칸이 314·600px 이라 적요 입력칸(`flex-1`)이 폰에서 0px 이 된다.
  **옆으로 미는 걸로는 안 된다** — 타이핑하다 칸이 화면 밖으로 나가 지금보다 나빠진다.
  계정·금액을 둘째 줄로 **접는**(`flex-wrap`) 쪽으로 고쳐야 한다. 사장님이 쓰는 화면이라 눈으로 보고 정할 것.
  그물([gridOverflow.test.ts](src/shared/gridOverflow.test.ts))의 **봐주는것**에 들어 있다 — 고치면 거기서 뺀다.

- ☐ **계정과목 필터 판정(`acctHit`)이 아직 화면 안에 있다** (2026-09-05 사장님 지시로 적어 둠)

  전표 목록 필터 줄의 **'계정 선택'** 드롭다운. 고른 값 하나(`'code:931'`·`'group:...'`
  같은 글자)를 놓고 "이 전표가 거기 걸리나"를 판정하면서 계정표를 타고 올라간다.

  **값어치는 낮다** — 돈이 안 걸려 있다. 틀려도 목록에 덜 나오거나 더 나올 뿐이고
  잔액·전표 금액은 안 틀린다. 그래서 뒤로 미뤘다. 계정 화면을 손볼 일이 생기면 같이 한다.
  (계정을 **줄**로 보는 규칙 자체는 이미 [timelineRows.ts](src/shared/timelineRows.ts)
  `rowCodes` 로 빠져 있고 시험이 붙어 있다.)

- ☐ **일반전표 창의 갈래 안 갈래(상환·급여·보험·세금)는 단추가 없다** — 템플릿을 골라야만 들어간다.
  그게 맞는 설계인지 안 정했다. 템플릿 없이 그 갈래로 끊고 싶은 날이 있으면 막힌다.
- ☐ **반품 전표(총액 음수)가 배분에서 상계되지 않는다.** (인천)청정식품 −693,000이 그 예. 잔액에서는 상계되는데 전표별 FIFO 배분은 못 건드려서, 그 −693,000이 영영 안 쓰인 채 남는다. 화면에는 티가 안 나고(잔액은 맞다) 앵커도 그대로 옮기게 해 뒀지만, **다음 매입에서 저절로 깎여야 맞는 것 아닌가**는 안 정했다.
- ☐ **131 선급금이 대변으로 30,740,650 밀려 있다.** 8/4 `260804-14`가 선급금 50,740,650을
  매입(500)으로 털었는데, 그 앞에 선급금을 세운 전표가 없다. 서 있는 건 8/20 웨일스드림
  20,000,000 하나뿐이다. 수입 대금을 먼저 보낸 전표가 안 끊겼거나, 다른 계정으로 들어갔다.
  **자산 계정이 대변 잔액인 건 말이 안 되니 어느 쪽이든 맞춰야 한다.** (2026-09-01 발견,
  선수금 오기입 고치다 나옴 — 그 건과는 별개다.)
- ☐ **`runOne`의 `busy` 이중 방어** — 단추가 이미 잠겨 있어 화면으로는 시험할 방법이 없다. 코드로만 확인했다.

## 나중에 할 것 — 재고·원가

### ☐ 매입 단가 칸에 서로 다른 게 섞여 있다 (2026-09-02 실측)
"매입처가 여럿이면 품목에 원가를 두기 애매하다"에서 출발했는데, 재보니 **매입처가 문제가 아니었다.**
매입 품목 197개 중 매입처 둘 이상은 **11개**뿐이고, 그 11개를 열어 보니 값이 갈린 게 아니라
**한 칸에 다른 걸 넣어 놨다.**

    참깨 (kg, 벌크)         cost 4,021
      한국농수산물유통공사  4,021    원물값
      카프코              4,090    원물값 — 2% 차이, 서로 맞는다
      화통무역 · 풍회유통       1    빈 값
    볶음검정참깨 (kg)        cost 11,550
      (인천)청정식품       11,550    kg 값
      청정식품            233,200    ÷11,550 = 20.19 → **20kg 한 포대 값**
    생들기름 (L)            cost 12,566
      현대농업            209,000    ÷18 = 11,611 → **18L 말 값**
    볶음참깨-낱개/1kg (개)    cost 5,510
      푸미푸드                550    원물값의 1/10 → **볶는 임가공비로 보인다**

`partner_item` 칸이 `{ itemId, partnerId, Direction, price, taxType, Account_Code }` 뿐이라
**550이 kg값인지 임가공비인지 알 방법이 없다.** 매입처가 하나인 품목도 같은 문제를 갖고 있다.

**정할 것** — 품목의 `cost` 는 입력인가 결과인가. 지금 `costSource` 가 `rollup`(BOM) / `manual`(손입력)
둘인데, 매입해서 쓰는 것들에는 **`purchase`(실제 매입에서 굴림 — 이동평균)** 가 빠져 있다.
실제로 품목 cost 가 매입단가 범위 **밖인 게 8개**다. 그걸 붙이려면 `partner_item` 에 최소 두 칸이
먼저 있어야 한다 — **단위**(무엇 하나당인가)와 **종류**(원물/임가공).

### ☐ 품목이 사라졌는데 남아 있는 매입 줄 16개
`partner_item` 의 `itemId` 가 `items` 에 없다. 지우기 전에 어느 품목이었는지 확인할 것.


### ☐ 재고관리를 한 번 갈아엎는다 (2026-09-01 사장님 판단)
훑다 나온 것들이 한 갈래를 가리킨다 — **같은 사실을 여러 군데서 따로 들고 있다.**

| 무엇 | 상태 |
|---|---|
| 개입수 | BOM · 규격 글자 · `boxSize` · '향미유면 12' — 근거가 넷이었다. BOM 우선으로 잡았지만 셋이 임시 다리로 남아 있다 |
| 박스 규격 | 낱개를 고쳐도 안 따라갔다. 전파를 붙였지만 **파생값을 저장하는 구조** 자체가 원인이다 |
| 개입수 12 | 화면 여덟 군데에 손으로 박혀 있었다 |
| 재고 실사 | `boxSizeOf` 가 모르면 12로 짐작한다 — 근거 없이 곱한다 |
| 박스 vs 낱개 | 별개 품목으로 따로 센다(사장님 확인). BOM 은 등가 관계일 뿐 차감 아님 |

**갈아엎을 때 정할 것** — 파생값(규격 글자·개입수)을 저장할 것인가 읽을 때 만들 것인가.
지금은 저장해 두고 전파로 맞추는데, 전파를 한 군데라도 빠뜨리면 조용히 어긋난다.


- ☐ **price → cost 통합** — `Product.price`를 `cost`로. 뜻이 겹치는 필드가 둘이라 어느 쪽이 진짜인지 매번 헷갈린다.
- ☐ **OEM 모델 통합** — 현재 가공입고·완제품 로트·판매 수불부·가공비전표는 이어졌다.
  남은 것은 완사입/임가공을 `procureType` 문자열 분기 대신 외주창고·Backflushing·가공비 계정까지
  갖는 한 모델로 묶고, 화면과 원가 계산이 그 모델만 읽게 하는 일이다.
- ☐ **OEM 외주발주도 재개 가능한 작업으로 묶는다** — 지금 `issueOemBatch`는 원료를 하나씩 먼저
  내보낸 뒤 마지막에 배치 카드를 만든다. 원료 둘 중 하나 또는 배치 저장이 실패하면 외주출고만
  일부 끝난 채 재개할 작업 ID가 없다. 배치 카드를 `processing`으로 먼저 세우고 원료별 결정적
  operation ID를 저장한 뒤, 완료된 원료는 건너뛰며 이어서 처리하는 job으로 바꿀 것.
- ☐ **OEM 로트번호 동시 발급** — 같은 날 같은 물질의 규격별 로트번호를 화면의 `items.lots`에서
  세어 `-01`, `-02`로 만든다. 두 가공입고가 동시에 열리면 같은 번호가 나올 수 있으므로
  물질+날짜별 카운터를 가공입고 transaction 안에서 올려 번호를 받아야 한다.
- ☐ **박스/낱개 재고** — 품목 1개 + 박스규격별 variant 재고 + 공유 낱개. 위 '박스 재고 모델' 판단이 먼저다.
- ☐ **배송·선물세트 kit 차감** — 원료배합 반제품은 Phantom 으로 풀었는데(2026-07-10), kit 쪽은 아직이다.
- ☐ **원료수불부 원장 분리** — 전 원료를 주문×품목으로 다시 세려다 박스 수량 10배 버그(주문 데이터 오류 + `autoRawMaterialUsage` 가 `stockUnits` 를 안 씀)를 보고 멈췄다.
  · 2026-09-11: **임가공 쪽 박스 결함은 고쳤다**(`oemLedgerKg`). 그 가지가 박스 수에 규격 앞부분(낱개 용량)을 그냥 곱해 열 배 적게 찍고 있었다. 남은 것은 이 항목 본문의 `autoRawMaterialUsage` 경로다.

## 나중에 할 것 — 직원 앱과 관리자 앱을 가른다

### ☐ 직원 → 관리자로 넘어가는 문을 하나로 판다 (2026-09-02 사장님 방향)
**지금은 경계가 없다.** `StaffApp` 은 42줄 껍데기고 `AdminApp` 에 `isAdmin={false}` 를
주는 게 전부다. 같은 코드·같은 DB 를 양쪽이 직접 읽고 쓴다 — 다른 건 메뉴가 숨는 것뿐.

그래서 **박스를 낱개로 푸는 일이 읽는 쪽(관리자 화면)에서 일어난다.**

| 어디 | 무엇 |
|---|---|
| `poToManualRows` | 발주카드 → 전표 줄 |
| `orderToManualRows` | 주문 → 전표 줄 |
| `stockUnits` | 주문 → 재고 차감 |
| `unpackQty` | 입고 → 재고 |

**사장님 방향** — 관리자 앱은 나중에 ERP 로 따로 떼고 DB 도 가른다. 그러면 직원 쪽에서
**이미 처리를 끝낸 상태로** 넘겨야 하고, 넘어가는 길은 **한 경로**여야 한다.
지금처럼 읽는 쪽에서 풀면, DB 를 가르는 순간 그 코드가 관리자 쪽에 남아 안 풀린 데이터가 온다.

**순서**
1. 직원이 만드는 것(주문·발주·반품·입고)이 지나는 **함수 하나**를 만든다.
   거기서 박스를 풀고 회사·날짜·단위를 정규화한다. 화면들은 그 문만 쓴다.
2. 그 문이 서면 읽는 쪽 풀기를 걷어낸다(위 표 네 곳).
3. 그 문이 곧 계약이 된다 — 직원 DB → (문) → 관리자 DB.

**먼저 정할 것** — 직원이 만들어서 관리자로 넘어가는 게 정확히 무엇인가.
코드로 보이는 건 주문·발주·반품·재고 실사인데, 실제 업무에서 직원이 하는 일과
사장님만 하는 일의 경계를 알아야 문을 제대로 판다.

## 나중에 할 것 — 오피스톡

- ☐ **카톡 공유는 아이폰에서 안 된다** — 애플이 Web Share Target 을 아직 안 받는다.
  홈 화면에 추가해도 공유 시트에 안 뜬다. 고칠 수 있는 게 아니라 **알고 두는 것**에 가깝다.


- ☐ **카톡·문자 내용을 오피스톡으로 바로 공유하기** (2026-09-03 사장님 질문)

  안드로이드 공유 시트에 "오피스톡"이 뜨게 하는 건 **Web Share Target** 이다.
  조건 두 가지 — 폰에 앱이 **홈 화면에 추가(설치)** 돼 있어야 하고, **안드로이드**여야 한다.
  **아이폰은 사파리가 이 기능을 아예 안 받는다.** 아이폰에서는 붙여넣기가 유일한 길이다.

  **글만 받는 길(쉬움, 반나절)** — 카톡 대화 내용 공유는 결국 글이라 이걸로 충분하다.
  `public/manifest-admin.webmanifest` 에 넣는다:
  ```json
  "share_target": {
    "action": "/", "method": "GET",
    "params": { "title": "title", "text": "text", "url": "url" }
  }
  ```
  그러면 카톡에서 공유하면 `/?text=...` 로 앱이 열린다. 앱 첫 화면에서
  `new URLSearchParams(location.search).get('text')` 를 읽어 오피스톡을 열고 입력칸에
  미리 채워 두면 된다. **서비스워커는 안 건드려도 된다.**

  **사진까지 받는 길(어려움)** — `method: "POST"` + `multipart/form-data` 라야 파일이 온다.
  그런데 POST 는 서비스워커가 `fetch` 를 가로채 `formData()` 를 꺼내야 하고,
  지금 [vite.admin.config.ts](vite.admin.config.ts) 는 `generateSW` 모드라 그런 코드를 못 넣는다.
  `injectManifest` 로 바꾸고 서비스워커를 직접 써야 한다 — PWA 자동갱신까지 다시 짜야 하니
  **글만 받는 길부터 해보고, 그걸로 모자랄 때 손댄다.**

- ☐ **첨부 올리는 길 셋이 따로다** — [chatUpload.ts](src/shared/chatUpload.ts) 를 오피스톡만 쓴다.
  [DocumentManager.tsx](components/DocumentManager.tsx) 와 [ItemList.tsx](components/ItemList.tsx) 도
  `uploadBytes` + `getDownloadURL` 을 각자 적어 뒀다. 크기 제한·파일명 걸러내기가 갈릴 자리다.
  통합 시 크기뿐 아니라 허용 MIME·확장자·회사/방 경로를 함께 검사한다. Storage rules 강화와
  같은 배포 묶음으로 진행한다([보안-개편-계획.md](로컬전용/docs/보안-개편-계획.md) 3~4단계).

## 나중에 할 것 — 여러 회사에 파는 준비

지금은 회사 구분이 `companyId` 하나로 걸러지는 정도다. 남의 회사에 팔려면 아래가 필요하다.
자세한 건 [인수인계.md](인수인계.md) 8장.

- ☐ **회사별 인증·보안 경계를 세운다** — 위 P0의 평문 로그인·관리자 인증·전면 허용 규칙을 먼저
  고친 뒤 Auth UID와 회사·역할을 연결한다. 그 다음 모든 query와 rules에 회사 범위를 강제한다.
  중복 계획은 [보안-개편-계획.md](로컬전용/docs/보안-개편-계획.md)로 합쳤다.
- ☐ **DB 갈아탈 여지** — 화면이 firebase 를 직접 부르지 않게. `firebaseService` 로 문을 넓혀 뒀고(`fetchWhere`·`subscribeWhere`·`writeMany`·`mutateDoc`·`fetchByIds`) 새 코드는 그 문만 쓴다. 옛 코드가 아직 많다.

## 나중에 할 것 — 코드 다듬기

- ☐ **`docUnpack` 과 `resolveOrderItem` 은 형제다** (2026-09-05)
  둘 다 박스를 낱개로 푼다. [docOil](src/shared/docOil.ts) 것은 품목 배열이 아니라
  **찾는 함수**를 받아서 모양만 다르다. `resolveOrderItem` 이 둘 다 받게 하면 합칠 수 있다.
  급하지 않다 — 둘 다 이름이 붙어 있고 각자 시험이 있다. 지금은
  [orderUnpackWiring.test.ts](src/shared/orderUnpackWiring.test.ts) 에 예외로 적어 뒀다.

### ☐ 전표번호를 **서버에서 원자적으로** 받는다 — 여러 명이 동시에 끊을 때
지금은 `claimDocNo` 가 **이번 판에서 내준 번호**를 담아 두고 건너뛴다(2026-09-03).
한 사람이 연달아 발행하는 건 이걸로 막힌다 — 목록 갱신을 안 기다린다.

**여러 사람이 동시에 끊으면 못 막는다.** 각자 자기 화면 목록에서 최대값을 보므로 같은
번호가 나올 수 있다. 지금은 사장님 혼자 쓰셔서 안 겪는데, 직원이 전표를 끊게 되면 걸린다.

**막는 법** — Firestore 트랜잭션으로 그날 번호 카운터를 원자적으로 올려 받는다.
발행마다 왕복이 한 번 는다. `functions` 쪽 스케줄러도 같은 카운터를 봐야 한다.

**지금 겹친 것** — 태백 `2026-08-0216` 한 쌍. 그건 경합이 아니라 **개수 기반** 옛 방식
때문이고(22분 차이), 그 방식은 이미 고쳤다. 8월이라 손대지 않는다 —
`docNo` 는 열쇠가 아니라(문서 id 가 열쇠다) 겹쳐도 지금 아무것도 안 깬다.


### ☐ 스케줄러(functions)가 앱 코드를 **진짜로 같이 쓰게** 만든다
지금은 같은 규칙이 **손으로 두 벌** 적혀 있다. 한쪽만 고치면 조용히 갈리고,
2026-09-03 에 실제로 둘이 갈려 있는 걸 찾았다(전표번호 · 재고평가 회사·음수, 합 1,050만원).

**왜 갈라져 있나 — 벽은 셋인데 진짜 벽은 하나다.**

| | 무엇 | 진짜 벽인가 |
|---|---|---|
| ① | `functions/` 가 자기 `package.json`·`tsconfig`·`node_modules` 를 갖는다. `rootDir` 이 그 폴더라 밖을 import 하면 tsc 가 거부한다 | **아니다 — 설정 문제** |
| ② | 앱은 클라이언트 SDK(`firebase/app`), 스케줄러는 서버 SDK(`firebase-admin`) | **그렇다.** SDK 를 문 파일은 못 나눈다 |
| ③ | 도는 곳이 브라우저 vs 구글 서버 | 아니다 — 순수 계산엔 상관없다 |

**나눌 수 있는 것** — SDK 를 안 무는 순수 계산 파일들:
[voucherStamp.ts](src/shared/voucherStamp.ts)(`nextDocNo`·`stampFor`) · [day.ts](src/shared/day.ts) ·
[types.ts](src/shared/types.ts)(`companyOf`·`invSnapDocId`) · [bomCost.ts](src/shared/bomCost.ts) ·
[autoVoucher.ts](src/shared/autoVoucher.ts).

**할 일** — `functions/tsconfig.json` 의 `rootDir` 을 프로젝트 루트로 올리고 경로를 잡아,
저 파일들을 양쪽이 같은 실물로 쓰게 한다. 그러면 손으로 옮겨 적은 벌이 사라진다.

**지금 붙여 둔 테스트는 증상만 잡는다.** [docNo.test.ts](src/shared/docNo.test.ts) 가 functions
소스 **글자**를 읽어 규칙이 갈렸는지 보는데, 이건 그물이지 원인을 없앤 게 아니다.
같이 쓰게 만들면 그 테스트도 지운다.

**주의** — `firebase deploy --only functions` 는 `npm run deploy:all` 에 안 들어 있다.

- ☐ **`fmt` 18벌 정리 — 급하지 않다.** 숫자를 글자로 바꾸는 함수가 파일마다 있다.
  그런데 **11개는 글자 하나 안 틀리고 같고**, 나머지는 일부러 다르다
  (로트는 소수 1자리, 품목원장은 3자리, 원가관리는 뒤에 '원', 롤백요약은 아예 숫자를 낸다).
  갈려도 쉼표가 안 찍힐 뿐 돈이 안 틀리고, 지금 실제로 갈린 것도 없다.
  **화폐 표기 규칙을 바꿀 때** 그때 같이 하면 된다.
- ☐ **`slice(0, 7)` 월 자르기 20파일** — 글자 자르기라 시간대 문제는 없다. 값어치 낮음.

## 나중에 할 것 — 설비

- ☐ **린트 설정과 실제 오류를 갈라 정리한다** — 앱 경로(`components src apps`) 기준 524건
  (오류 157·경고 367)이다. 그중 미사용 기호가 314건이고 `no-undef` 83건은 TS·브라우저 환경
  설정 잡음이 섞여 있다. 환경 설정을 먼저 바로잡고, 남은 앱 오류를 파일별로 줄인 뒤 CI에 건다.
- ☐ **React Hooks 검사** — 위 P1 항목처럼 `exhaustive-deps` 52건 중 실제 오래된 값 위험과
  React Compiler 오탐을 시험으로 가른다. 전부 오탐으로 보던 옛 결론은 폐기한다.
- ☐ **Firebase 에뮬레이터 + E2E** — Playwright 는 깔려만 있고 안 쓴다. 앱이 **운영 DB 에 바로 붙어서** 지금 돌리면 진짜 전표가 쌓인다. 에뮬레이터가 먼저다.

## 알고 두는 것 (고칠 게 아니다)

- **`stmt-hide-20260828`** — 분개가 안 만들어지는 전표 1건. 내가 만든 더미이고 회계에 안 잡힌다. 태백 496건 중 유일하게 걸리는 것이라, 앞으로 이 숫자가 2가 되면 진짜 문제가 생긴 것이다.
- **통장 잔고는 관리하지 않는다** — 103 보통예금이 음수여도 문제가 아니다.
- **원료수불부(`rawDocEntries`)와 원장(`rawMaterialLedger`)은 안 맞춘다** — 서류용과 실물용이라 뜻이 다르다.  · 2026-09-11 **사장님 결론: "들어온만큼 입고 잡히고 나간만큼 차감되면 된다."**
    근거 — 임가공 완제품 품목은 **자기 로트에 kg 을 지고 있다**:
    `lot-p-1785907900413-…` = `qtyIn 41박스 · kgIn 410kg · unitKg 10`.
    그 로트는 판매 때 `deductProductLotsForOrder` 가 박스 수로 깎고 kg 도 같이 준다.
    그러니 **원료 홀더(`raw-볶음참깨`) 로트를 또 깎으면 두 번 빼는 것**이고,
    원장에 안 적으면 나간 kg 이 수불부에서 사라진다. **로트는 완제품 쪽, 원장은 원료 쪽.**

- ☐ **사입·임가공 완제품의 음수 재고와 완제품 로트 갈림을 정리한다.**

  2026-09-14 17:17 운영 DB를 다시 읽은 현재 음수 완제품은 322개 중 **3개**다. 일반 자체생산
  완제품은 음수가 없고, 사입 1개와 임가공 2개뿐이다.

  · 고춧가루 1kg(`gck-1`) **−40개** — 9/2 진보반찬 출고 40개 뒤부터 남은 옛 음수다.
    품목 입고 기록은 0건이고, 옛 주문이라 상태변경 감사 이력도 없다.
  · 볶음참깨 낱개(`PLDhkjOgcPIhO1hhReHm`) **−7개** — 9/14 출고 감사 이력의
    태위형 1 + 태수정 1 + 훈장골 5 = **7개 차감과 정확히 같다.** 출고 전 숫자재고는 0이었다.
  · 볶음참깨 20개입(`p-1785907940315`) **−4박스** — 9/14 출고 감사 이력의
    논두렁 1 + 제일큰마트 2 + 수암식품 1 = **4박스 차감과 정확히 같다.** 출고 전 숫자재고는 0이었다.

  사입·임가공은 작업완료 때 생산하지 않고 출고 때 자기 재고를 그대로 뺀다. 현재 확인창도
  부족하면 음수가 된다고 경고한 뒤 진행을 허용하므로, 입고를 먼저 안 잡으면 음수는 다시 생긴다.
  완제품 로트는 낱개 −14 / 20개입 −17로 숫자재고보다 각각 7개·13박스 더 적다. 이 차이는
  오늘 출고가 아니라 로트를 시작하기 전의 옛 부족분과 9/6 stock만 옮긴 보정에서 남은 것이다.

  조사 도중 참고소(연한)는 −120개였다가 운영 DB에서 다시 0 이상으로 조정되어 음수 목록에서
  빠졌다. 처음 읽었을 때는 입고 기록 0건·9/14 양촌 120개 출고 상태였다. 누가 어떤 근거로
  조정했는지는 DB에 품목재고 수동조정 감사 이력이 없어 현재 기록만으로 확인할 수 없다.

- ☐ **재고 예약 도입 전에 작업완료된 주문의 미출고 재고를 한 번 이관한다.**

  성민식자재 `ORD-1788851357420`은 9/11 작업완료 때 시골향들깨가루(중간) 10개입을
  **6박스 생산(+6)**했고, 낱개 60개 자동생산과 박스·실링 차감도 스냅샷에 남았다. 하지만
  주문 재고 예약 기능은 9/13에 들어왔으므로 이 옛 작업완료 주문에는 `inventoryReservations`가 없다.
  그 결과 9/14 일성상회 3박스와 짠지네 2박스가 같은 재고를 출고했고 현재 stock은 1박스다.
  새 코드만으로 과거 `DISPATCHED` 주문을 소급 예약하지 않으므로, 미출고 주문 전체를 읽어 주문별
  출고량을 예약으로 이관하되 현재고보다 큰 부족분도 드러내는 1회성 스크립트가 필요하다.

- ☐ **통깨참기름 원장 정렬을 고친다.** 화면에는 52.659kg가 한 번 더 빠져 보인다. 새 원자 이력은
  `recordedAt`, 옛 이력은 `createdAt`인데 `sortLedger`가 `createdAt`만 정렬해 같은 날의 실사 앞뒤가 뒤집힌다.

- ☐ **임가공 옛 원장·재고 데이터는 운영값 확인 뒤 별도 정리한다.**

  새 주문 경로는 `ledger-consume` 명령으로 원료 로트 없이 사용 이력만 남기고, 생산 취소 때
  `reverse` 이력을 쌓도록 고쳤다. 아래는 새 코드로 소급 수정하지 않은 과거 자료다.

  · 원장 `8/11 입 1,425kg` · `8/28 입 712kg` 로 **OEM 가공입고가 전량(완포장 포함) 들어와 있는데
    그 날짜의 로트가 없다.** 원장과 로트가 그래서 계속 갈렸고 실사정정 로트가 셋이나 끼어 있다.
  · 임가공 판매가 남긴 `rm-auto-…` 줄이 열 배 적은 값으로 아직 있다(9/10 한울 2 · 9/11 무경 15 등).
    9/10 실사정정 앞의 것은 그 정정에 먹혔다. 조사 도구: `scripts/diag-oem-box-ledger.mts`.
  · 현재 숫자재고 음수와 출고 근거는 바로 위 항목에 최신 값으로 적었다.

  **사실 정리** — 임가공 품목은 3개뿐이고 전부 볶음참깨다
  (`PLDhkjOgcPIhO1hhReHm` 낱개 · `p-1785907900413` 10개입 · `p-1785907940315` 20개입).
  외주는 푸미푸드 한 곳, 발주 2건(7/24 참깨 1,500kg · 8/19 750kg).

## 회사별·메뉴별 보안 작업 — 현재 상태와 남은 순서 (2026-09-16)

사장님 확정 기준:

- **모든 업무자료를 회사별로 나눈다.** 공지·오피스톡·알림·설정·서류·HACCP·기준정보도 공용으로 두지 않는다.
- 직원과 관리자는 계정에 지정된 회사 **한 곳으로만** 로그인한다. 로그인할 때 회사를 고르거나 관리자 화면에서 회사를 전환하지 않는다.
- 화면에서 메뉴를 숨기는 데서 끝내지 않고 Firestore·Storage 규칙에서 회사와 메뉴 권한을 강제한다.
- 커밋·DB 적용·배포는 사장님 지시 전에는 하지 않는다.

### 완료된 것

- [x] 직원 평문 비밀번호를 scrypt 해시로 이관하고 Callable `employeeLogin`으로 로그인한다.
- [x] 로그인 claim은 `employeeId`, 직원 문서의 `companyId`, `isAdmin`을 사용한다. 관리자 양사 전환용 `allowedCompanies`는 제거하는 방향으로 코드가 수정됐다.
- [x] 로그인 화면 회사 선택과 관리자 회사 전환 UI를 제거했다.
- [x] 태백·풍회 관리자 계정을 회사별로 분리했다.
  - 태백: `taebaekfood`, `lek861215`
  - 풍회: `lek861215_p`, `pung`
- [x] 전용 로컬 정정 서비스 계정과 키를 저장소 밖에 만들었다. Firestore 데이터·Hosting·Rules 권한만 부여했다.
- [x] `useAppData(enabled, companyId)`와 `useAdminData(enabled, companyId)`가 업무 컬렉션 조회에 회사 조건을 붙이고, `settings/{companyId}`를 읽도록 배선된 상태다.
- [x] 회사값 누락 진단을 다시 실행했다. 대표 누락: `partner_item` 1,289, `productionRecords` 1,214, `item_bom` 1,014, `notifications` 780, `orders` 329, `partners` 297.
- [x] `scripts/fix-missing-company-ids.mts` 초안을 만들었다. 아직 **dry/apply 모두 실행하지 않았다.**

### 바로 다음 작업

1. [ ] **클로드가 진행 중인 변경을 먼저 받는다.** 같은 파일을 동시에 수정하지 말고, 끝나면 `git diff`로 회사 로그인·조회 배선을 검토한다.
2. [ ] **누락 회사 이관 스크립트를 보강한다.** 현재 초안은 모든 누락값을 태백으로 찍으므로 그대로 적용하면 안 된다.
   - 일반 옛 자료는 태백으로 지정한다.
   - `partner_item`은 `partnerId`·`itemId`, `item_bom`은 부모·자식 품목, 주문 감사·작업품목·반품은 `orderId`, 생산기록은 `itemId`, 전표 후속자료는 전표·거래처를 따라 회사를 판정한다.
   - 양쪽 원본 회사가 다르거나 원본이 없는 문서는 적용 대상에서 빼고 진단 목록으로 남긴다.
   - `usernames`, `authLoginAttempts`, 앱 유지보수 메타데이터는 업무자료 이관 대상에서 제외한다.
3. [ ] 보강 스크립트의 `--dry` 결과를 회사별·판정불가별로 확인한 뒤 사장님 승인 후 `--apply`한다. 로컬 백업, 중복 실행 방지, `--undo`, DB-CHANGELOG 기록을 유지한다.
4. [ ] `settings/company`를 `settings/taebaek`으로 옮기고 풍회 회사정보 `settings/punghoe`를 별도로 만든다. 태백 정보를 풍회에 그대로 복사하지 않는다.
5. [ ] 모든 새 문서 생성·수정 경로를 감사한다. 주문·전표·현금·재고뿐 아니라 공지·채팅·알림·서류·HACCP·기준정보에도 현재 로그인 회사를 명시한다.
6. [ ] 직접 Firestore 호출을 공통 서비스로 모으고, 부모 자료와 회사가 다른 연결문서 저장을 거부한다. 브라우저에서 다른 `companyId`를 보내도 규칙이 막아야 한다.

### 메뉴 권한 구현

- [ ] `Employee`에 배포 없이 바꿀 수 있는 메뉴 권한 필드를 둔다. 권한 키는 `ViewType` 문자열을 그대로 흩뿌리지 말고 한 목록에서 관리한다.
- [ ] 관리자는 자기 회사의 전체 관리자 메뉴를 사용할 수 있다. 직원은 허용된 현장 메뉴만 사용할 수 있다.
- [ ] 기본 직원 메뉴: 공지, 오피스톡, 주문·배송, 재고, 품목, 파렛트, 본인 연차, 확인사항, 위생점검, 현장 서류.
- [ ] 기본 관리자 메뉴: 전표·세금계산서·견적, 인사, 문서함, 현금출납·원장, 재무제표, 손익·비용·통계·생산실적, 거래처 승인.
- [ ] 로그인 claim에는 규칙에서 필요한 작은 역할값만 넣는다. 메뉴 배열 전체를 claim과 직원 문서에 중복 저장해 서로 어긋나게 만들지 않는다.
- [ ] UI 메뉴 표시, 라우팅 진입, Firestore 컬렉션 권한이 같은 권한표를 사용하게 한다.

### Firestore·Storage 규칙

- [ ] 읽기: 문서 `companyId == request.auth.token.companyId`만 허용한다. 목록 쿼리도 반드시 같은 회사 조건을 포함한다.
- [ ] 생성: `request.resource.data.companyId`가 로그인 회사와 같아야 한다.
- [ ] 수정·삭제: 기존 문서 회사와 새 문서 회사가 모두 로그인 회사여야 하며 회사 변경은 금지한다.
- [ ] 민감 컬렉션은 관리자 전용으로 시작한다: `cashEntries`, `cashAccounts`, `settlements`, `issuedStatements`, `pendingStatementEdits`, `payrolls`, `fixedCosts`, `fixedCostTemplates`, `fileCabinet*`, 고객 `users`.
- [ ] `employees`는 같은 회사 관리자만 관리한다. 본인은 FCM 기기 필드만 수정할 수 있다.
- [ ] `leaveRequests`는 본인 신청과 관리자 승인 필드를 나누고, 다른 회사 직원 자료는 차단한다.
- [ ] `adjustmentRequests`는 직원 생성과 관리자 처리 필드를 구분한다.
- [ ] 오피스톡 메시지는 방과 같은 회사여야 하고, 다른 회사 참여자를 넣지 못하게 한다.
- [ ] 알림은 원본 회사와 수신자 회사가 같아야 한다.
- [ ] Storage 파일은 회사 경로(`companies/{companyId}/...`)로 옮기고 토큰 회사와 경로가 같을 때만 접근하게 한다. 기존 파일은 목록·백업·이관 후 규칙을 잠근다.

### 시험·적용·배포 순서

1. [ ] Emulator 규칙 시험: 미로그인, 태백 직원, 풍회 직원, 태백 관리자, 풍회 관리자.
2. [ ] 다른 회사 읽기·목록·생성·수정·삭제 거부, 관리자 컬렉션 직원 거부, 본인 FCM·연차 허용을 시험한다.
3. [ ] 회사 조회가 붙지 않은 컬렉션 구독이 없는지 정적 시험을 둔다.
4. [ ] DB 이관 dry → 사장님 승인 → apply → 태백·풍회 실제 계정으로 읽기·쓰기 확인.
5. [ ] Functions 배포 후 앱 두 벌을 배포하고 마지막에 Firestore·Storage 규칙을 배포한다. 실패 시 직전 규칙과 DB 백업으로 복원한다.
6. [ ] 마지막에 `npx tsc --noEmit -p tsconfig.json`, `npx vitest run` 전체를 한 번 실행한다.
7. [ ] 위 경계가 안정된 뒤 App Check를 적용한다. 먼저 모니터링으로 정상 앱 트래픽을 확인하고 강제 모드로 전환한다.

### 주의

- 현재 작업 폴더에는 보안 변경과 주문·재고·전표 변경이 함께 있다. `git add -A`, 광범위한 되돌리기, 일괄 포맷을 하지 않는다.
- `scripts/fix-missing-company-ids.mts`는 관계 기반 판정이 들어가기 전에는 **절대 `--apply`하지 않는다.**
- 회사별 쿼리 코드는 DB 이관과 함께 배포해야 한다. 먼저 앱만 배포하면 누락 문서가 화면에서 사라지고, 먼저 규칙만 배포하면 기존 전체 조회가 권한 오류로 막힌다.

---

### 클로드 회신 (2026-09-16) — **위 목록 중 둘은 이미 끝났다. 다시 하지 마라.**

같은 일을 두 번 하지 않도록 먼저 적는다. 사장님 지시로 각 단계마다 승인을 받고 적용했다.

#### ① 「바로 다음 작업」 2·3번 — **끝났다. 누락 0건이다.**

`scripts/fix-missing-company-ids.mts` **초안은 쓰지 마라.** 관계 기반 판정을 넣은
`scripts/fix-company-backfill.mts` 로 이미 적용했다(dry → 사장님 승인 → apply → 다시 읽어 확인).

    23개 컬렉션 4,883건 — 원본 ID 로 판정 4,510 · 태백 기본값 373 · 충돌 0
    적용 뒤 확인: companyId 가 빈 문서 0건

그쪽이 적어 둔 판정 규칙 그대로 풀었다. 사장님이 정해 준 것만 덧붙인다:

- **직원** — 빈 8명 태백. 우용만 풍회(이미 찍혀 있었다).
- **거래처** — **풍회 전표·자금·주문에만 나오는 곳만 풍회**(1건). 양쪽 거래는 태백.
- **`partner_item`** — **품목 회사를 따른다.** 거래처와 같이 보면 3건 충돌(회사 간 거래).
- **`chatMessages`** — **방 회사를 따른다.** 보낸 사람도 보면 **71건 충돌** — 두 회사에 계정이
  있는 사람(이은경)이 섞여 대화한 자리다. 메시지는 방에 속하므로, 보낸 사람으로 가르면
  한 방 안에서 대화가 반쪽만 보인다.
- **`notifications` 780건은 지웠다**(사장님 지시). 원본 id 도 수신자 id 도 없어 판정 불가였다.
  **새로 만드는 알림에 회사를 붙이는 일은 남아 있다** — 「바로 다음 작업」 5번에 해당한다.

`usernames`·`authLoginAttempts` 는 대상에서 뺐다. DB-CHANGELOG 에 기록했다.

> 그쪽 `useAppData` 머리말의 **"이관 스크립트를 돌리기 전까지 그 문서들은 어느 화면에도
> 뜨지 않는다 — 이관 전까지의 의존성이다"** 는 **해소됐다.** 그 주석을 지워도 된다.

#### ② 「Firestore·Storage 규칙」과 「시험 1·2」 — **초안이 있다. 검토해 달라.**

- `firestore.rules.next` — 회사별·메뉴별 권한판. **배포 안 했다.** 살아 있는 `firestore.rules`
  는 안 건드렸다(그쪽이 배포할 수 있어서 새 파일로 뒀다).
- `src/rules/firestore.rules.test.ts` — 에뮬레이터 시험 **40개 통과.** 6주체:
  미로그인 · 익명 · 태백직원 · 풍회직원 · 태백관리자 · 풍회관리자.

그쪽 규칙 체크리스트를 그대로 담았다(읽기·생성·수정·삭제 회사 검사, 민감 컬렉션 관리자 전용,
`employees` 본인 FCM 만, `leaveRequests` 본인 신청·관리자 승인, `adjustmentRequests` 구분).
**Storage 는 안 했다** — 그쪽 lane 으로 둔다.

시험이 규칙 구멍 둘을 실제로 잡았다(눈으로만 봤으면 놓쳤을 것들):

- `isAdmin()` 만 보면 **회사값이 없는 문서까지 관리자에게 열린다.**
- `delete` 에는 `request.resource` 가 없어, 생성·수정과 같은 함수를 쓰면 **지우기가 통째로 막힌다.**

**회사 경계는 관리자도 못 넘게 짰다** — 사장님 확인: "관리자가 회사를 골라보는 구조가
아니잖아 지금 관리자 계정은 그냥 회사 내의 관리자 아니야?". `isAdmin` 은 **메뉴만** 가른다.
그쪽이 `allowedCompanies` 를 제거하는 방향으로 고쳤다는 것과 같은 결론이다.

#### ③ 규칙을 배포하려면 그쪽 것이 먼저 있어야 한다

`보안-개편-계획.md` 4단계 **"직원 앱에서 전표·자금·인사 모듈을 번들 및 구독에서 제거"**.
지금 `useAppData` 가 일반 직원에게도 아래를 구독한다. 규칙을 잠그면 **그 순간
`permission-denied`** 가 난다 — 직원 로그인이면 구독을 아예 걸지 않아야 한다:

    issuedStatements · pendingStatementEdits · settlements · quotations
    cashEntries · cashAccounts · cashFlowManual
    payrolls · fixedCosts · fixedCostTemplates · expensePresets
    accountCodes · accountGroups · inventorySnapshots · productionSalesLogs
    fileCabinet · files · users · partnerSignups

**이 목록은 사장님이 정한 관리자 전용 메뉴에서 뽑은 것이라 현장 기능과 어긋날 수 있다.**
`inventorySnapshots`(재고 실사)·`accountCodes` 가 직원 화면에서 실제로 쓰이는지는 그쪽이
더 잘 안다. 쓰이면 알려 달라 — `adminOnlyCollection()` 에서 빼겠다.

#### ④ 안 건드린 파일 — 그쪽 작업 중으로 보여 남겨 뒀다

`AdminApp.tsx` · `TradeStatement.tsx` · `statementHistoryRowView.ts` · `StatementPaymentRow.tsx`
· `useAppData.ts` · `firestore.rules` · `apps/*/main.tsx` · `AuthPage.tsx` · `CLAUDE.md`

앞의 넷에는 **내 변경이 섞여 있다**(배송지 표시 · `holdsUnitStock`). `AdminApp.tsx` 는
`src/shared/itemCompany.ts` 가 아직 untracked 라, 담으면 main 이 반쪽 나서 뺐다.
그쪽이 커밋할 때 같이 들어간다.

#### ⑤ 내가 커밋한 것

`bf08c27` — 배송지(거래처 안의 납품처) · 캔 반제품 이관(`holdsUnitStock`) · 회사값 이관
스크립트 · 서비스 계정 접속(`scripts/_admin.mts`).

**스크립트는 이제 서비스 계정으로 붙는다** — 그쪽이 규칙에서 익명을 막은 뒤로 옛 스크립트가
전부 막혔다. 열쇠는 저장소 밖, `GOOGLE_APPLICATION_CREDENTIALS` 로만 읽고 경로도 내용도
코드·로그·백업에 안 적는다. Admin SDK 는 규칙을 우회하므로 스크립트가 스스로 지킨다:
dry 기본 · `--apply` 필수 · 바꾸는 것 열거 · 백업 · `--undo` · 쓴 뒤 다시 읽어 확인 ·
**프로젝트 id 못 박기**(다른 프로젝트에 쓰는 것이 제일 위험하다).
