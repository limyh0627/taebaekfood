# 할 일

여기에는 **아직 끝나지 않은 일만** 둔다. 완료 이력은 `DB-CHANGELOG.md`, 규칙은
`인수인계.md`, 상세 설계는 `로컬전용/docs/`에서 본다.

## 작업 원칙

- Codex는 설계·검증·`할일.md` 정리를 맡고, Claude Code에는 겹치지 않는 구현 작업만 맡긴다.
- 같은 작업폴더를 동시에 수정하지 않는다.
- 커밋·푸시·배포·DB 적용은 사장님 지시가 있을 때만 한다.
- 로컬 동작 확인은 운영 DB가 아니라 Firebase Emulator를 사용한다.

## 다음 진행

- **캔 개봉을 원료 원자 명령과 한 트랜잭션으로 통합한다.**
  - 2026-09-19 수입산들기름 캔 40개가 옛 `wip-들기름` BOM으로 들어간 사고는 복구했다.
  - BOM은 `raw-수입들기름`으로 고쳤지만 현재 `unpackService`는 캔 품목과 `items` 벌크만
    직접 갱신한다. 다음 개봉 전에 `rawInventories`·원장·캔 로트를 한 트랜잭션으로 묶고,
    개봉 작업번호를 저장해 재시도도 한 번만 먹게 한 뒤 배포해야 한다.

- **2026-09-18 일일점검의 남은 음수 재고 1건을 실제 수량과 대조한다.**
  - 수입들기름 `-61.174kg`은 9/19 캔 40개 개봉 오연결을 복구해 `598.826kg`으로 해소됐다.
  - `PET-H-1800-GD`(1800ML 페트병 골드) 현재고 `-46개`는 실제 입고 누락인지
    생산·출고 선반영인지 확인하기 전에는 수정하지 않는다.

## 바로 할 것

- [ ] **로트 목록·상세·다중 혼합 사용을 에뮬레이터에서 검수한 뒤 배포한다.**
  - 리스트 열 순서, 보드 카드, 로트별 타임라인, FIFO 순서 변경을 함께 확인한다.
  - 가상 참깨 원료에 활성 로트 `LOCAL-001~003`을 넣어 두었다.
  - 여러 로트를 선택하고 비율 합계 100%를 저장한 뒤 실제 차감 분배를 확인한다.

- [ ] **2026-09-18 일일점검의 남은 PET 음수 재고를 실제 자료와 대조한다.**
  - 보고서: `로컬전용/일일점검/2026-09-18.md`
  - 확인 필요 7건은 생산 시점 부족 이월이며, 현재 재고가 0 이상인 건은 후속 입고로 해소됐는지 본다.

- [ ] **App Check를 모니터링한 뒤 강제 모드로 전환한다.**
  - 회사값 정적 감사, Firestore Emulator 회사 경계 시험, 규칙 배포는 2026-09-18 완료했다.

## 실제 데이터 확인

- [ ] **제품별 원장의 독립 검증 기준을 전 품목에 세운다.**
  - 현재 521개 대상 중 실사 앵커가 있는 품목은 1개뿐이고, 나머지 520개는 현재 재고에서 기초를 역산하므로 과거 누락을 독립적으로 판별할 수 없다.
  - 화면에는 실사 기준 유무를 구분해 표시했다. 다음 전수 실사 때 품목별 앵커를 원자 명령으로 남긴 뒤, 이후 생산·출고·입고·실사 기록과 현재 재고를 매일 대조한다.
  - 원료 네 축(`items.stock`·로트 합계·원료 실제원장·원자 상태)은 현재 불일치 0건이다.

- [ ] **임가공 옛 원료수불부 153kg를 판단한다.**
  - 한울유통 18kg, 무경유통 135kg가 9/10 실사 이후 원료수불부에서 빠진 후보다.
  - 현재 재고 네 축은 일치하므로 재고를 차감하지 않고 과거 표시용 기록만 보충할지 판단한다.
  - 근거: `로컬전용/일일점검/임가공-옛기록-판단표.md`.

- [ ] **전표와 주문 품목이 다른 5줄을 판단한다.**
  - 해피유통 300↔350ml, 초당노인복지기관 선물세트 2↔3호,
    모란식품 특↔골드A/A, 알이네식품 삭제 품목 `p-150`.
  - 근거: `로컬전용/일일점검/전표-주문-품목불일치-판단표.md`.

- [ ] 품목이 삭제됐는데 남은 `partner_item` 16건을 원래 품목 확인 후 정리한다.

## 기능·구조 개선

- [ ] **복사주문 판독 규칙이 사용자 확정 결과를 바탕으로 개선되게 만든다.**
  - 주문 원문과 사람이 최종 확정한 거래처·품목·수량·단위·배송일·메모를 함께 기록한다.
  - `미리→ml`, 붙여 쓴 `참기름10박스`, `스티커X`, 요일·방문수령 같은 표현을 정규화한다.
  - 같은 수정이 반복되면 거래처별 품목 별칭 또는 공통 표현 규칙으로 제안한다.
  - 잘못된 자동 연결이 누적되지 않도록 규칙은 바로 학습시키지 않고 관리자 승인 후 적용한다.
  - 판독 순서는 승인된 규칙·거래처별 별칭을 먼저 쓰고, 애매한 문장만 Gemini로 보낸다.
- [ ] 직원 주문·발주·반품·입고를 한 정규화 명령으로 통과시키고 관리자 구독·번들을 분리한다.
- [ ] 비원료 입고도 작업번호, before/after 재고, 입고 기록을 한 transaction에 저장한다.
- [ ] 전표번호를 회사·날짜별 서버 transaction으로 발급하고 앱과 Functions가 같은 카운터를 쓴다.
- [ ] 날짜·전표번호·재고평가·BOM 원가·자동전표 계산을 앱과 Functions의 공용 순수 함수로 합친다.
- [ ] 회계·전표 모듈 분리를 `로컬전용/docs/전표-원가-원장-재무-통합설계.md` 9~12장대로 계속한다.
- [ ] 스마트스토어 변경 주문 조회 → 거래처·품목 ID 확인 → 사람 확인 → 저장 연동을 구현한다.
- [ ] 안드로이드 Web Share Target과 오피스톡 첨부 업로드를 통합한다.

## 원가·재고 장기 작업

- [ ] 매입단가에 단위와 성격(원물·임가공)을 추가하고 `purchase` 원가 산식을 확정한다.
- [ ] 박스 개입수·규격 파생값과 옛 호환 필드를 단계적으로 제거한다.
- [ ] `Product.price`를 `cost`로 통합한다.
- [ ] 완사입·임가공을 외주창고·Backflushing·가공비 계정까지 포함한 한 모델로 통합한다.
- [ ] OEM 외주발주를 재개 가능한 job으로 만들고 결정적 operation ID와 원자 로트번호를 사용한다.
- [ ] 배송·선물세트 kit 차감을 실제 BOM 구조에 맞게 통합한다.

## 품질·정리

- [ ] React hook 의존성 경고를 실제 오류와 오탐으로 나눠 고친다.
- [ ] 죽은 파일·백업 사본·중복 `ViewType`을 import graph로 확인한 뒤 삭제한다.
- [ ] `functions/lib` 생성물을 Git에서 빼고 미사용 패키지·AI 청크를 정리한다.
- [ ] deprecated 필드의 쓰기·읽기·DB 잔존량·대체 필드·삭제 버전을 표로 만들고 제거한다.
- [ ] 직접 Firestore 호출과 정정 스크립트를 공용 명령·서비스로 옮긴다.
- [ ] Firebase Emulator 기반 E2E를 붙인다.

## 날짜가 정해진 것

- [ ] **2026-10-30 전:** Cloud Functions Node.js 20 → 22. 함수 6개 확인 후
  `firebase-functions` 6.x 업그레이드는 별도로 한다.
- [ ] **2026-12-31 결산:** 거래처 원장 앵커 생성 스크립트를 dry-run 후 적용한다.

## 사장님 판단이 필요한 것

1. **옛 Storage 원본 73개:** 회사 경로 복사·MD5·참조 전환 완료. 최종 대조 후 삭제할지 결정.
2. **직원별 메뉴 권한:** 회사 내부에서 직원마다 메뉴를 제한할지 결정.
3. **표준 계정과목:** 원재료비·부재료비 500/505를 어느 표준 계정으로 옮길지 결정.
4. **박스 재고:** 제조한 박스를 그대로 출고하는지, 출고 때 낱개로 풀어 다시 포장하는지 결정.
5. **낱개 판매단가 7건:** 박스 단가의 1/입수로 들어간 낮은 값을 바로잡을지 결정.
6. **과세·면세 11건:** 같은 거래처·물건의 박스와 낱개 중 어느 과세구분이 맞는지 결정.
7. **임가공 수불부 153kg:** 과거 표시용 사용기록을 보충할지 결정.
8. **전표·주문 불일치 5건:** 실제 납품 품목을 확인해 연결을 결정.
9. **반품 음수 전표:** 다음 매입 잔액과 FIFO 자동 상계할지 결정.
10. **직원 업무 범위:** 주문·발주·반품·입고·재고실사 중 직원이 직접 처리할 범위 결정.
11. **정정 스크립트 원칙:** 모든 재고 정정에 공용 원자 명령을 의무화하고 구조 이관만 예외로 둘지 결정.

## 운영 기준

- 판매일지·서류수불부는 실제 BOM과 다른 **서류용 환산 비율**로 검증한다.
- 통장 잔고는 앱에서 관리하지 않는다. 103 보통예금 음수만으로 오류 판정하지 않는다.
- 임가공 완제품 로트는 완제품에서 차감하고 원료수불부에는 원료 사용 이력을 남긴다.
- 같은 이름이어도 라벨·캡·BOM이 다른 품목은 합치지 않는다.
- `stmt-hide-20260828`은 회계에 잡히지 않는 더미 전표 1건이며 늘어나면 오류다.
