# 원료 로트 작업 — 변경 이력 & 복구 가이드

> 2026-06 원료 로트(lot) 시스템 도입 전후로 바꾼 코드를 "무엇을·어디서·어느 커밋에서" 정리.
> **나중에 되돌리기 쉽게** 하는 게 목적. (작업 현황은 `원료로트-작업현황.md`, 운영 DB 변경은 `DB-CHANGELOG.md` 참조)
> 시작 기준 커밋: **1398ae6** (로트 작업 직전 상태).

---

## 한눈 요약 — 빠른 복구
- **특정 변경만 되돌리기**: `git revert <커밋해시>` (아래 표의 해시).
- **로트 도입 전 코드로 통째 복귀**: `1398ae6`이 직전 상태. 단 ⚠️ 그 이후 커밋엔 **로트 외 버그픽스(향미유 이중차감, 생산판매기록부 엑셀, 수불부 전재고 등)도 섞여** 있어 통째로 되돌리면 그것들도 사라짐 → 가능하면 **필요한 커밋만 revert** 권장.
- **데이터(운영 Firestore) 되돌리기**: `npx tsx scripts/lot-migration.mts --undo`, `npx tsx scripts/backfill-cheongjeong-260617.mts --undo`.

---

## 커밋별 변경 내역 (내 작업 = limyh0627 에이전트 커밋)

### 484d65b — 원료 로트 시스템 (1·2단계: 입고→로트, 납품→선입선출 차감)
- `src/shared/types.ts`: `RawMaterialLot` 타입 신설, `Item`에 `lots/packageType/packageKg`, `Order`에 `rawLotsDeducted`.
- `src/constants/formula.ts`: `baseRawName`, `parsePackageKg`, `kgToUnit`/`unitToKg`, `lotKgRemaining`, `lotStockInUnit` 추가.
- `src/shared/lotUtils.ts` **(신규)**: `withCarryOverLot`, `buildReceiveLot`, `nextLotNo`, `deductFromLots`, `receiptToKg`.
- `src/shared/services/firebaseService.ts`: `mutateRawMaterialLots`(로트 트랜잭션 갱신).
- `components/ReceivingReturnsManager.tsx`: 입고 2경로(스캔/선입고)에서 로트 생성+수불부, 매입 SKU 재고 누적 중단(`rawLotTarget`).
- `components/RawMaterialLotPanel.tsx` **(신규)**: 로트 목록 UI(▲▼ 순서/삭제/로트번호/L·kg/소진 숨김).
- `components/ItemList.tsx`: 원료·연결 반제품 클릭 시 로트 패널, 수동 입고 로트 생성형.
- `src/features/admin/AdminApp.tsx`: `deductSubmaterialsForOrder`에 납품완료 시 원료별 합산→선입선출 차감 + 중복방지(`rawLotsDeducted`).
- `scripts/lot-migration.mts`: 이월 로트 시드 + 캔 SKU(p-1779251603644) 재고 정리. (DB 변경)

### 8a96acd — 문서 (작업현황 배포반영). 코드 영향 없음.

### 69844fd — 기름 혼합 + 벤조피렌 + 반제품 탭 + 읽기 최적화
- 기름 혼합(3단계): `Item.mixEnabled/mixTopPercent`, `deductFromLots(…, mix)`, `RawMaterialLotPanel` 혼합 토글(상위2로트 비율, 기본 50:50).
- 벤조피렌: `components/BenzopyreneLog.tsx` **(신규)** + `AdminApp` 서류관리 "벤조피렌 검사성적서" 탭. 컬렉션 `benzopyreneTests`.
- 반제품(wip) 탭 버그수정: `ItemList` 테이블 렌더 조건에 `topTab === 'wip'` 누락 보완.
- 읽기 최적화(C): `AdminApp` 원료수불부 전체이력 세션 1회 캐시, `PalletManager` 24개월 5분 모듈캐시.

### 8f7727a — 파레트 개편
- `components/PalletManager.tsx`: 미회수/지급/교체중 요약, 전역 회수/지급/교체 버튼, 교체중→교체완료 흐름, 이력 탭. `types.ts` `PalletTransaction.status/exchangeReturnQty`.

### 331df87 — 모든 입고 경로 로트 통일 + 수동사용/수율/실사조정
- `src/shared/rawReceipt.ts` **(신규)**: `rawLotTarget` + `recordRawMaterialReceipt`를 ReceivingReturnsManager에서 추출(공용화).
- `AdminApp`: `handleFinishConfirmedOrder`(발주 입고확인)·`handleRequestPoEdit`(입고대기 수정)에도 로트 기록 연결, 수율 자동입고가 파생 원료 로트 생성.
- `ItemList`: 수동 "사용" → 로트 FIFO/혼합 차감, 재고 직접수정 → '실사조정'(로트를 실사값에 맞춤+수불부).
- `ReceivingReturnsManager`: 입고 시 로트/수불부 기록 실패하면 경고창.
- `scripts/backfill-cheongjeong-260617.mts` + `DB-CHANGELOG.md`(06-17 누락분 백필).

### (이번 커밋) 읽기 한도(429) 폴백 — 입고/기록이 읽기 막혀도 되게
- `src/shared/services/firebaseService.ts`: `mutateRawMaterialLots`에 `fallback` 파라미터 추가 — 트랜잭션이 `resource-exhausted`로 실패하면 **메모리 로트 기준 순수 쓰기**로 폴백(기록 보존). ⚠️ 같은 원료를 동시 다기기 수정 시 lost update 가능(단일 작업 안전).
- 폴백 전달: `rawReceipt.recordRawMaterialReceipt`(입고), `ItemList`(수동 사용·실사조정), `AdminApp`(수율 자동입고).
- **납품 출고 차감(`deductSubmaterialsForOrder`)엔 폴백 미적용** — 일괄처리라 메모리 폴백 시 유실 위험. (읽기 한도 풀려야 작동)

---

## 다른 기기/사람 작업 (내가 한 게 아님 — 되돌릴 때 주의)
- `cf51c14` — 원료수불부 전재고 누락 수정(탭 진입 시 전체 이력 온디맨드 로드).
- `ac274f1` — 생산판매기록부 엑셀 저장 시 주문 이력 이동 보장.
- `b33c5c1` — 완제품 카테고리 통일(`'완제품'`→`'product'`) + 향미유 이중차감 수정 + 파레트 추가 개편(월별보기/삭제/신규입고). **(내 파레트 작업 위에 병합됨)**

---

## 신규 파일
- `src/shared/lotUtils.ts`, `src/shared/rawReceipt.ts`, `components/RawMaterialLotPanel.tsx`, `components/BenzopyreneLog.tsx`
- `scripts/lot-migration.mts`, `scripts/backfill-cheongjeong-260617.mts` (+ 진단용 `scripts/diagnose-*.mjs`, `check-*.mjs`, `verify-lots.mjs` — 읽기 전용)
- 문서: `원료로트-작업현황.md`, `DB-CHANGELOG.md`, 이 파일

## 2026-06-19 추가 작업
- **출고(납품) 차감도 읽기한도 폴백 적용** — 단일 납품은 메모리 로트, 일괄(생산판매기록부 저장)은 `lotCache`로 메모리 체이닝(같은 원료 연속 차감 유실 방지). 한도 막혀도 출고 차감 보존.
- **배합식 출처 통일(해결)** — 실제 차감도 화면 표시와 동일하게 **DB BOM(item_formula) 우선, 없으면 PRODUCT_FORMULA**. 이제 앱에서 배합비 수정하면 실제 차감도 그대로 따라감.

## 🧹 Blaze 전환 후 삭제할 코드 (읽기한도 폴백 — `[QUOTA-FALLBACK]`)
Blaze로 읽기 한도가 사라지면 아래 "폴백"은 불필요(트랜잭션만으로 안전+항상 작동). **`[QUOTA-FALLBACK]` 로 grep**하면 다 나옴. 지울 것:
1. `firebaseService.ts` `mutateRawMaterialLots`: `fallback` 파라미터 + `catch` 블록의 폴백 분기(트랜잭션 `try`만 남기면 됨). 반환타입/return은 그대로 둬도 무방.
2. 호출부 4번째 인자 `{ lots, stock }` 제거 — `rawReceipt.ts`(입고), `ItemList.tsx`(수동 입고·사용·실사조정), `AdminApp.tsx`(수율 자동입고, 출고 차감).
3. `AdminApp.tsx` `deductSubmaterialsForOrder`의 `lotCache` 파라미터 + 일괄 루프의 `batchLotCache` + `lotCache?.set/get` (출고 차감은 트랜잭션이 신선하게 읽으니 체이닝 불필요).
→ 삭제 후 모든 로트 쓰기는 순수 트랜잭션(동시성 안전). 기능 동일.
- ※ **입고 실패 경고창**(ReceivingReturnsManager·AdminApp)은 폴백과 무관하게 유용하니 **남겨도 됨**.
- ※ 읽기 절약 캐시(수불부 세션캐시·파레트 5분캐시)는 성능에도 도움 → 남겨도 됨(선택).

## 알려진 미해결 / 주의
- **동시 다기기 같은 원료 동시 수정** 시, 읽기 한도 폴백 경로에서는 마지막 쓰기가 이겨 한쪽 유실 가능(평소엔 트랜잭션이라 안전). Blaze 전환 시 이 위험도 사라짐.
- 원료 차감은 완제품이 `category === 'product'`여야 작동 — 카테고리 통일이 그렇게 유지(quota 여유 시 1회 점검 권장).
- 근본 해결(읽기 한도): **Blaze 전환**(예정).
