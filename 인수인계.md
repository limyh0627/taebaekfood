# 인수인계

이 프로젝트에서 **지켜야 할 규칙**을 한자리에 모은다. 사장님이 정한 것과, 사고가 나서 정해진 것.
새로 붙는 사람은 코드를 읽기 전에 이걸 먼저 읽는다.

- 기술 스택·배포 명령: [CLAUDE.md](CLAUDE.md)
- DB를 건드린 이력과 되돌리는 법: [DB-CHANGELOG.md](DB-CHANGELOG.md)
- 설계 문서: [docs/](docs/)

---

## 0. 배포와 커밋 — 사장님이 말할 때까지 안 한다
**고쳐 두기만 하고 기다린다.** 사장님이 "배포"라고 할 때 **커밋과 배포를 같이** 한다.
말없이 커밋해 두면 무엇이 나갔고 무엇이 안 나갔는지 사장님이 못 읽는다. (2026-09-02 확정)

- 그때까지는 tsc · 테스트 · 빌드까지만 돌려 두고, 무엇을 고쳤는지 말로 보고한다.
- 데이터를 고치는 스크립트도 마찬가지다 — `--dry` 로 보여 드리고 허락받고 `--apply`.

## 컬렉션 이름은 collections.ts 에 적는다

컬렉션 이름을 글자로 쓰면 **오타가 조용히 지나간다.** `'issuedStatments'` 라고 쓰면
없는 컬렉션이 새로 생기고, 쓴 쪽은 성공했다고 여기고 읽는 쪽은 영영 빈 목록을 본다.
**화면에 아무 표시도 안 난다.**

54개를 [collections.ts](src/shared/collections.ts) 에 모았다(2026-09-05).
새 컬렉션을 만들면 여기 적는다.

[collections.test.ts](src/shared/collections.test.ts) 가 지킨다 — 코드에 쓰인 이름이
목록에 다 있는지, 목록에 적어 두고 안 쓰는 게 없는지 양쪽으로 본다.

## 부가세율을 손으로 곱하지 않는다

`VAT_RATE` 를 [lineAmount](src/shared/lineAmount.ts) 에 두고도 `Math.round(supply * 0.1)`
이 네 곳, `* 1.1` 이 세 곳에 손으로 적혀 있었다(2026-09-05).
**세율이 한 곳에 있어도 쓰는 자리가 손으로 곱하면 소용이 없다.**

  `vatOn(공급가액, 면세)`   공급가액에 붙는 세액
  `VAT_UP`                세금 얹는 곱수(면세 원료가 과세품 원가에 얹힐 때도 이것)
  `lineAmount(...)`        **세포함** 값에서 공급가액·세액을 역산

[vatRateWiring.test.ts](src/shared/vatRateWiring.test.ts) 가 지킨다 —
세금 얘기를 하는 줄에서만 본다(0.1 은 투명도에도 쓰인다).

## 계정코드는 autoJournal 한 곳에서 온다

`'108'` 처럼 번호를 그냥 박아 두면 **표준계정과목으로 옮길 때 한쪽만 고쳐진다**
(그 계획이 [docs/표준계정과목-이전계획.md](docs/표준계정과목-이전계획.md) 에 있고 아직 안 했다).

2026-09-05 에 훑어보니 — cashLedger·interCompany 는 값을 **손으로 옮겨 적었고**,
ProfitAnalysis·financials·cashTemplates 는 숫자를 그대로 견주고 있었다(20곳).

`AR`·`AP`·`OTHER_PAYABLE`·`VAT_PAYABLE`·`VAT_RECEIVABLE`·`BANK`·`INVENTORY` 를
[autoJournal](src/shared/autoJournal.ts) 에서 가져다 쓴다.
계정표에서 읽고 받침으로 두는 건 괜찮다 — `accountCodes.find(...)?.code ?? '255'`.

[accountCodeWiring.test.ts](src/shared/accountCodeWiring.test.ts) 가 지킨다.

## 같은 것은 같은 이름으로 부른다

**거래처는 `partner` 다.** `client` 는 옛 이름이고, 바꾸다 만 자국이 431곳 남아 있다.
새로 쓸 때 쓰지 마라 — 찾을 때 한쪽만 걸리고, 읽는 사람은 다른 것인 줄 안다.

특히 **상태와 그 setter 는 이름이 같아야 한다.** `[partnerSearch, setClientSearch]`
처럼 어긋난 자리가 열 곳 있었다(2026-09-05 사장님 지적).

[naming.test.ts](src/shared/naming.test.ts) 가 지킨다 — setter 이름이 어긋나면 잡고,
`client` 로 부르는 자리가 431보다 **늘면** 잡는다(줄이는 건 언제나 좋다. 줄었으면 그 숫자를 낮춰 적어라).

## 꺼둔 화면 코드를 남기지 않는다

`{false && (...)}` 로 한 덩어리를 꺼두고 지우지 않으면 **살아 있는 줄 알고 고치게 된다.**

실제로 그랬다 — 세금계산서 발행이 [TaxStatement](components/TaxStatement.tsx) 로 옮겨진
뒤에도 [TradeStatement](components/TradeStatement.tsx) 에 **364줄이 꺼진 채** 남아 있었고,
2026-09-05 에 그걸 살아 있는 코드로 보고 손을 댔다(공용 함수까지 연결했다).

**지워라. 되살릴 일이 있으면 git 에 있다.**
[deadCode.test.ts](src/shared/deadCode.test.ts) 가 지킨다.

## 칸 고정 표는 폰에서 잘리면 안 된다

`grid-cols-[1fr_100px_100px_120px]` 처럼 **고정 칸이 여럿이면 폰에서 `1fr` 이 0으로 눌린다.**
머리글이 `품/목` 으로 접히고 뒷칸이 화면 밖으로 나간다(2026-09-05 사장님이 견적서에서
발견 — 훑어보니 네 곳이 더 있었다).

**고정 폭 합이 260px 을 넘으면 둘 다 한다** —
  ① 늘어나는 칸에 최소 폭: `minmax(140px,1fr)`
  ② 표에 `min-w-[...]`, 담은 칸에 `overflow-x-auto`

어차피 옆으로 미는 표다. **미는 김에 안 잘리는 게 맞다.**

[gridOverflow.test.ts](src/shared/gridOverflow.test.ts) 가 지킨다 — 어기면 파일·줄·고정폭을
짚어 준다. 날짜 칸(`input[type=date]`)은 [index.css](src/index.css) 에서 한 번에 잡았다.


## 1. 화면

### 검색할 때 창 크기가 바뀌면 안 된다
목록을 걸러도 팝업·패널 높이는 그대로여야 한다. 검색어를 칠 때마다 창이 커졌다 작아지면
누르려던 자리가 도망간다. 컨테이너에 **고정 높이**를 주고 안쪽만 스크롤시킨다
(`max-h-…` + `flex-1` 조합은 내용에 따라 흔들린다 — `h-[70vh]`처럼 못 박을 것).

### 빈 행을 딸려 보내지 않는다
'빠른 비용'·'품목 선택'처럼 줄을 추가하는 기능은 **요청한 줄만** 넣는다. 편의로 빈 줄을 하나
더 붙이면 누를 때마다 하나씩 쌓여 지우는 일이 된다. 빈 줄이 필요하면 '행 추가'가 따로 있다.

### 검색은 어디서나 초성으로 된다
`shared/hangul`의 `matchesSearch` 하나만 쓴다. 화면마다 제 초성 함수를 복사해 두면
어떤 창은 되고 어떤 창은 안 된다(실제로 세 벌이 돌아다녔다).

**겹자음을 펴야 한다** — 자판이 자음을 잇달아 치면 붙여 버린다(ㄱ·ㅅ → `ㄳ`). 초성은 19자뿐이라
겹자음은 초성으로 절대 안 나오고, 안 펴면 `ㄳ`으로 '고소한'을, `ㅄ`으로 '박스'를 못 찾는다.

### 같은 일을 두 화면에 두지 않는다
단가를 고치는 자리가 '단가관리' 패널과 '품목 선택'에 따로 있으면 단가 하나 바꾸려고 창을
두 번 연다. **고르는 화면에서 바로 고친다.** (2026-08-24에 단가관리 패널을 품목 선택으로 합쳤다.)

### 분류(타입·서브타입·카테고리)는 코드에 박지 않는다
탭 이름·순서·숨김, 목록의 선택지, 정렬 순서는 전부 **분류 관리(`itemTaxonomy`)**에서 딴다.
아이콘·색만 코드가 쥔다(분류표에 담을 값이 아니다).

- 비교는 **키**로 한다(`p.type === 'product'`). 라벨(`'완제품'`)로 견주면 이름을 바꾸는 순간 깨진다.
- 새 분류를 만들면(비닐·케이스) 코드를 안 고쳐도 화면에 떠야 한다.

> **분류표를 고쳐도 이미 붙은 값은 안 따라온다.** 분류 이름을 바꾸면 품목의 값도 같이 옮겨야 한다
> (배송→박스로 바꿨을 때 품목 130건이 옛 값을 들고 있어 새 이름으로 거르면 0건이었다).

### 이름을 근거로 판정하지 않는다
"박스 묶음인가"를 `subtype === '배송'`으로 가려내던 자리가 네 곳 있었다. 이름은 분류 관리에서
언제든 바뀐다. 판정은 **구성(BOM)**으로 한다 — 완제품 구성품 딱 하나 × 수량 2 이상.

---

## 2. 품목과 구성(BOM)

### 구성은 `item_bom`이 유일 원천이다
품목은 제 구성을 안 들고 다닌다. 읽기는 `bomOf(item.id)` ([src/shared/bomIndex.ts](src/shared/bomIndex.ts)),
저장은 편집 폼이 `bomDraft`로 따로 넘긴다. 품목 문서에 구성을 복사해 두면 같은 사실이 두 곳에 생겨
어느 쪽이 참인지 늘 물어야 한다.

### 중복 판정은 이름 + BOM
박스와 낱개는 이름이 같아도 **다른 품목**이다. 구성이 다르면 안 묶인다.
(이름만 보던 시절 111그룹이 떴고 그중 110개가 박스↔낱개 오탐이었다.)

### 부자재의 `category`는 '무슨 물건이냐'다
`모란 검정참깨`는 라벨이지 검정참깨가 아니고, `테이프-검정`의 검정은 색이다.
품목 계열을 옮길 때 부자재는 건드리지 않는다.

### 3단 분류
| 단 | 값 | 누가 정하나 |
|---|---|---|
| `type` | product · goods · wip · raw · submaterial | 고정(엔진 분기 키) |
| `subtype` | 낱개 · 박스 · 선물세트 · 벌크 · 무라벨 | 분류 관리 |
| `category` | 참기름 · 들기름 · 참깨 · 들깨 · 용기 · 마개 · 라벨 … | 분류 관리 |

- **선물세트·배송은 타입이 아니라 완제품의 subtype이다.** 타입에도 두면 같은 것이 두 자리에 존재한다.
- **`벌크`는 `isBulkItem`이 보는 값이다.** 재고차감·원료홀더 탐색 30여 곳이 여기 얹혀 있다.
  분류 관리 목록에 없으면 새 품목에 달 방법이 없어 조용히 개수로 취급된다.

---

## 3. 재고

### 로트와 재고는 다른 숫자일 수 있다
`lotsAreTotal`을 켠 원료(볶음참깨처럼 낱개·박스가 딸린 것)는:

```
로트 합계     통틀어 몇 kg (자루 + 낱개 + 박스)
items.stock   그중 자루로 남은 벌크만
```

로트를 건드려도 `stock`을 덮지 않는다. 판정은 `mutateRawMaterialLots` 안에 있다 —
부르는 쪽마다 챙기게 하면 한 곳이 빠뜨려서 출고할 때마다 벌크 재고가 로트합으로 덮인다.

### 재고는 화면값이 아니라 DB에서 읽어 더한다
`updateItem(col, id, { stock: 화면값 + delta })`는 금지. 화면값은 클릭 순간의 React 상태라,
그 사이 다른 쓰기가 끼면 통째로 덮어쓴다. `adjustItemStock`을 쓴다.

### 저장 단위는 언제나 kg
`items.stock` · `item_bom.quantity` · 로트 · 원료수불부 전부 kg. 기름도 마찬가지다.
L은 **표시·입력에서만** 쓰고 `density`로 환산한다.

### ⚠ 원장은 둘이다 — 실제 원장 ≠ 원료수불부

**섞으면 안 된다.** 같은 날짜·같은 양일 필요가 없고, 서로 맞추려 들면 둘 다 망가진다.
자세한 건 [src/shared/docOil.ts](src/shared/docOil.ts) 머리말에 있다.

| | **실제 원장** (`rawMaterialLedger`) | **서류용 원장** (`rawDocEntries`) |
|---|---|---|
| 무엇 | 창고에서 실제로 일어난 일 | 관청에 내는 원료수불부 |
| 언제 | 그 일이 **실제로 일어난 때** | **배송완료일**(판매기록부와 같은 날) |
| 사용량 | 작업완료 시 자동차감(`rm-auto`) | **판매에서 되계산** (`docSaleLine → docOilKg → addOilByRaw`) |
| 입고 | 실제 입고 | 실제 원장 그대로 ← 실제 원장에서 오는 **유일한** 값 |
| 이월 | 로트 누적 | **전달 서류의 기말재고** (실제 원장 누적이 아니다) |
| 단위 | 등급(통깨참기름·깨분참기름 …) | 서류 품목 기준 |
| 화면 | 재고관리 › 원료재고 › 입출고 기록 | 서류 관리 › 원료수불부 |
| 로트 | **로트와 한 몸**으로 움직인다 | 로트와 무관 |

**절대 하면 안 되는 것**
- 서류용 원장에 실제 원장의 자동차감(`rm-auto`)·실사를 넣는 것
- 두 원장의 숫자가 다르다고 한쪽을 다른 쪽에 맞추는 것 — 다른 게 정상이다
- 서류 생성기가 이미 있는 줄을 덮어쓰는 것 (매입·전월이월은 손입력이다)

**공통**: 완제품·박스는 **어느 원장에도 안 들어간다.** 둘 다 원료 전용이다.
완제품 재고는 `items.stock` 숫자만 바뀌고, 입출고 이력은 주문(`orders`)에만 남는다.

**서류 계산은 `docOil.ts` 한 곳에만 둔다.** 네 서류(원료수불부·생산판매기록부·생산작업기록부·
생산작업기록부2)가 같은 숫자를 써야 한다. 예전엔 같은 계산이 다섯 군데에 흩어져 kg 규격 처리·
품목 리매핑·배합비가 서로 달랐다.

---

## 4. 원가

### 원가 = Σ 구성품 원가 × 수량 ÷ 수율
**수율은 나누는 값이다.** 수율 37%는 "들깨 1kg에서 기름 0.37kg이 나온다"는 뜻이라
기름 1kg에는 들깨 1/0.37 = 2.7kg이 든다. 곱하면 7배 작아진다.

### 면세 원료 → 과세품이면 ×1.1
매입세액을 못 빼서 그만큼이 그대로 원가에 얹힌다. 근거는 `Item.taxType`(안 정하면 과세).
면세품을 만들 땐 안 얹는다 — 뗄 세금이 없다.

- 면세: 참깨 · 검정참깨 · 들깨 · 탈피들깨가루
- 과세: 나머지 전부(깨분 · 깻묵 포함)

### 원가 출처는 품목이 정한다 (`costSource`)
| 값 | 뜻 |
|---|---|
| `rollup` (기본) | 구성·원료식에서 계산. 원료값이 바뀌면 따라온다. |
| `manual` | 손으로 넣은 `cost`를 못 박는다. 계산이 실제와 안 맞을 때의 탈출구. |

저장값이 무조건 이기게 두면 원료값이 올라도 원가가 옛 값에 굳는다
(통들깨들기름이 들깨 5,795원 시절 값을 들고 있었다 — 실제 6,700원).

---

## 5. 전표·회계

### 전표 구분은 "갚을 상대가 있느냐"
매입/매출/대체 전표 + 자금원장 네 갈래. 자세한 건 [docs/복식부기-설계.md](docs/복식부기-설계.md).

### 쪼갠 줄(`lines`)이 있으면 줄이 임자다
한 번 움직인 돈의 성격이 둘 이상일 때(대출상환=원금+이자, 급여=총액+원천공제) `lines`로 끊는다.
`amount`는 줄 합이고 `accountCode`는 안 쓴다. **편집 화면은 `lines`를 반드시 같이 실어 보낸다** —
안 보내면 저장 한 번에 줄이 사라지고 원장 금액과 분개가 따로 논다.

### 상계(대체)는 방향을 고를 수 없다
돈이 안 움직였으니 통장 줄을 안 세운다. 줄의 부호가 차·대를 정한다(양수=차변, 음수=대변).
편집 화면이 '출금'으로 보여줄 뿐이니 저장할 때 `dir`을 건드리면 안 된다.

### 음수를 빈 값으로 취급하지 않는다
반품은 수량·공급가·세액·합계가 전부 음수다. `> 0`으로 거르면 화면이 통째로 빈다. `!== 0`으로 볼 것.

### 전표에서 거래처 단가를 저장할 때 과세/면세도 같이 저장한다
옛 값을 물려주기만 하면, 전표에서 면세로 끊어도 거래처엔 과세로 남아 다음 전표가 또 과세로 열린다.

### 통장 잔고는 관리하지 않는다
103 보통예금이 음수여도 문제가 아니다. 지적하지 말 것.

### 거래처 잔액은 연말 앵커부터 읽는다
`partnerBalanceSnapshots/{회사}-{연도}` — 그 해 12/31의 거래처별 받을 돈·갚을 돈과
**그날 아직 안 끝난 전표·남은 금액**을 박아 둔다. 화면은 앵커 **다음 날부터만** 읽고,
앵커가 짚어 준 미결 전표만 id로 따로 집어 온다(`fetchByIds`).

- 박는 법: `npx tsx scripts/stamp-partner-anchor.mts --year=2026 --apply` (기본은 미리보기)
- 앵커가 없으면 기초일부터, 기초도 없으면 전부 읽는다. **지금은 앵커가 없어 기초일부터 읽는다.**
- 규칙은 `src/features/admin/partnerAnchor.ts` 하나에 있다. 화면이 규칙을 따로 들면 언젠가 갈린다.

**잔액만 담으면 앵커가 아니다.** 이 화면은 전표 한 장씩 얼마 남았나도 쓴다(수금·지불 버튼이
거기 달린다). 미결 전표를 안 담고 옛 전표를 안 읽으면 그 거래처 수금이 새 전표를 갉아먹어
안 갚은 전표가 '완납'으로 보이고 버튼이 사라진다.

**소급 전표가 들어오면 앵커가 낡는다.** 그 거래처만 다시 세면 된다(`refreshPartner`) —
스크립트를 다시 돌려도 된다. 어차피 그날까지를 처음부터 다시 센다.

**앵커 안에서 미결 합 = 잔액이어야 한다.** 스크립트가 이 어긋남을 찍는다. 그래서 반품처럼
총액이 음수인 전표도 미결에 담는다(FIFO 배분은 못 건드리는데 잔액에서는 상계되기 때문).

### 화면 모양도 한 곳에서 — 메뉴마다 다르면 안 된다 (2026-09-03 사장님 지시)
**기준은 거래명세서와 손익/비용분석이다.** 사장님이 지금 쓰기 괜찮다고 한 둘이고,
실제로 세어 보니 그 둘이 거의 같은 어휘를 쓰고 있었다. 그걸 그대로 규칙으로 삼는다.

    모서리   rounded-xl(칸·단추) · rounded-2xl(판)
    글자     text-xs(본문) · text-[11px](곁들임) · text-[10px](딱지·표 머리)
    여백     px-4 py-2.5(표 칸) · px-5 py-4(판 안쪽)
    틈       gap-2(가로) · gap-3(세로)
    테두리   border border-slate-200(판) · border-slate-100(줄)
    바탕     bg-white(판) · bg-slate-50(머리·바닥)

**단추는 뜻으로 색을 고른다** — 화면마다 다른 색을 쓰면 무엇이 중요한지 안 보인다.

    BTN            회색   이 화면에서 제일 중요한 하나(발행·저장)
    BTN_IN         파랑   **돈이 들어오는 쪽**(수금·매출)
    BTN_OUT_MONEY  빨강   **돈이 나가는 쪽**(지불·매입)
    BTN_OUT        초록   서류를 내보내는 쪽(엑셀·PDF)

돈이 오가는 단추는 **파랑/빨강을 지킨다.** 한 번 회색으로 통일해 봤는데
"들어오나 나가나"가 색으로 안 읽혔다 — 그게 그 화면에서 제일 중요한 정보다(2026-09-03 사장님).
    BTN_SUB     흰색   그 밖(취소·닫기·보조)
    BTN_WARN    노랑   아직 안 끝난 것·봐야 할 것(대기·주의)
    BTN_DANGER  빨강   되돌릴 수 없는 것(삭제)
    (탭 활성은 남색 bg-indigo-600)

**탭도 셋뿐이다.** 2026-09-03 에 세어 보니 관리자 화면에 **72가지**가 있었다 —
같은 탭인데 화면마다 크기·모서리·글자가 달랐다. 크기로 층을 나눈다.

    tab     화면 맨 위 큰 갈래       px-3 py-2    rounded-lg   text-xs
    tabSm   그 안 작은 갈래          px-2.5 py-1  rounded-lg   text-[11px]
    pill    목록 안 딱지형 고르기     px-2 py-0.5  rounded-full text-[10px]

2026-09-03 에 관리자 화면 단추 색이 **열넷**이었다(indigo-500/600, amber-500/600,
slate-600/700, rose-500/600, emerald-500/600, sky-500, teal-600, violet-600, blue-600).
뜻이 같은데 색만 다른 것들이라 **여섯으로 묶었다.** 색이 많으면 무엇이 중요한지 안 보인다.

**폰에서 표는 옆으로 민다 — 칸을 감추지 않는다.**
진짜 문제는 스크롤이 아니라 **글자가 한 자씩 세로로 쌓이는 것**이었다. 둘이 같이 필요하다:

    ① 표에 최소 너비    w-full min-w-[720px]   → 넘쳐서 스크롤이 생긴다
    ② 칸에 nowrap       whitespace-nowrap      → 안 접힌다

`w-full` 만 주면 폭에 맞춰 칸이 찌그러지고, 넘치는 게 없으니 스크롤도 안 생긴다.
좌우 두 판을 쓰는 화면은 폰에서 **위아래로 쌓는다**(`lg:` 위로만 가로).

셈이 아니라 모양도 **[shared/ui/table.ts](src/shared/ui/table.ts) 한 곳**에 있다.
새 화면을 만들 때 거기 있는 걸 쓰고, 없으면 거기 더한다.

### 규칙은 글이 아니라 **테스트로** 지킨다 (2026-09-03 사장님 지시)
"같은 일에는 같은 함수를 쓴다"를 이 문서에 적은 그날, **적은 사람이 바로 어겼다** —
거래처통계의 수금·지불을 거래처원장으로 옮기면서 `accountCode: 매출 ? '108' : '251'` 을
또 한 벌 적었다. 사장님이 짚어 줘서 알았다.

**글로 적은 규칙은 안 지켜진다.** 그래서 지킬 수 있는 것은 테스트로 막는다.

    payment.test.ts   화면에서 108/251 을 손으로 가르면 걸린다
    docNo.test.ts     스케줄러가 앱과 다른 번호 규칙을 쓰면 걸린다
    docTabs.test.ts   화면은 있는데 들어갈 문이 없으면 걸린다

셈을 공용 모듈로 뺄 때는 **"손으로 다시 짓는 자리가 없다"는 테스트를 같이 붙인다.**
소스를 글자로 짚는 무른 테스트라도, 없는 것보다 낫다 — 실제로 조용히 깨진 게 저것들이다.

### 같은 일에는 같은 함수를 쓴다 — 고치거나 붙일 때도 (2026-09-03 사장님 지시)
**새 기능을 붙이거나 고칠 때, 그 셈이 이미 어딘가에 있으면 거기서 가져다 쓴다.**
없으면 공용 모듈에 만들고 부르는 쪽은 결과만 받는다. **화면 안에서 새로 세지 않는다.**

한 벌 더 적는 순간 갈릴 준비가 끝난다 — 두 벌이 같은 값을 내는 건 오늘까지고,
누가 한쪽만 고치는 날 갈린다. 그리고 **갈린 걸 아무도 못 알아챈다.**
이 프로젝트에서 실제로 그렇게 갈린 것들이다:

| 무엇 | 몇 벌 | 얼마나 갈렸나 |
|---|---|---|
| 부가세 역산 | 9 | 1,070원×7개에서 2원 |
| 마진율 | 4 | 328줄 중 **308줄**, 평균 36.7%p, 부호까지 뒤집힘 |
| 전표번호 규칙 | 2 (앱·스케줄러) | `2026-08-0216` 이 두 전표에 붙음 |
| 재고평가 | 2 (앱·스케줄러) | **1,050만원** |
| 월별 실수금 | 2 (settlements·분개) | 8월 1억 9,872만 vs **5억 74만** |
| 손익 판정 | 2 (전표갈래·분개) | 급여 대체전표 **1,931만원**이 통째로 빠짐 |
| 전표 잔액 | 2 (`openBalance`·`allocatePartnerCash`) | 같은 전표가 두 화면에서 1,026만 vs 186만 |

**셈이 있는 곳** — 새로 짜기 전에 여기부터 본다.

    돈 가르기    lineAmount(세포함) · lineAmountFromSupply(세별도) · priceParts
    마진         margin.ts
    원가         bomCost.ts(buildCostFn) · costCalc.ts
    손익 판정     financials.ts(plMovement · plOfJournals)
    분개         autoJournal.ts · buildJournals.ts
    거래처 잔액   cashLedger.ts(buildPartnerLedger · allocatePartnerCash · cashPaidByMonth)
    수금·지불     payment.ts(buildPaymentEntry)
    날짜·시각     day.ts(today · dateOfLocal · timeOfLocal) · voucherStamp.ts(stampFor · nextDocNo)
    입고         receipt.ts(recordReceipt) · rawReceipt.ts
    개입수        orderUnits.ts · packIndex.ts · bomIndex.ts

**`functions/` 는 예외가 아니다.** 별도 빌드라 import 를 못 해서 손으로 옮겨 적었는데,
그래서 두 번 갈렸다. 옮겨 적을 때는 **양쪽에 서로를 가리키는 주석을 남기고**,
가능하면 테스트로 묶는다([docNo.test.ts](src/shared/docNo.test.ts) 가 functions 소스를 읽는다).

### 같은 일을 하는 코드가 둘이면 공용 모듈로 뺀다
둘로 두면 언젠가 갈리고, **갈린 쪽이 어느 쪽인지 아무도 모른다.** 이번 세션에 실제로 셋 나왔다.

| 무엇 | 몇 벌이었나 | 어떻게 갈렸나 |
|---|---|---|
| 부가세 역산 ([lineAmount.ts](src/shared/lineAmount.ts)) | 9 | 주문 경로만 **단가를 먼저 나눠** 1,070원×7개에서 2원 차이 |
| 거래처 단가 되밀기 ([partnerPriceSync.ts](src/shared/partnerPriceSync.ts)) | 3 | 수정 경로가 "아니요"를 무시했고, 매출이 통째로 빠져 있었다 |
| 자금 전표 창 ([CashEntryModal](components/voucher/CashEntryModal.tsx)) | 2 | 수금·지불과 자금전표 수정을 따로 그리고 있었다 |
| 초과수금 가르기 ([paymentSplit.ts](src/shared/paymentSplit.ts)) | 1 | 발행과 같은 클릭이면 방금 끊은 전표를 못 봐 전액이 선수금으로 갔다 |
| 박스 개입수 ([orderUnits.ts](src/shared/orderUnits.ts)) | 8 | 12를 손으로 박아 놨다. 박스 품목 140개 중 102개가 12개입이 아니다 |
| 마진율 ([margin.ts](src/shared/margin.ts)) | 4 | 둘은 공급가로, 둘은 **세금 포함 단가로** 나눴다. 328줄 중 308줄이 갈렸다 |

**부가세는 합계에서 역산한다** — `반올림(수량 × 단가 ÷ 1.1)`. 단가를 먼저 나누면 개수만큼
오차가 쌓인다. 세금계산서가 공급대가 합계 기준이라 그쪽이 맞다(2026-09-01 확정).
이 앱에서 사람이 치는 단가는 **언제나 세금 포함 값**이다.

### 승인된 휴가는 '사용'이 아니라 '예정'이다
승인만 하면 두 달 뒤 연차가 오늘 사용에 잡혔다(2026-09-02 사장님 지적).
아직 쉬지도 않았는데 쓴 것으로 보인다.

그렇다고 잔여에서 빼지 않으면 **없는 날을 또 내준다.** 그래서 칸을 나눈다 —

    사용  승인 + **이미 시작됨**(시작일 ≤ 오늘)
    예정  승인 + **아직 안 옴**
    잔여  = 부여 − 사용 − 예정      ← 숫자는 전과 같다. 보이는 방식만 갈랐다

'시작됐나' 판정은 [leave.ts](src/shared/leave.ts) 의 `hasStarted` 하나뿐이다.
관리자(HRManager)와 직원 앱(LeaveManager)이 같은 걸 쓴다 — 직원 앱은 여태 손으로
따로 세고 있었다.

**여러 날에 걸친 신청은 쪼개지 않는다.** 시작일 하나로 가른다. 9/1~9/5 를 3일 쓰고
2일 남았다고 나누면 화면의 그 숫자가 뭘 뜻하는지 아무도 못 읽는다.

### 손익은 분개에서 센다 — 전표 갈래로 세지 않는다
`type === '매입'` 으로 비용을 세면 **대체전표가 통째로 빠진다.** 급여 발생·감가상각·
퇴직급여충당은 갈래가 '비용'이다. 2026-08 급여 **19,314,620원**이 전표 화면 하단 합계에서
그렇게 사라져 있었다(2026-09-03 사장님 발견).

전표 총액으로 세도 안 된다 — **부가세가 섞인다.** 1,070,000 매출전표의 손익은 1,000,000이고
70,000은 부가세예수금(부채)이다.

셈은 [financials.ts](src/features/admin/financials.ts) 의 `plMovement`(줄 하나) ·
`plOfJournals`(뭉치) 둘뿐이다. 규칙은 —

    ① 수익·비용 계정만 본다        자산·부채·자본은 손익이 아니다
    ② 정상 방향(normalBalance)으로  비용은 차변, 수익은 대변

### 마진은 공급가에서 센다
부가세는 손님한테 받아서 그대로 내는 돈이라 **우리한테 남는 게 아니다.** 그걸 분모에 두면
마진이 부풀어 보이고, 밑지는 품목이 남는 것처럼 보인다.

    볶음참깨/1kg   단가 5,600  원가 5,510
      판매가 기준   (5600−5510)/5600 = **+1.6%**   ← 남는다고 보인다
      공급가 기준   (5091−5510)/5091 = **−8.2%**   ← 실제로는 밑진다

거래처 단가 328줄 중 **308줄에서 값이 갈렸고, 평균 36.7%p** 차이였다. 부호가 뒤집힌 줄도 있다.

셈은 [margin.ts](src/shared/margin.ts) 하나다. 세금 포함 단가를 들고 있으면 `marginOf(단가, 원가, 면세)`,
견적서처럼 공급가를 이미 손에 들고 있으면 `marginFromSupply(공급가, 원가)` — 부가세를 두 번 빼지 않으려고 갈라 뒀다.
**마진율(÷판 값)과 원가율(÷든 값)은 다른 것이다.** 장사에서 말하는 '마진'은 앞엣것이라 그쪽이 기본이다.

원가 자체를 굴리는 셈([bomCost.ts](src/shared/bomCost.ts)의 `buildCostFn`)은 처음부터 한 벌이라 손대지 않았다.

### 갈래 딱지는 카테고리를 본다 — 이름으로 짚지 않는다
`categoryOf(item)` ([productChip.tsx](src/shared/productChip.tsx)). `category` 가 있으면 그것,
없으면 타입 이름(완제품·부자재…). **이름에서 '참기름'을 읽어 채우던 길은 걷어냈다** —
그건 카테고리를 채우기 전 마이그레이션용 임시였고, 지금은 525품목 중 13개만 비어 있다.

품목관리 화면이 카테고리 칸에 **`subtype`(낱개·박스·벌크)** 을 그리고 있었다(518품목).
그리고 subtype 이 빈 라벨 부자재는 이름으로 짚어 '참기름'이 찍혔다.
재고관리 화면은 이미 `category` 를 봤다 — **같은 셈이 두 벌이라 한쪽만 고쳐져 있었다.**

### 박스 개입수의 근거는 둘뿐이다
| | 무엇 | 몇 품목 |
|---|---|---|
| ① BOM | **박스를 별개 품목으로 둔 것.** 박스째 쌓아 두고 박스로 센다. 겉박스·테이프까지 그 BOM 에 달려 있다(129/136) | 136 |
| ② `item_pack` | **낱개로만 세는데 박스로 말하는 것.** 재고는 개로 두고 주문 입력만 박스로 받는다 | 9 (향미유 7·고춧가루 2) |

`unitsPerBoxOf` 가 이 둘만 본다. 예전엔 `boxSize` 필드 · 규격 글자 · 코드에 박은
'향미유면 12' 까지 넷이었는데, **근거가 넷이면 어느 게 맞는지 아무도 못 믿는다.**
실제로 갈려서 규격이 안 따라간 다섯 품목에서 0이 나왔다(BOM 엔 40·10 이 멀쩡히 있었다).

- 박스로 팔 품목이 늘면 ①이나 ②에 넣는다. **품목 문서에 필드를 더하지 않는다.**
- 화면의 낱개/BOX 토글도 `unitsPerBoxOf(product) > 0` 으로 뜬다 — 갈래(`향미유`)로 안 가른다.
- 규격 글자(`1kg * 20`)는 **사람이 읽는 것**이지 근거가 아니다. 낱개를 고치면 따라 쓴다([boxSpec.ts](src/shared/boxSpec.ts)).

### 오늘 날짜는 `today()` — `new Date().toISOString()` 을 쓰지 않는다
`toISOString()` 은 **UTC** 다. 한국(UTC+9) 아침 9시 전에는 **어제 날짜**를 준다.
공장은 아침 일찍 도는데, 그 시간에 찍은 전표·실사·로트·급여가 전부 하루 앞으로 들어갔다.
**22개 파일 49군데**가 그렇게 쓰고 있었다(2026-09-01 정리).

### 스케줄러(functions)는 앱과 **같은 규칙**이어야 한다
`functions/` 는 구글 서버에서 저절로 도는 코드다 — 앱을 안 켜도 실행된다.

    dailyAutoVoucher          매일 07:00   정기 전표 자동 발행(임대료·리스료·보험료)
    monthlyInventorySnapshot  매일 23:00   말일이면 월말 재고평가액 저장
    findUsername · requestPasswordReset    로그인 화면에서 부른다

**별도 빌드라 앱 소스를 import 못 한다.** 그래서 같은 규칙이 손으로 한 벌 더 들어가 있고,
한쪽만 고치면 조용히 갈린다. 2026-09-03 에 두 군데가 실제로 갈려 있었다 —

| 무엇 | 앱 | 스케줄러(고치기 전) | 벌어진 것 |
|---|---|---|---|
| 전표번호 | `260901-01` (그날 최대+1) | `2026-09-0001` (그 달 개수+1) | 지운 번호를 다시 써서 `2026-08-0216` 이 두 전표에 붙었다 |
| 재고평가 회사 | 태백/풍회를 가른다 | 안 가른다 | 풍회 재고 **566만원**이 태백 재고자산에 얹혔다 |
| 재고평가 음수 | 넣는다 | `stock > 0` 으로 뺀다 | **483만원**. 어긋난 재고를 감추면 창고와 장부가 갈린다 |

합쳐서 재고평가액이 **1,050만원** 갈려 있었고, 그 값은 `journalizeInventory` 를 타고
**매출원가로 흘러간다.** 지금은 38원(반올림 11품목)까지 좁혔다.

**단가는 `item.cost` 를 쓴다 — 그게 맞다.** 앱이 BOM 롤업 결과를
[recomputeAllCosts](src/features/admin/AdminApp.tsx) 로 그 칸에 되써 두기 때문이다.
BOM 롤업을 functions 에 또 옮겨 적으면 갈릴 자리만 하나 더 는다.

**배포도 따로다** — `npm run deploy:all` 로는 안 올라간다. `firebase deploy --only functions`.
앱 테스트가 functions 소스를 읽어 전표번호 규칙이 갈렸는지 본다([docNo.test.ts](src/shared/docNo.test.ts)).

### 시간은 **로컬(KST) 하나로만** 읽고 쓴다 (2026-09-03 사장님 지시)
저장은 ISO(UTC)로 하지만, **읽을 때 UTC로 자르면 안 된다.**

    '2026-08-03T15:00:00Z'.slice(0, 10)   →  2026-08-03   ← 여태 이렇게 읽었다
    dateOfLocal('2026-08-03T15:00:00Z')   →  2026-08-04   ← 한국에서는 8월 4일이다

밤 9시(=12:00Z) 이후에 만든 기록이 통째로 하루 앞으로 밀린다.
실측 — 주문 만든날 **75줄**, 생산일 **11줄**, 전표 도장 **22줄**. 배송완료일은 0줄이라
서류 넷(원료수불부·판매기록부·생산작업기록부·벤조피렌)은 안 움직였다. 운이 좋았던 것이다.

**읽는 자리는 [day.ts](src/shared/day.ts) 둘뿐이다** — 날짜는 `dateOfLocal`, 시각은 `timeOfLocal`.
쓰는 자리는 `today()` 와 `stampFor()`. 넷 다 로컬 기준이라 서로 어긋나지 않는다.
저장된 시각 칸(`createdAt`·`issuedAt`·`taxIssuedAt`·`deliveredAt`…)에 `.slice(0, 10)` 을 쓰지 마라.

### 전표를 고칠 때 거래일이 바뀌면 시각 도장도 다시 찍는다
끊을 때는 `stampFor(tradeDate)` 로 제대로 찍는데(소급 23:59:59 · 예약 00:00:00 · 당일 지금),
**고치는 경로에는 그게 안 붙어 있었다.** 날짜만 옮기고 도장은 옛 날짜 것을 달고 있었다.

    260827-12   거래일 8/28 인데 도장은 8/27 23:59:59
    260828-02   거래일 8/31 인데 도장은 만든 시각 그대로 (예약이면 00:00:00 이어야 한다)

발행 경로는 `docNo`·`issuedAt`·`tradeDate` 를 **같은 변수 하나**에서 뽑아 절대 안 어긋난다.
실측으로도 어긋난 10건이 **전부 만들 때는 맞았고 나중에 거래일만 고친 것**이었다.

셈은 [statementEdit.ts](src/shared/statementEdit.ts) 하나다 — 전표는 `statementEditPatch`,
자금원장은 `cashEditPatch`. 화면마다 `updateItem('issuedStatements', …)` 을 인라인으로 부르던
**다섯 자리**를 `AdminApp.updateStatement` 하나로 모았다.

**전표번호(docNo)는 다시 안 매긴다** — 인쇄해서 건넨 종이에 박혀 있을 수 있다.
어긋난 건 `docNoDateMismatch` 로 짚어만 낸다.

**8월 전표는 손대지 않는다** — 사장님이 날짜를 손으로 다 맞춰 뒀다(2026-09-03).
`scripts/fix-stamp-after-dateedit.mts` 는 기본 시작달이 `2026-09` 이고 그 이전은 거절한다.

### 날짜 셈은 `src/shared/day.ts`로
`new Date('2026-12-31T00:00:00')`은 그 자리 시간의 자정이고 `toISOString()`은 UTC로 되돌린다.
한국(UTC+9)에서는 전날 15:00Z가 되어 `slice(0,10)`이 **하루 앞**을 준다.
견적서 유효기한·주문 배송일이 실제로 하루씩 밀리고 있었다. 손으로 세지 말 것.

---

## 6. 데이터를 고칠 때

### 스크립트는 `--dry`가 기본, `--apply`로만 쓴다
`scripts/fix-*.mts` 형식을 따른다.

1. 미리보기로 **무엇이 몇 건 바뀌는지** 먼저 찍는다
2. 되돌릴 값을 백업 JSON으로 남긴다(가능하면 `--undo`도)
3. 적용한 뒤 [DB-CHANGELOG.md](DB-CHANGELOG.md)에 **근거와 되돌리는 법**을 적는다

읽기 전용 진단은 `scripts/diag-*.mts`로 만들고 일이 끝나면 지운다.

### 덮어쓰기 전에 대조한다
옛 필드를 지우기 전에 새 원천과 **차이가 0인지** 확인하고 지운다.

---

## 7. 코드

- 커밋 전 `npx tsc --noEmit` + `npx vitest run` 둘 다 통과시킨다
- 고친 자리에는 **왜 그렇게 됐는지**를 주석으로 남긴다. "무엇을 하는가"는 코드가 말한다
- 화면 조각에 묻혀 테스트가 안 닿는 판정은 순수 함수로 꺼낸다
  (`shared/cashEntryEdit.ts` · `shared/bomIndex.ts` 가 그렇게 나왔다)
- 죽은 상수·별칭은 발견하는 대로 지운다. 남겨 두면 다음 사람이 그게 근거인 줄 안다

---

### 화면도 테스트한다 (2026-09-01부터)
`vitest.config.ts` + `src/test/setup.ts`. 화면을 띄우는 테스트는 파일 맨 위에
`/** @vitest-environment jsdom */`를 단다 — 순수 계산 테스트 700여 개까지 브라우저를
흉내낼 이유가 없어서 기본은 node다.

돈이 오가는 창은 **눈으로 보고 넘기지 않는다.** 첫 대상은
[components/voucher/CashEntryModal.test.tsx](components/voucher/CashEntryModal.test.tsx) —
거기 잠근 건 전부 실제로 한 번씩 틀렸던 것이다(총액이 기본값이라 두 번 나감 ·
날짜가 오늘로 박힘 · 쪼갠 줄이 사라짐).

**테스트를 쓰면 일부러 망가뜨려 본다.** 통과만 하는 테스트는 값이 없다.
위 다섯 가지를 하나씩 되돌려 전부 빨간불이 뜨는 걸 확인하고 넣었다.

E2E(Playwright)는 깔려만 있고 안 쓴다. 앱이 **운영 파이어베이스에 바로 붙어서**
돌리면 진짜 전표가 쌓인다. 쓰려면 에뮬레이터부터 붙여야 한다.

---

## 8. 데이터베이스 — 지금 묶인 곳과 옮길 때 드는 값

> 지금 Firestore를 쓴다. 언젠가 SQL로 옮길 수도 있고, 여러 회사에 팔 수도 있다.
> **당장 옮기려고 층을 미리 짓지는 않는다** — 안 쓸 추상은 옮길 때 결국 안 맞는다.
> 대신 아래 습관만 지키면 그때 드는 값이 크게 줄어든다.

### Firestore를 부르는 자리는 `shared/services/firebaseService.ts` 하나여야 한다

여기가 유일한 문이다. 조건 조회·트랜잭션·배치까지 문 안에 있다
(`fetchWhere` · `subscribeWhere` · `writeMany` · `mutateDoc`).

**밖에서 `firebase/firestore`를 직접 부르면 안 된다.** 예전엔 이 파일에 없는 기능이
필요하면 화면이 직접 불렀고, 그렇게 8개 파일이 샜다. 필요한 게 없으면 **문을 넓혀라.**

문이 하나여야 되는 것들:
- 회사 격리를 한 곳에서 강제 (지금은 화면마다 `companyOf(x) === companyId`를 손으로 적는다 — 2026-09-01에만 세 군데가 빠져 있었다)
- 다른 DB로 갈아 끼우기
- 읽기 횟수 줄이기·캐시

### Firestore 고유 타입을 쓰지 않는다

`Timestamp` · `serverTimestamp` · `FieldValue` · `DocumentReference` · `increment` —
**한 군데도 안 쓰고 있다. 그대로 두자.**

날짜는 `'2026-09-01'` / `'2026-09-01T09:00:00.000Z'` 문자열, 숫자는 숫자, 참조는 id 문자열.
이게 옮길 때 제일 큰 짐인데 이미 없다. 새로 쓸 때도 이 규칙을 지킨다.

### 문서 안에 배열을 새로 만들지 않는다

지금 이렇게 든 것들이 있다:

```
items.lots[]            로트
items.submaterials[]    구성(이건 item_bom 으로 옮겼고, 화면용 파생값이다)
issuedStatements.items[]  전표 줄
cashEntries.lines[]     쪼갠 줄
```

SQL이면 전부 별도 테이블 + 외래키다. **옮길 때 진짜 비용은 여기다** — 드라이버를
갈아 끼우는 건 하루지만 이걸 풀어내는 건 몇 달이다.

이미 있는 건 그대로 두되(고치는 게 더 위험하다) **새 데이터는 별도 컬렉션으로 만든다.**
`item_bom` · `settlements` · `partner_item` 이 그렇게 나왔다.

### 조인이 없다는 걸 기억한다

`allItems.find(p => p.id === ...)` 가 수백 군데 있다. Firestore엔 조인이 없어서
앱이 메모리에서 맞추는 것이다. 그래서:

- **한 번에 다 읽는다**(`useAppData`가 컬렉션을 통째로 들고 있다)
- 데이터가 늘면 이게 먼저 무너진다 — 지금 전표 300건·품목 700개라 버틴다
- SQL로 가면 이 자리들이 전부 쿼리가 된다

### 실시간 구독은 SQL에 없다

`subscribeToCollection` · `subscribeWhere` 는 Firestore 기능이다. SQL로 가면
폴링이나 별도 인프라(WebSocket·LISTEN/NOTIFY)가 필요하다.
**구독을 쓰는 자리를 늘리지 않는다** — 한 번 읽어도 되는 건 `fetchCollection`을 쓴다.

### 여러 회사에 팔려면 — 순서

지금은 "한 사장님이 회사 둘을 굴리는" 수준이다. 남에게 팔려면 이 순서다.

1. **인증을 갈아엎는다** — 지금은 익명(Anonymous Auth)이라 Firebase가 "이 사람이 누구
   회사인지" 모른다. 실제 계정 + Cloud Function으로 회사 id를 Custom Claim에 박아야 한다.
2. **보안 규칙으로 격리한다** — `resource.data.companyId == request.auth.token.companyId`.
   1번 없이는 쓸 수 없는 규칙이다. **지금 규칙은 `if request.auth != null` 하나라, 앱을
   연 사람은 누구나 전 회사 데이터를 읽는다.**
3. **회사 없는 데이터를 없앤다** — `companyOf(x)`가 `companyId`가 없으면 태백으로 친다.
   편해서 그렇게 뒀는데, 남에게 팔면 주인 없는 데이터가 전부 첫 회사 것이 된다.
4. **회사별로 갈려야 할 것을 가른다** — `accountCodes` · `accountGroups` · `itemTaxonomy` ·
   `openingBalances`가 아직 공용이다. 사장님 회사 둘이면 안 거슬리지만, A업체가 계정
   이름을 바꾸면 B업체 장부가 같이 바뀐다.

**SQL 전환은 이 넷 다음이다.** 순서를 바꾸면 두 번 일한다.
