# 인수인계

이 프로젝트에서 **지켜야 할 규칙**을 한자리에 모은다. 사장님이 정한 것과, 사고가 나서 정해진 것.
새로 붙는 사람은 코드를 읽기 전에 이걸 먼저 읽는다.

- 기술 스택·배포 명령: [CLAUDE.md](CLAUDE.md)
- DB를 건드린 이력과 되돌리는 법: [DB-CHANGELOG.md](DB-CHANGELOG.md)
- 설계 문서: [docs/](docs/)

---

## 1. 화면

### 검색할 때 창 크기가 바뀌면 안 된다
목록을 걸러도 팝업·패널 높이는 그대로여야 한다. 검색어를 칠 때마다 창이 커졌다 작아지면
누르려던 자리가 도망간다. 컨테이너에 **고정 높이**를 주고 안쪽만 스크롤시킨다
(`max-h-…` + `flex-1` 조합은 내용에 따라 흔들린다 — `h-[70vh]`처럼 못 박을 것).

### 빈 행을 딸려 보내지 않는다
'빠른 비용'·'품목 선택'처럼 줄을 추가하는 기능은 **요청한 줄만** 넣는다. 편의로 빈 줄을 하나
더 붙이면 누를 때마다 하나씩 쌓여 지우는 일이 된다. 빈 줄이 필요하면 '행 추가'가 따로 있다.

### 검색은 어디서나 초성으로 된다
`shared/hangul`의 `matchesSearch` 하나만 쓴다. 화면마다 제 초성 함수를 복사해 두면
어떤 창은 되고 어떤 창은 안 된다(실제로 세 벌이 돌아다녔다).

**겹자음을 펴야 한다** — 자판이 자음을 잇달아 치면 붙여 버린다(ㄱ·ㅅ → `ㄳ`). 초성은 19자뿐이라
겹자음은 초성으로 절대 안 나오고, 안 펴면 `ㄳ`으로 '고소한'을, `ㅄ`으로 '박스'를 못 찾는다.

### 같은 일을 두 화면에 두지 않는다
단가를 고치는 자리가 '단가관리' 패널과 '품목 선택'에 따로 있으면 단가 하나 바꾸려고 창을
두 번 연다. **고르는 화면에서 바로 고친다.** (2026-08-24에 단가관리 패널을 품목 선택으로 합쳤다.)

### 분류(타입·서브타입·카테고리)는 코드에 박지 않는다
탭 이름·순서·숨김, 목록의 선택지, 정렬 순서는 전부 **분류 관리(`itemTaxonomy`)**에서 딴다.
아이콘·색만 코드가 쥔다(분류표에 담을 값이 아니다).

- 비교는 **키**로 한다(`p.type === 'product'`). 라벨(`'완제품'`)로 견주면 이름을 바꾸는 순간 깨진다.
- 새 분류를 만들면(비닐·케이스) 코드를 안 고쳐도 화면에 떠야 한다.

> **분류표를 고쳐도 이미 붙은 값은 안 따라온다.** 분류 이름을 바꾸면 품목의 값도 같이 옮겨야 한다
> (배송→박스로 바꿨을 때 품목 130건이 옛 값을 들고 있어 새 이름으로 거르면 0건이었다).

### 이름을 근거로 판정하지 않는다
"박스 묶음인가"를 `subtype === '배송'`으로 가려내던 자리가 네 곳 있었다. 이름은 분류 관리에서
언제든 바뀐다. 판정은 **구성(BOM)**으로 한다 — 완제품 구성품 딱 하나 × 수량 2 이상.

---

## 2. 품목과 구성(BOM)

### 구성은 `item_bom`이 유일 원천이다
품목은 제 구성을 안 들고 다닌다. 읽기는 `bomOf(item.id)` ([src/shared/bomIndex.ts](src/shared/bomIndex.ts)),
저장은 편집 폼이 `bomDraft`로 따로 넘긴다. 품목 문서에 구성을 복사해 두면 같은 사실이 두 곳에 생겨
어느 쪽이 참인지 늘 물어야 한다.

### 중복 판정은 이름 + BOM
박스와 낱개는 이름이 같아도 **다른 품목**이다. 구성이 다르면 안 묶인다.
(이름만 보던 시절 111그룹이 떴고 그중 110개가 박스↔낱개 오탐이었다.)

### 부자재의 `category`는 '무슨 물건이냐'다
`모란 검정참깨`는 라벨이지 검정참깨가 아니고, `테이프-검정`의 검정은 색이다.
품목 계열을 옮길 때 부자재는 건드리지 않는다.

### 3단 분류
| 단 | 값 | 누가 정하나 |
|---|---|---|
| `type` | product · goods · wip · raw · submaterial | 고정(엔진 분기 키) |
| `subtype` | 낱개 · 박스 · 선물세트 · 벌크 · 무라벨 | 분류 관리 |
| `category` | 참기름 · 들기름 · 참깨 · 들깨 · 용기 · 마개 · 라벨 … | 분류 관리 |

- **선물세트·배송은 타입이 아니라 완제품의 subtype이다.** 타입에도 두면 같은 것이 두 자리에 존재한다.
- **`벌크`는 `isBulkItem`이 보는 값이다.** 재고차감·원료홀더 탐색 30여 곳이 여기 얹혀 있다.
  분류 관리 목록에 없으면 새 품목에 달 방법이 없어 조용히 개수로 취급된다.

---

## 3. 재고

### 로트와 재고는 다른 숫자일 수 있다
`lotsAreTotal`을 켠 원료(볶음참깨처럼 낱개·박스가 딸린 것)는:

```
로트 합계     통틀어 몇 kg (자루 + 낱개 + 박스)
items.stock   그중 자루로 남은 벌크만
```

로트를 건드려도 `stock`을 덮지 않는다. 판정은 `mutateRawMaterialLots` 안에 있다 —
부르는 쪽마다 챙기게 하면 한 곳이 빠뜨려서 출고할 때마다 벌크 재고가 로트합으로 덮인다.

### 재고는 화면값이 아니라 DB에서 읽어 더한다
`updateItem(col, id, { stock: 화면값 + delta })`는 금지. 화면값은 클릭 순간의 React 상태라,
그 사이 다른 쓰기가 끼면 통째로 덮어쓴다. `adjustItemStock`을 쓴다.

### 저장 단위는 언제나 kg
`items.stock` · `item_bom.quantity` · 로트 · 원료수불부 전부 kg. 기름도 마찬가지다.
L은 **표시·입력에서만** 쓰고 `density`로 환산한다.

### ⚠ 원장은 둘이다 — 실제 원장 ≠ 원료수불부

**섞으면 안 된다.** 같은 날짜·같은 양일 필요가 없고, 서로 맞추려 들면 둘 다 망가진다.
자세한 건 [src/shared/docOil.ts](src/shared/docOil.ts) 머리말에 있다.

| | **실제 원장** (`rawMaterialLedger`) | **서류용 원장** (`rawDocEntries`) |
|---|---|---|
| 무엇 | 창고에서 실제로 일어난 일 | 관청에 내는 원료수불부 |
| 언제 | 그 일이 **실제로 일어난 때** | **배송완료일**(판매기록부와 같은 날) |
| 사용량 | 작업완료 시 자동차감(`rm-auto`) | **판매에서 되계산** (`docSaleLine → docOilKg → addOilByRaw`) |
| 입고 | 실제 입고 | 실제 원장 그대로 ← 실제 원장에서 오는 **유일한** 값 |
| 이월 | 로트 누적 | **전달 서류의 기말재고** (실제 원장 누적이 아니다) |
| 단위 | 등급(통깨참기름·깨분참기름 …) | 서류 품목 기준 |
| 화면 | 재고관리 › 원료재고 › 입출고 기록 | 서류 관리 › 원료수불부 |
| 로트 | **로트와 한 몸**으로 움직인다 | 로트와 무관 |

**절대 하면 안 되는 것**
- 서류용 원장에 실제 원장의 자동차감(`rm-auto`)·실사를 넣는 것
- 두 원장의 숫자가 다르다고 한쪽을 다른 쪽에 맞추는 것 — 다른 게 정상이다
- 서류 생성기가 이미 있는 줄을 덮어쓰는 것 (매입·전월이월은 손입력이다)

**공통**: 완제품·박스는 **어느 원장에도 안 들어간다.** 둘 다 원료 전용이다.
완제품 재고는 `items.stock` 숫자만 바뀌고, 입출고 이력은 주문(`orders`)에만 남는다.

**서류 계산은 `docOil.ts` 한 곳에만 둔다.** 네 서류(원료수불부·생산판매기록부·생산작업기록부·
생산작업기록부2)가 같은 숫자를 써야 한다. 예전엔 같은 계산이 다섯 군데에 흩어져 kg 규격 처리·
품목 리매핑·배합비가 서로 달랐다.

---

## 4. 원가

### 원가 = Σ 구성품 원가 × 수량 ÷ 수율
**수율은 나누는 값이다.** 수율 37%는 "들깨 1kg에서 기름 0.37kg이 나온다"는 뜻이라
기름 1kg에는 들깨 1/0.37 = 2.7kg이 든다. 곱하면 7배 작아진다.

### 면세 원료 → 과세품이면 ×1.1
매입세액을 못 빼서 그만큼이 그대로 원가에 얹힌다. 근거는 `Item.taxType`(안 정하면 과세).
면세품을 만들 땐 안 얹는다 — 뗄 세금이 없다.

- 면세: 참깨 · 검정참깨 · 들깨 · 탈피들깨가루
- 과세: 나머지 전부(깨분 · 깻묵 포함)

### 원가 출처는 품목이 정한다 (`costSource`)
| 값 | 뜻 |
|---|---|
| `rollup` (기본) | 구성·원료식에서 계산. 원료값이 바뀌면 따라온다. |
| `manual` | 손으로 넣은 `cost`를 못 박는다. 계산이 실제와 안 맞을 때의 탈출구. |

저장값이 무조건 이기게 두면 원료값이 올라도 원가가 옛 값에 굳는다
(통들깨들기름이 들깨 5,795원 시절 값을 들고 있었다 — 실제 6,700원).

---

## 5. 전표·회계

### 전표 구분은 "갚을 상대가 있느냐"
매입/매출/대체 전표 + 자금원장 네 갈래. 자세한 건 [docs/복식부기-설계.md](docs/복식부기-설계.md).

### 쪼갠 줄(`lines`)이 있으면 줄이 임자다
한 번 움직인 돈의 성격이 둘 이상일 때(대출상환=원금+이자, 급여=총액+원천공제) `lines`로 끊는다.
`amount`는 줄 합이고 `accountCode`는 안 쓴다. **편집 화면은 `lines`를 반드시 같이 실어 보낸다** —
안 보내면 저장 한 번에 줄이 사라지고 원장 금액과 분개가 따로 논다.

### 상계(대체)는 방향을 고를 수 없다
돈이 안 움직였으니 통장 줄을 안 세운다. 줄의 부호가 차·대를 정한다(양수=차변, 음수=대변).
편집 화면이 '출금'으로 보여줄 뿐이니 저장할 때 `dir`을 건드리면 안 된다.

### 음수를 빈 값으로 취급하지 않는다
반품은 수량·공급가·세액·합계가 전부 음수다. `> 0`으로 거르면 화면이 통째로 빈다. `!== 0`으로 볼 것.

### 전표에서 거래처 단가를 저장할 때 과세/면세도 같이 저장한다
옛 값을 물려주기만 하면, 전표에서 면세로 끊어도 거래처엔 과세로 남아 다음 전표가 또 과세로 열린다.

### 통장 잔고는 관리하지 않는다
103 보통예금이 음수여도 문제가 아니다. 지적하지 말 것.

---

## 6. 데이터를 고칠 때

### 스크립트는 `--dry`가 기본, `--apply`로만 쓴다
`scripts/fix-*.mts` 형식을 따른다.

1. 미리보기로 **무엇이 몇 건 바뀌는지** 먼저 찍는다
2. 되돌릴 값을 백업 JSON으로 남긴다(가능하면 `--undo`도)
3. 적용한 뒤 [DB-CHANGELOG.md](DB-CHANGELOG.md)에 **근거와 되돌리는 법**을 적는다

읽기 전용 진단은 `scripts/diag-*.mts`로 만들고 일이 끝나면 지운다.

### 덮어쓰기 전에 대조한다
옛 필드를 지우기 전에 새 원천과 **차이가 0인지** 확인하고 지운다.

---

## 7. 코드

- 커밋 전 `npx tsc --noEmit` + `npx vitest run` 둘 다 통과시킨다
- 고친 자리에는 **왜 그렇게 됐는지**를 주석으로 남긴다. "무엇을 하는가"는 코드가 말한다
- 화면 조각에 묻혀 테스트가 안 닿는 판정은 순수 함수로 꺼낸다
  (`shared/cashEntryEdit.ts` · `shared/bomIndex.ts` 가 그렇게 나왔다)
- 죽은 상수·별칭은 발견하는 대로 지운다. 남겨 두면 다음 사람이 그게 근거인 줄 안다
