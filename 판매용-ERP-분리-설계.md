# 판매용 ERP 분리 설계

## 1. 목표

태백푸드 운영판을 실제 영업과 생산에 계속 사용하면서, 같은 코드를 다른 식품 제조사에 판매할 수 있게 한다.
판매용 시험·고객 데이터 때문에 태백푸드 운영 데이터와 배포가 영향을 받으면 안 된다.

## 2. 첫 판매 단계의 원칙

- React 코드 한 벌을 공유한다.
- 고객사마다 Firebase 프로젝트를 따로 만든다.
- 고객사마다 Firestore, Auth, Storage, Hosting, Functions, Secret을 분리한다.
- 태백푸드 운영 프로젝트는 판매용 시험 계정이나 고객 데이터에 사용하지 않는다.
- 공통 기능은 코드로 합치되 회사별 차이는 설정 문서와 기능 플래그로 둔다.

이 방식은 고객사마다 배포가 하나씩 늘지만, 한 회사의 보안 규칙·데이터 정정·장애가 다른 회사로 번지지 않는다.
초기 고객 수가 적을 때는 공용 DB 멀티테넌트보다 안전하고 복구도 쉽다.

## 3. 앱과 주소 구성

한 고객사 안에서는 다음 세 화면이 같은 Firebase 프로젝트를 사용한다.

| 영역 | 예시 주소 | 접근 |
|---|---|---|
| 회사 홈페이지 | `customer.example.com/` | 공개 |
| 거래처 주문 | `customer.example.com/order` | 거래처 로그인 |
| ERP | `erp.customer.example.com/` 또는 `/admin` | 직원 로그인 |

홈페이지와 주문 화면은 같은 링크 계열로 합칠 수 있지만, 브라우저가 Firestore를 직접 신뢰하게 만들면 안 된다.
주문 생성·단가 확정·거래처 연결 확인은 서버 함수가 담당한다.

## 4. 데이터 소유 경계

고객사 Firebase 프로젝트 하나에는 그 고객사의 데이터만 둔다. 모든 업무 문서에는 `companyId`를 넣되,
프로젝트 자체가 고객사 경계이므로 다른 고객사 데이터와 섞이지 않는다.

거래처 계정에는 서버에서 확정한 다음 식별자를 둔다.

- `companyId`: 어느 ERP 회사 소속인지
- `partnerId`: ERP의 어느 거래처인지
- `role`: `partner`, `employee`, `admin`

거래처가 브라우저 요청에 임의의 `partnerId`를 넣어도 믿지 않는다. 서버는 로그인 토큰의 Claim 또는 서버 계정 문서에서
`companyId`와 `partnerId`를 다시 읽는다.

## 5. 거래처 주문 흐름

1. 거래처가 로그인한다.
2. 주문 화면은 서버를 통해 그 거래처에 연결된 `partner_item`만 받는다.
3. 화면에는 허용된 품목·규격·현재 판매단가만 보인다.
4. 거래처가 품목과 수량을 제출한다.
5. 서버가 로그인 계정의 `partnerId`, 품목 연결, 판매 가능 여부, 단가를 다시 검증한다.
6. 서버가 주문번호를 발급하고 `orders` 문서를 저장한다.
7. ERP가 같은 DB의 주문을 구독해 신규 주문으로 표시한다.

브라우저가 보낸 품목명·거래처명·단가는 저장 근거로 쓰지 않는다. ID를 기준으로 서버가 현재 값을 다시 채운다.

## 6. 인증·권한표

| 역할 | 홈페이지 | 자기 주문 생성·조회 | 전체 주문 | 재고·원가·전표 | 사용자 관리 |
|---|---:|---:|---:|---:|---:|
| 비로그인 | O | X | X | X | X |
| 거래처 | O | O | X | X | X |
| 직원 | O | 회사 설정에 따름 | O | 권한에 따름 | X |
| 관리자 | O | O | O | O | O |

Firestore 규칙과 Functions가 같은 권한표를 사용해야 한다. 화면 메뉴를 숨기는 것만으로는 권한 통제가 되지 않는다.

## 7. 고객사 생성 절차

1. 고객사 코드와 표시 이름을 정한다.
2. 새 Firebase 프로젝트와 결제·지역을 설정한다.
3. Auth 로그인 방식을 켠다.
4. Firestore·Storage 규칙을 배포한다.
5. Functions Secret을 고객사 프로젝트에 등록한다.
6. 고객사 전용 환경변수로 관리자·직원·홈페이지를 빌드한다.
7. 초기 관리자 계정과 회사 설정 문서를 만든다.
8. Emulator 보안 시험과 새 프로젝트의 스모크 테스트를 통과시킨다.
9. 고객사 도메인을 연결하고 인수 확인을 받는다.

이 절차는 나중에 수동 체크리스트가 아니라 재실행 가능한 생성 스크립트로 만든다. 스크립트는 키 원문을 저장소에 남기지 않는다.

## 8. 환경변수와 배포 구성

- Firebase 공개 설정은 고객사별 배포 환경에서 주입한다.
- Gemini·스마트스토어 같은 비밀키는 Firebase Secret Manager에만 둔다.
- 고객사별 프로젝트 ID와 Hosting 대상은 배포 설정으로 분리한다.
- 코드의 기본 회사값으로 태백을 추정하는 호환 규칙은 판매판에서 사용하지 않는다.
- 배포 명령은 반드시 대상 고객사와 앱 종류를 화면에 출력한 뒤 실행한다.

태백 운영판 배포 명령과 판매판 배포 명령을 같은 이름으로 만들지 않는다. 잘못된 프로젝트에 배포하는 사고를 막아야 한다.

## 9. 백업·복구·탈퇴

- 고객사별 Firestore 정기 내보내기와 Storage 원본 백업을 둔다.
- 복구 시험을 분기마다 별도 시험 프로젝트에서 수행한다.
- 고객사 요청 시 데이터 전체 반출 형식과 전달 방법을 정한다.
- 계약 종료 시 로그인 차단 → 최종 백업 → 고객 확인 → 보존기간 경과 → 삭제 순서로 처리한다.
- 정정 스크립트는 태백 운영판과 같은 `--dry`, 백업, `--apply`, `--undo` 규칙을 사용한다.

## 10. Emulator 보안 시험

최소한 다음을 자동 시험한다.

- 비로그인 사용자가 주문·거래처·단가를 읽지 못한다.
- 거래처 A가 거래처 B의 품목·단가·주문을 읽거나 수정하지 못한다.
- 거래처가 연결되지 않은 품목을 주문하지 못한다.
- 거래처가 제출한 단가가 서버의 현재 단가와 다르면 서버 값으로 저장되거나 요청이 거절된다.
- 직원이 자기 회사 문서만 읽고 쓴다.
- `companyId` 없는 신규 업무 문서는 거절된다.
- 관리자 전용 데이터와 Secret이 브라우저 번들에 포함되지 않는다.

## 11. 공용 멀티테넌트 전환 조건

고객사별 프로젝트 운영 비용과 배포 시간이 실제 병목이 된 뒤에만 공용 DB를 검토한다. 전환 전에 다음이 모두 필요하다.

- 모든 업무 문서의 `tenantId` 필수화
- 모든 쿼리의 서버 측 tenant 조건 강제
- Firestore 규칙의 tenant 경계 시험
- Functions의 tenant 확인 공용 미들웨어
- 고객사별 감사 로그·사용량·백업·반출·삭제
- 한 고객사 장애·대량 작업이 다른 고객사에 미치는 영향 제한

이 조건이 갖춰지기 전에는 고객사별 Firebase 프로젝트 분리를 유지한다.

## 12. 구현 순서

1. 현재 코드의 태백 기본값·익명 인증·전체 컬렉션 조회를 목록화한다.
2. 로그인 주체와 권한표를 공용 모듈로 확정한다.
3. 거래처 주문 생성 서버 함수를 만든다.
4. 홈페이지 공개 영역과 거래처 로그인 영역을 같은 사이트 라우팅으로 합친다.
5. 고객사 생성·배포 절차를 스크립트화한다.
6. Emulator 보안 시험을 통과시킨다.
7. 시험 고객사 프로젝트로 데이터 없이 설치·삭제·복구를 연습한다.

## 완료 조건

- 태백 운영 프로젝트를 사용하지 않고 새 고객사 한 곳을 처음부터 생성할 수 있다.
- 거래처가 자기 연결 품목과 주문만 볼 수 있다.
- 주문이 서버 검증을 거쳐 ERP에 한 번만 생성된다.
- 고객사별 배포·백업·복구·탈퇴 절차가 재실행 가능하다.
- Emulator 보안 시험과 시험 고객사 스모크 테스트가 통과한다.

## 부록 A. 1차 코드 감사 결과

### 익명 인증

- `src/shared/firebase.ts`는 저장된 직원 세션만 복원하며 익명 로그인을 새로 만들지 않는다.
- `src/rules/firestore.rules.test.ts`에는 미로그인·익명 계정의 읽기·쓰기를 거절하는 시험이 있다.
- 현재 앱 코드에서 `signInAnonymously` 호출은 발견되지 않았다.

따라서 익명 인증 제거는 현재 코드 기준 완료된 것으로 보되, 판매판 보안 시험에 같은 거절 시험을 유지한다.

### 태백 기본값 후보

| 위치 | 현재 동작 | 판매판 조치 |
|---|---|---|
| `src/shared/types.ts`의 `companyOf` | `companyId`가 없으면 태백으로 본다 | 판매판에서는 누락 문서를 오류로 처리한다 |
| `functions/src/employeeLogin.ts` | 옛 직원 문서의 회사가 없으면 태백으로 본다 | 데이터 이관 후 fallback을 제거한다 |
| `functions/src/index.ts` 월말 재고 | 회사가 없는 품목을 태백으로 집계한다 | 고객사 신규 프로젝트에서는 누락을 경고·제외한다 |
| `components/FinancialReports.tsx` | `companyId` prop 기본값이 태백이다 | 필수 prop으로 바꾼다 |
| `AdminApp.tsx` 일부 옛 문서 필터 | 회사 없는 고정비 등을 현재 회사에 포함한다 | 이관 완료 뒤 누락 문서를 허용하지 않는다 |

이 fallback은 태백 운영판의 옛 데이터를 읽기 위한 호환 장치다. 운영판에서 곧바로 지우지 않고,
판매판 빌드에서는 금지하거나 신규 고객사 프로젝트 생성 검사에서 0건임을 확인한다.

### 전체 컬렉션 조회 후보

다음은 회사 조건 없이 컬렉션 전체를 읽는 경로가 확인됐다. 전역 설정인지 회사 업무 데이터인지 판정한 뒤 고친다.

| 위치 | 컬렉션 | 1차 판정 |
|---|---|---|
| `components/DashboardLinks.tsx` | `dashboardLinks` | 회사별 업무 설정 후보 |
| `components/FinancialReports.tsx` | `openingBalances` | 문서 ID로 다시 고르지만 전체 읽기라 수정 필요 |
| `components/HRManager.tsx` | `payrolls` | 회사별 급여라 수정 필요 |
| `components/QuotationManager.tsx` | `quotations` | 회사별 견적이라 수정 필요 |
| `components/ReturnManager.tsx` | `returnRequests` | 회사별 반품이라 수정 필요 |
| `components/ReceivingReturnsManager.tsx` | `returnRequests` | 위와 같은 경로, 공용화 후보 |
| `src/features/statements/hooks/useStatementHistoryFilters.ts` | `itemTaxonomy` | 회사별 분류 여부 확인 필요 |
| `src/features/admin/useVoucherLedger.ts` | `openingBalances` | 재무제표 경로와 함께 수정 필요 |
| `components/BenzopyreneLog.tsx` | `benzopyreneTests` | HACCP 자료의 회사 소유권 확정 필요 |
| `components/HaccpChecklist.tsx` | 여러 `haccp_*` | 회사별 자료라면 모든 쿼리에 회사 조건 필요 |

`src/shared/hooks/useAppData.ts`의 주요 주문·품목·거래처·전표 구독은 이미 회사 조건을 받는다.
위 후보는 한꺼번에 고치지 않는다. 각 컬렉션 문서에 `companyId`가 실제로 있는지 Emulator 시드와 타입을 먼저 확인하고,
보안 규칙 시험을 붙인 뒤 화면 쿼리를 바꾼다.
