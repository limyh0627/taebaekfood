# DB 변경 기록 (Firestore: taebaek-3abe4)

> 운영 Firestore에 직접 가한 변경을 시간순으로 기록합니다. **DB를 건드린 사람/에이전트는 반드시 여기에 추가하세요.**
> 코드 변경은 git 히스토리로 추적되므로 여기엔 **데이터(문서) 변경만** 적습니다.

---

## 2026-09-06 — 원가를 공급가액 기준으로 (매입 단가 ÷1.1)

**작업자:** Claude 에이전트 (사장님 지시: "아예 면세로만 원가 보는게 fm 아니야?",
"공급가액이랑 비교해서 마진 내는게 맞잖아")
**스크립트:** `scripts/fix-cost-supply-basis.mts` (`--apply`), 백업 `scripts/fix-cost-supply-basis-backup.json`

**부가세는 원가가 아니다.** 과세 매입에 낸 매입세액은 매출세액에서 공제받고, 면세 매입은
부가세를 애초에 안 낸다. 어느 쪽이든 원가 = 공급가액이다. 마진도 판매가의 공급가액과
견주므로 양쪽 기준이 같아야 한다.

그런데 매입 단가는 이 앱에서 **부가세 포함**으로 적히는데(`lineAmount`), 그 값을 그대로
`items.cost` 에 넣어 와서 과세 매입품 원가가 10% 부풀어 있었다.

**확인한 값** — 과세로 사 오는 종단 품목 60개 중 매입 전표와 맞춰볼 수 있는 26개가
**전부 전표 단가와 같았고**(=부가세 포함), 공급가액으로 넣은 것은 **0개**였다.

| 바뀐 것 | 값 |
|---|---|
| `items.cost` 60건 (과세로 사 오는 종단 품목) | ÷1.1 |
| 합계 | 135,116 → 122,833 |

**손대지 않은 것**
- 면세로 산 5건 (참깨×2 · 깻묵 · 들깨 · 탈피들깨가루) — 낸 부가세가 없다
- BOM·원료식이 있는 것(반제품 · 완제품 308건) — 롤업이라 원료값을 고치면 저절로 따라온다

**코드도 같이 고쳤다** — `bomCost`·`costCalc` 에서 「면세 원료 → 과세품이면 ×1.1」을 걷어냈다.
면세 농산물은 살 때 부가세를 안 내므로 못 빼는 매입세액이 없다. 2026-08-24에 넣은 규칙인데
근거가 틀렸다.

**⚠ 사고 하나** — 안전장치를 붙이려다 스크립트를 한 번 더 돌려 **원가가 10% 더 깎였다**
(122,833 → 111,666). `--undo` 로 되돌렸고 지금 값은 122,833 이 맞다. 두 번째 실행이 백업을
덮어써서 원래 값 백업은 ×1.1 로 되짚어 다시 만들었다(반올림 오차 1원). 이제 백업이 있으면
`--apply` 를 막는다.

**되돌리기:** `npx tsx scripts/fix-cost-supply-basis.mts --undo`

---

## 2026-09-06 — 선물세트에 남은 서류용 품목 지움

**작업자:** Claude 에이전트 (사장님 지시: "지워놔")
**스크립트:** `scripts/fix-clear-set-pumok.mts` (`--apply`), 백업 `scripts/fix-clear-set-pumok-backup.json`

선물세트는 이제 **든 완제품 각각으로 풀려** 서류(원료수불부·생산작업기록부·판매일지)에
올라간다. 그래서 세트 자신의 `품목`(서류용 이름)은 아무도 안 읽는다.

값이 남아 있으면 다음 사람이 "세트는 이 품목으로 잡히는구나" 하고 잘못 읽는다 -
실제로 그렇게 적혀 있어서 서류가 틀어져 있었다.

지운 것 4개 (`items.품목` 필드 삭제):

| 세트 | 지운 값 | 이제 서류에 잡히는 것 |
|---|---|---|
| 참기름+볶음참깨/스마트/300ml | 시골향참기름1 | 시골향참기름1 · 시골향볶음참깨 |
| 참+들/스마트/300ml | 시골향들기름2 | 시골향참기름1 · 시골향들기름2 |
| 참2+볶음참깨/스마트/300ml | 시골향참기름1 | 시골향참기름1(×2) · 시골향볶음참깨 |
| 참+들+볶음참깨/300ml | 시골향들기름2 | 시골향참기름1 · 시골향볶음참깨 · 시골향들기름2 |

**규격(`spec`)은 안 지웠다** - 그건 서류용이 아니라 전표 규격 칸·화면이 쓰는 값이다.

나머지 세트 3개(180ml 세트·1호·5호)는 원래 품목이 비어 있었다. 전에는 그래서
**줄이 통째로 서류에서 빠졌는데**, 이제는 든 것으로 풀려 제대로 잡힌다.

**되돌리기:** `npx tsx scripts/fix-clear-set-pumok.mts --undo`

---

## 2026-09-06 — 거래처-품목 연결의 옛 방식(`items.partnerIds`) 걷어냄

**작업자:** Claude 에이전트 (사장님 지시: "옛방식은 정리")
**스크립트:** `scripts/fix-drop-legacy-partnerids.mts` (`--apply`), 백업 `scripts/fix-drop-legacy-partnerids-backup.json`

거래처-품목 연결이 **두 벌**로 있었다 - 새 방식인 `partner_item` 컬렉션과,
품목 문서 안에 배열로 든 옛 방식 `items.partnerIds`. 두 벌이 어긋나도 화면엔
아무 표시가 안 났다.

**실제로 한 건이 어긋나 있었다.** 동우 볶음참깨는 `partner_item` 으론 10개입에
연결돼 있는데 옛 배열은 20개입을 가리켰다. 그래서 9/1 주문 5박스가
**20개입 재고에서 차감됐다** (같은 날 `fix-dongwoo-sesame-box.mts` 로 정정).

재 본 값: 연결 561개 중 554개는 두 벌 다 있었고, **7개만 옛 배열에만** 있었다.

- (1) 옛 배열에만 있던 연결 7개를 `partner_item` 에 만들었다
      - C033 <- 볶음참깨-낱개/1kg
      - C067 <- 시골향참기름/특골드/1750ml (2건)
      - C040, C047 <- 시골향참기름/특/1750ml (12개입)
      - c-1773584858565 <- 시골향탈피들깨가루/1kg
      - C060 <- 시골향들깨가루(고운)/4kg
- (2) 품목 96개에서 `partnerIds` 를 비웠다. **`'SMARTSTORE'` 표식은 남겼다** -
      그건 거래처 연결이 아니라 판매 채널 표시라서 뜻이 다르다.

코드는 먼저 옮겼다 - 화면 8곳(`AddOrderModal`, `PasteOrderModal`, `ItemList`,
`ItemManager`, `AddItemModal`, `OrdersList`, `PartnerPortal`,
`ReceivingReturnsManager`)이 이제 `shared/partnerPrice` 의
`partnersOfItem` / `isLinkedToPartner` 로 `partner_item` 만 본다.
되돌아가지 않게 `src/shared/partnerLink.test.ts` 가 소스를 훑는다.

**되돌리기:** `npx tsx scripts/fix-drop-legacy-partnerids.mts --undo`

---

## 2026-09-04 — 박스 품목 3건의 서브타입을 '박스' 로

**작업자:** Claude 에이전트 (사장님 지시)

단위가 `박스` 인데 서브타입만 `낱개` 로 찍혀 있던 품목.

| 품목 | 규격 | 전 | 후 |
|---|---|---|---|
| 시골향참기름/특/1750ml (12개입) | 1750ml | 낱개 | **박스** |
| 시골향참기름/A/흰정사각/1750ml | 1750ml * 10 | 낱개 | **박스** |
| 시골향참기름/특A/흰정사각/1750ml | 1750ml * 10 | 낱개 | **박스** |

**동작에는 영향이 없다** — 개입수는 BOM 에서 오고, 박스 판정은 `isBox`·`unit` 이 한다.
`subtype === '낱개'` 를 보고 갈라지는 코드가 없는 것을 먼저 확인했다.
**화면에서 갈래로 거를 때만** 빠졌다(제품별원장 분류 필터에서 박스로 걸러도 안 나왔다).

할일에는 2건으로 적혀 있었는데 세어 보니 3건이었다.

**되돌리기:** `npx tsx scripts/fix-box-subtype.mts --undo`
(백업: `scripts/fix-box-subtype-backup.json`)

---

## 2026-09-03 — 이총제 팀장 · 황준호 대리 관리자 앱 접근 허용

**작업자:** Claude 에이전트 (사장님 지시: "이총제 황준호도 관리자 기능 access해줘")

| 문서 | 전 | 후 |
|---|---|---|
| 이총제 팀장 (`e3`) | `adminAccess` 없음 | `adminAccess: true` |
| 황준호 대리 (`e6`) | `adminAccess` 없음 | `adminAccess: true` |

**되돌리기:** `npx tsx scripts/fix-admin-access-add.mts --undo`
(백업: `scripts/fix-admin-access-add-backup.json`)

이번엔 사람마다 스크립트를 짜지 않았다 — [fix-admin-access-add.mts](scripts/fix-admin-access-add.mts)
맨 위 `IDS` 목록만 갈아 끼우면 다음 사람에게도 쓴다.
(이은경 상무 때 만든 `fix-admin-access-eunkyung.mts` 는 그 건의 되돌리기용으로만 남긴다.)

**지금 관리자 앱에 들어갈 수 있는 사람** — 태백식품(사장님) · 이은경 상무 · 이총제 팀장 · 황준호 대리.

---

## 2026-09-03 — 이은경 상무 관리자 앱 접근 허용

**작업자:** Claude 에이전트 (사장님 지시: "이은경 상무 계정도 관리자 앱 들어갈 수 있게 해줘")

`employees/emp-1773373867440` (이은경) 에 **`adminAccess: true`** 를 넣었다. 한 칸이다.

**왜 데이터인가** — 전에는 [apps/admin/main.tsx](apps/admin/main.tsx) 에 `id !== 'admin'`
이 박혀 있어서 사장님 계정 말고는 아무도 못 들어왔다. 사람 이름을 코드에 박으면 사람이
바뀔 때마다 배포해야 한다. 그래서 **직원 기록에 칸을 두고** 코드는
[adminAccess.ts](src/shared/adminAccess.ts) `canEnterAdmin` 하나만 본다.

| 문서 | 전 | 후 |
|---|---|---|
| 이은경 (`emp-1773373867440`) | `adminAccess` 없음 | `adminAccess: true` |

**되돌리기:** `npx tsx scripts/fix-admin-access-eunkyung.mts --undo`
(백업: `scripts/fix-admin-access-eunkyung-backup.json`)

**앞으로 다른 사람에게 줄 때** — 스크립트를 또 짤 일이 아니다. 직원 문서에
`adminAccess: true` 만 넣으면 된다. 로그인할 때 박아 둔 사본이 낡아도
`freshAccess` 가 지금 목록으로 덮어쓰므로 앱을 다시 깔 필요는 없다.

---

## 2026-09-01 — 251/253 분리 · 풍회 깻묵 · 상차비 계정

**작업자:** Claude 에이전트 (사장님 지시)

### ① 251 외상매입금을 상거래만으로 (코드 변경 — 데이터는 안 건드림)

**사유:** "부가세예수금 법인세등은 매입이 아닌데 전표가 매입으로 끊기니까",
"계정 가르면 되는거 아니야?"

251 줄은 저장된 게 아니라 분개할 때 계산된다 — `autoJournal.payableCodeFor` 하나를
고치니 과거 전표까지 같이 갈렸다.

| | 전 | 후 |
|---|---|---|
| 태백 251 | −145,965,745 | **−114,467,234** |
| 태백 253 | 440,000 | **−31,058,511** |
| 풍회 251 | −3,793,600 | **−88,000** |
| 풍회 253 | 0 | **−3,705,600** |

거래처 잔액은 251·253을 **한 덩어리**로 본다(cashLedger). 전후 대조 결과
**잔액이 달라진 거래처 0곳** — 69곳 전부 그대로다. 재무상태표에서만 갈린다.

### ② 상차비 → 원료매입

**스크립트:** `scripts/fix-loading-fee-to-material.mts` (`--undo`)
980 잡손실 32,175 × 2건(8/25 풍회유통·한국농수산물유통공사) → 500 원료매입.
참깨를 들여오는 취득부대비용이라 원료 원가에 얹히는 게 맞다.

### ③ 풍회 깻묵 신설 + 8월말 스냅샷

**사유:** "풍회에 깻묵 300원잡고 8월말 재고스냅샷에 8톤 넣어놔"
**스크립트:** `scripts/fix-punghoe-kkaetmuk.mts` (`--undo`)

품목 `raw-깻묵-punghoe` — 벌크 · raw · kg · 원가 300 · 재고 8,000kg.
태백 깻묵(단위 '개', 원가 380)은 단위가 달라 복사하지 않고 새로 만들었다.

풍회 2026-08 스냅샷 3,267,000 → **5,667,000** (깻묵 2,400,000 얹음).

### ④ 7월말 스냅샷에도 깻묵

**사유:** "태백 깻묵은 7월 31일에 1.5톤넣어놔봐 풍회는 7월 31일에 6톤"
**스크립트:** `scripts/fix-kkaetmuk-july.mts` (`--undo`)

| 회사 | 수량 | 원가 | 금액 | 스냅샷 총액 |
|---|---|---|---|---|
| 태백 `inv-snap-2026-07` | 1,500kg | 380 | 570,000 | 161,220,237 → **161,790,237** |
| 풍회 `inv-snap-punghoe-2026-07` | 6,000kg | 300 | 1,800,000 | 9,939,600 → **11,739,600** |

원가는 품목이 들고 있는 값을 썼다 — **태백 380, 풍회 300으로 다르다.**
사장님이 300은 풍회 것으로 말씀하셔서 태백엔 안 밀어 넣었다.

태백 깻묵은 단위가 '개'로 잘못 박혀 있어 kg으로 고쳤다 — 로트는 이미 kg으로 돌고 있었다
(kgIn 3 / kgRemaining 3). 수량·원가는 안 건드렸다.

**⚠ 7월말 기말재고는 8월 기초재고다** — 손익분석 매출원가가 그만큼 움직인다.

### ⑤ 태백 깻묵 3톤 · 8월말 스냅샷 갱신

**사유:** "태백엔 지금 3톤", "9월달 스냅샷에 깻묵 3톤 넣어서 그걸 8월말 재고 스냅샷에
덮어버려", "현재 깻묵 재고도 3톤으로 바꿔두고"

**스크립트:** `scripts/fix-kkaetmuk-sep.mts` · `scripts/fix-kkaetmuk-3ton.mts` (둘 다 `--undo`)

| | 전 | 후 |
|---|---|---|
| `items.stock` (p-1782788799105) | 3kg | **3,000kg** |
| 로트 합계 | 3kg | **3,000kg** (기존 로트에 +2,997) |
| 원장 잔량 | 3kg | **3,000kg** (앵커 `rm-anchor-깻묵-20260901`) |
| `inv-snap-2026-09` 깻묵 | 3kg 1,140 | **3,000kg 1,140,000** |
| `inv-snap-2026-08` | 218,137,184 · 223품목 | **9월 것으로 덮음** — 219,518,832 · 227품목 |

9월 1일에 찍은 스냅샷이 사실상 8월말 재고라 그대로 덮었다.
재고·로트·원장이 셋 다 3,000kg으로 맞는 걸 되읽어 확인했다(차이 0).

---

---

## 2026-09-01 — 풍회 카드 세금납부를 전액 부가세로

**작업자:** Claude 에이전트 (사장님 확인)
**사유:** "풍회꺼 카드대금은 전부 부가세래".

어제 태백 것에 맞춰 부가세·법인세 반반으로 갈랐는데, 풍회는 전액 부가세였다.

**스크립트:** `scripts/fix-punghoe-card-vat.mts` (`--undo`)

| 전표 | 전 | 후 |
|---|---|---|
| 풍회 `260824-01` 8/24 3,110,000 | 255 부가세 1,555,000 + 998 법인세등 1,555,000 | **255 부가세예수금 3,110,000** |

분개 (차)255 3,110,000 / (대)251 외상매입금 3,110,000.

**결과:** 풍회 255 부가세예수금이 −2,500,850 → **−945,850**.
998 법인세등은 이제 쓰는 줄이 0이다 — 계정은 남겨 뒀다(법인이라 언젠가 쓴다).

**⚠ 아직 안 고친 것:** 이 전표는 갈래가 '매입'이다. 표준으로 매입전표는 상거래 외상매입
전용이고, 상대계정도 251이 아니라 253 미지급금이 맞다. 지금 251에 상거래 아닌 채무가
6,400만원 섞여 있다 — 별건으로 정리해야 한다(사장님과 방향 논의 중).

---

## 2026-09-01 — 풍회 급여를 발생주의로

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "기존 급여 전표 280만원이랑 350만원을 미지급급여 출금으로 바꾸고 말일에 급여
두건 발생을 새로 만들어줘", "7월 31일에 기초 미지급급여 잔액을 630만원 세워두고".

**스크립트:** `scripts/fix-punghoe-payroll-0831.mts` (`--undo`)

전에는 나갈 때 바로 비용이었다 — (차)515 급여 / (대)통장. 태백과 같은 방식으로 바꿨다.

| 날짜 | 무엇 | 분개 |
|---|---|---|
| 2026-07-31 | 기초 `기초260731-03` | (차)375 이월이익잉여금 6,300,000 / (대)263 미지급급여 |
| 2026-08-13 | 우용 2,800,000 (계정만 515→263) | (차)263 / (대)103 |
| 2026-08-18 | 3,500,000 (계정만 515→263) | (차)263 / (대)103 |
| 2026-08-31 | 발생 `급여260831-01` 우용 2,800,000 | (차)515 / (대)263 |
| 2026-08-31 | 발생 `급여260831-02` 3,500,000 | (차)515 / (대)263 |

**263 미지급급여가 도는 고리** (되읽어 확인):

```
7/31 기초        + 6,300,000
8/13·8/18 지급   − 6,300,000   ← 7월분을 갚은 것
8/31 발생        + 6,300,000   ← 8월분, 9월에 나간다
8월말 잔액       = 6,300,000
```

515 급여는 8/31에 6,300,000. **8월에 나간 돈은 7월 인건비라 8월 손익에 안 잡히고,
8월 인건비는 8/31에 온전히 선다.**

**⚠ 원천공제는 0으로 뒀다.** 뗀 게 있으면 총급여가 더 크고 그 몫이 254 예수금으로
서야 한다. 공제액을 알려주시면 다시 세운다.
(8/18 건은 적요에 이름이 없어 '(이름 미기재)'로 뒀다.)

---

## 2026-09-01 — 전표 템플릿을 회사별로 · 풍회 인사관리 개방

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "템플릿도 회사별로 따로 쓰게", "풍회는 왜 인사관리가 없지".

**스크립트:** `scripts/fix-template-company.mts` (`--undo`)

`FixedCostTemplate.companyId` 신설. **비우면 모든 회사에서 보인다**(공용 뼈대).
거래처·금액이 박힌 건 그 회사 것이라 회사를 단다 — 안 달면 풍회 화면에 태백 거래처가
물린 템플릿이 뜨고, 그걸로 끊은 전표가 엉뚱한 거래처에 붙는다.

| | 개수 | 무엇 |
|---|---|---|
| taebaek 달림 | 12 | 임대료·리스료·이자(수협)·적금(수협)·차할부금·세스코·4대보험·전기세·카드론상환 등 |
| 공용(회사 없음) | 28 | 카드대금·수금·지불·인출금·운임·할부금·부가세 신고 등 계정만 붙은 뼈대 |

판정: 거래처가 붙었거나 · 금액이 박혔거나 · 자동발행을 켰거나 (`principal`/`insCorp`/`gross` 포함).

새로 만드는 템플릿은 지금 회사를 달고 나간다.

---

## 2026-09-01 — 풍회 8월말 재고 마감

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "풍회 8월말 재고에 깨분 0으로 바꾸고 깨분참기름 30캔해서 재고 마감해줘",
"8월 말로 스냅샷 남겨놔 태백 풍회 분리하고".

**스크립트:** `scripts/fix-punghoe-close-0831.mts` (`--undo`)

| 품목 | 전 | 후 | 원가 | 평가 |
|---|---|---|---|---|
| 깨분 | 2,000 kg | **0** | 2,970 | 0 |
| 깨분참기름/16.5kg | 0 | **30 캔** | 108,900 | 3,267,000 |
| 참깨 | 0 | 0 | 4,090 | 0 |

깨분 재고를 0으로 두면서 **로트도 같이 비웠다**(kgRemaining 0 · depleted).
재고만 0으로 두고 로트를 남기면 다음 사용에서 로트만 깎여 재고가 음수로 간다 —
원장·로트가 갈리는 그 병이다.

### 스냅샷 — 회사별로 갈려 있다

| id | 회사 | 전 | 후 |
|---|---|---|---|
| `inv-snap-punghoe-2026-08` | 풍회 | 5,940,000 · 2품목 | **3,267,000 · 3품목** |
| `inv-snap-2026-08` | 태백 | 218,137,184 · 223품목 | **안 건드림** |

id를 `invSnapDocId(회사, 월)`이 갈라 준다 — 태백은 `inv-snap-{월}`, 그 밖은
`inv-snap-{회사}-{월}`. 두 회사 재고가 섞이면 태백 재고 증감이 풍회 손익에
500 원료매입으로 잡힌다.

---

## 2026-09-01 — 풍회 장부를 태백 장부에 맞춤

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "태백에서 풍회에 매입 지불 한게 풍회에서 태백으로 매출 수금 한거랑 금액이
똑같이 맞춰졌는지 확인해봐" → 안 맞았다. → "풍회쪽을 태백에 맞춰, 다른건 신경 쓰지말고."

**왜 벌어졌나:** 회사이체로 만든 건만 상대 장부에 거울이 서고, 그냥 지불로 처리한 건은
한쪽에만 섰다. 게다가 거울 셋은 태백 원본이 나중에 지워져 풍회에만 남았다.

**스크립트:** `scripts/fix-punghoe-mirror-taebaek.mts` (`--undo`)

| | 전 | 후 |
|---|---|---|
| 매입(태백) vs 매출(풍회) | 66,189,600 vs 57,954,600 (차이 8,235,000) | **둘 다 66,189,600** |
| 지불(태백) vs 수금(풍회) | 61,030,000 vs 55,632,719 (차이 5,397,281) | **둘 다 61,030,000** |
| 태백이 줄 돈 / 풍회가 받을 돈 | 5,159,600 / 2,321,881 | **둘 다 5,159,600** |

### 만든 것 5건 (풍회 장부)

- 매출전표 `260828-01` 2026-08-28 8,235,000 — 태백 `260828-08` 매입의 짝.
  품목·금액·과세는 그대로 두고 계정만 500 원료매입 → 800 일반매출로 바꿨다.
  분개 (차)108 8,235,000 / (대)800 7,486,364 + (대)255 748,636
- 수금 4건 — 8/04 6,200,000 · 8/11 3,200,000 · 8/24 3,110,000 · 8/31 500,000 (합 13,010,000)

### 지운 것 3건 (풍회 장부)

태백에 원본이 없는 거울 수금 — 8/07 284,169(풍회중진공이자) · 8/11 2,800,000(풍회비용 03) ·
8/18 4,528,550. 합 7,612,719.

**⚠ 남은 문제:** 지운 셋 중 둘은 적요가 '이자'·'비용'이다. 태백이 대신 낸 돈일 수 있는데
그렇다면 매출채권이 아니라 다른 계정이 맞다. 사장님이 "다른건 신경 쓰지말고" 하셔서
태백 기준으로만 맞췄다. 성격을 아시면 그때 다시 세워야 한다.

**아직 안 막은 것:** 앞으로도 풍회 거래처로 그냥 지불하면 거울이 안 선다. 코드에서
강제하는 건 별도 작업.

---

## 2026-09-01 — 로트를 원장잔량에 맞춤 (3원료)

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "이 세개 원장잔량에 로트 맞춰. **원료수불부 아니고 원장잔량** 말하는거다,
원료수불부용 원장은 냅두고."

셋 다 **로트가 원장보다 덜 남아 있었다** — 로트만 깎이고 원장은 안 깎인 게 아니라,
원장에 잡힌 입고가 로트로 안 들어간 쪽이다.

**스크립트:** `scripts/fix-lot-to-ledger.mts <원료명>` (`--undo`, 원료별 백업)

| 원료 | 원장 | 로트(전) | 로트(후) | 채운 양 |
|---|---|---|---|---|
| 탈피들깨가루 | 1,772.600 | 1,764.600 | 1,772.600 | +8.000 |
| 통깨참기름 | 3,890.008 | 3,855.133 | 3,890.008 | +34.875 |
| 통들깨들기름 | 176.955 | 158.764 | 176.955 | +18.191 |

앱의 실사와 같은 방식이다 — '원장맞춤' 로트를 얹고, 원장에 `targetKg` 앵커 줄을
남겨 이후 잔량이 그 값부터 다시 세어지게 한다(표류가 거기서 끊긴다).
`items.stock`도 로트 합계로 따라 맞췄다.

**건드리지 않은 것:** `rawDocEntries`(원료수불부). 이건 규제 서류라 실제 원장과
맞추지 않는다 — 인수인계.md의 그 규칙 그대로다. 쓴 컬렉션은 `rawMaterialLedger`와
`items` 둘뿐이다.

---

## 2026-09-01 — 적금·카드세금 전표 바로잡기

**작업자:** Claude 에이전트 (사장님 지시)

### ① 적금(수협) — 매입전표 → 자금 출금

**사유:** 사장님 지적 — "외상매입금은 재고자산을 외상으로 샀을 때 서는 부채다.
정기적금 납입과는 전혀 관계가 없다."

제 잘못이다. 옆에 있던 이자(수협) 템플릿을 그대로 베껴 `dir: '대체'` + 거래처로 만들었더니
매입전표가 됐고, 수협은행에 갚을 외상이 생긴 것처럼 251이 섰다.
적금은 통장에서 그날 바로 빠진다 — 자금 출금이다.

**스크립트:** `scripts/fix-savings-to-cash.mts` (`--undo`)

| | 전 | 후 |
|---|---|---|
| 템플릿 `fct-savings-suhyup` | 대체 | **출금** (taxExempt 제거 — 자금전표엔 부가세 갈래가 없다) |
| 8월분 | 매입전표 `260812-09` (차)106 / (대)251 | 자금 출금 (차)106 / (대)103 |

거래처(수협은행)는 남겼다 — 106 줄은 채권·채무가 아니라 잔액을 안 흔든다.

**아직 같은 병:** `fct-1787283396231` 이자(수협)도 `dir: '대체'` + 거래처라 9/19에
매입전표로 선다. 실제 8/19 이자는 자금 출금으로 끊으셨다 — 사장님 확인 후 맞출 것.

### ② 카드로 낸 세금 — 650 카드대금 → 성격별로 반반

**사유:** "카드세금납부 절반은 부가세 납부로 절반은 소득세, 풍회는 법인세로."
카드대금은 **결제 수단**이지 성격이 아니다.

**스크립트:** `scripts/fix-split-card-tax.mts` (`--undo`)

계정 신설: **998 법인세등** (비용 · `ag-other-expense`)

| 전표 | 금액 | 후 |
|---|---|---|
| 태백 `260820-13` 8/20 | 3,680,000 | 255 부가세예수금 1,840,000 · 338 인출금 1,840,000 |
| 태백 `260831-06` 8/31 | 160,580 | 255 80,290 · 338 80,290 |
| 풍회 `260824-01` 8/24 | 3,110,000 | 255 1,555,000 · 998 법인세등 1,555,000 |

부가세 255 · 소득세 338은 앱의 '세금납부' 템플릿(mode `세금`)이 쓰는 계정과 같다.
풍회 건은 **과세로 잡혀 있어** 부가세대급금 282,727이 붙어 있었다 — 세금 납부에 매입세액이
붙을 리 없어 면세로 바꿨다. 8/25 `260825-12` 지방세 677,214는 성격이 달라 안 건드렸다.

---

## 2026-09-01 — 적금 계정과 전표 템플릿 추가

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "적금 같은 부금은 계정과목 뭘로 끊어야하냐" → 사업자 명의 적금.
"12일 수협은행에 30만원으로 전표 발행해주고 아예 템플릿으로 하나 박아줘",
"할부금 기본 템플릿으로 만들고".

적금 납입은 **비용이 아니라 자산**이다. 통장에서 적금으로 자리만 옮기는 것이라
비용으로 끊으면 이익이 그만큼 깎여 보인다.

**스크립트:** `scripts/fix-add-savings-accounts.mts` · `scripts/fix-add-voucher-templates.mts` (둘 다 `--undo`)

### ① 계정과목 2개 — `accountCodes`

| 코드 | 이름 | 갈래 | isCash | 쓰는 곳 |
|---|---|---|---|---|
| 106 | 정기적금 | 자산 | false | 적금 납입·만기 |
| 136 | 선납세금 | 자산 | false | 이자 원천징수(15.4%) |

`isCash: false` — 적금은 현금성이 아니다. true로 두면 현금흐름표가 적금을 현금으로 세고
보유자금에도 잡혀 "쓸 수 있는 돈"이 부풀어 보인다.

### ② 전표 템플릿 2개 — `fixedCostTemplates`

| id | 이름 | 갈래 | 계정 | 금액 | 발행일 |
|---|---|---|---|---|---|
| `fct-builtin-installment` | 할부금 (기본) | 출금 | 253 미지급금 | — | — |
| `fct-savings-suhyup` | 적금 (수협) | 대체 | 106 정기적금 | 300,000 | 12일 |

적금은 `taxExempt: true` — 안 그러면 300,000이 272,727 + 부가세 27,273으로 갈린다.
할부금은 `CASH_TEMPLATES`(코드 시드)에도 같이 넣었다. 화면이 읽는 원천은 DB다.

### ③ 차할부금 템플릿 + 8월 적금 전표

**스크립트:** `scripts/fix-car-installment.mts` (`--undo`)

사장님 확인: "그냥 차 할부로 산거야". 할부는 상환이지 비용이 아니다.

| id | 이름 | 갈래 | 발행일 | 합계 | 줄 |
|---|---|---|---|---|---|
| `fct-car-installment` | 차할부금 | 출금(상환) | 10일 | 470,280 | 253 원금 440,000 · 951 이자 30,280 |

전표 2건 (8월분만 — 9월분은 템플릿이 그날 낸다):

| 날짜 | 갈래 | 금액 | 분개 |
|---|---|---|---|
| 2026-08-12 | 자금 출금 `AUTO-fct-savings-suhyup-2026-08` | 300,000 | (차)106 정기적금 / (대)103 보통예금 |
| 2026-08-10 | 자금 출금 `AUTO-fct-car-installment-2026-08` | 470,280 | (차)253 440,000 · (차)951 30,280 / (대)103 |

차할부금 8월분은 `scripts/fix-issue-car-0810.mts` (`--undo`). 손으로 만들지 않고
템플릿과 같은 `buildCashVoucher`를 태웠다 — 8월분과 9월분이 어긋나지 않게.

**⚠ 확인이 남은 것**

- 원금 440,000 / 이자 30,280 은 제가 예시로 든 갈래다. 실제 상환표가 다르면
  템플릿에서 원금·이자만 고치면 된다(합계는 따라 바뀐다).
- 차량(208)도 할부 부채(253)도 장부에 없다. 취득 전표를 안 세운 채 갚기만 하면
  253이 음수로 파고든다. 취득가·할부 잔액이 정해지면 그때 한 번 세워야 한다.
- 같은 병이 293 장기차입금에 이미 있다 — 8/12 중진공 원리금 상환 2,770,000이
  차변으로만 들어가 있고 빌린 기록(대변)이 없다.

---

## 2026-08-30 — 규격(spec)을 낱개 기준으로 통일

**작업자:** Claude 에이전트 (사장님 지시)
**사유:** "고춧가루 규격을 낱개로 바꿔놓고 … 벌크만 규격을 벌크로 넣어놔",
"개입수 없어도 된다고 낱개라", "지금 향미유처럼 처리해".

`spec`은 **낱개 하나의 용량**이다. 거기 개입수('* 20')가 박혀 있으면 코드가 그걸 박스로 읽어
(`parseSpecCount`) 수량 환산이 어긋난다. 개입수는 박스 품목이 제 규격으로 쥔다.

**스크립트:** `scripts/fix-spec-loose-bulk.mts` · `scripts/fix-boxsize-restore.mts` (둘 다 `--undo`)

### ① 낱개인데 규격에 개입수가 박힌 것 — 7건

| 품목 | 전 | 후 |
|---|---|---|
| 고춧가루 1kg | `1kg * 20` | `1kg` |
| 고춧가루 5kg | `5kg * 4` | `5kg` |
| 참기름/병/분·원액/엘생명/180ml | `180ml * 40` | `180ml` |
| 시골향참기름/원액·A·특A/1750ml | `1750ml * 10` | `1750ml` |

박스·선물세트는 그 개입수가 제 규격이라 안 건드렸다.

### ② 벌크 22건 — 규격을 `벌크`로

참깨·깨분·검정참깨·들깨·볶음참깨·생들기름·수입들기름·통깨참기름 … 자루째 kg/L로 세는 것들.
빈칸으로 두면 규격을 안 적은 것인지 없는 것인지 안 갈린다.

### ③ 고춧가루 `boxSize` 되살림 (2건)

"낱개로 관리하고 몇박스인지 볼 수 있게만" — 개입수를 알아야 박스 수를 셀 수 있다.
짝 박스 품목이 있으면 그쪽이 쥐지만(boxSiblings) 고춧가루는 짝이 없어 `boxSize`가 유일한 근거다.
규격은 낱개 그대로 두고 `boxSize`만 되살렸다: 고춧가루 1kg → 20, 5kg → 4.
(재고 38개 = 1박스 18개 · 152개 = 38박스)

---

## 2026-08-29 — 풍회유통 장부 분리 (거래처·품목을 회사별 문서로)

**작업자:** Claude 에이전트 (사장님 지시)
**스크립트:** `scripts/fix-punghoe-setup.mts` (dry/`--apply`/`--undo`)

**원칙:** 거래처·품목은 **회사마다 문서를 따로 둔다.** 한 문서를 공유하면 잔액·재고가
어느 회사 것인지 흐려진다 — 장부는 회사마다 따로다. 태백 문서는 하나도 안 건드렸다.

### ① 거래처 — 풍회 사본 10개 신설 (id = `<원본>-punghoe`)

카프코 · 한국농수산물유통공사 · 따나카코리아 · 품질검사(풍회) · 보험료 · 카드대금 ·
소상공 · 중진공 · 농협은행 · 한전

풍회 전표 7건·자금 8건의 `partnerId`를 사본으로 옮겼다. 태백 전표·자금은 원본을 그대로 가리킨다
(카프코·한국농수산물유통공사·보험료·카드대금·소상공·중진공은 **양쪽이 같이 쓰는 곳**이라
한 문서에 회사를 하나만 달면 태백에서 사라졌을 것이다).

### ② 태백푸드 → 태백식품, 풍회로 이관

`c-taebaek-food` — 태백 쪽 전표·자금·주문이 **0건**(풍회의 매출처일 뿐)이라 사본을 안 만들고
이름을 바꿔 `companyId: 'punghoe'`로 넘겼다. 전표·자금 18건의 `partnerName`도 함께 고쳤다.

### ③ 품목 — 풍회는 깨분·깨분참기름·참깨 셋

| 품목 | 상태 |
|---|---|
| 깨분 `p-1782788554239` | 이미 풍회 (원가 2,970/kg, 재고 2,000kg) |
| 깨분참기름/16.5kg `wip-깨분참기름-캔-punghoe` | 이미 풍회 — **원가 싼 쪽**(108,900). 태백 것은 `p-1779251603644` 135,000으로 그대로 |
| 참깨 `p-1779251176421-punghoe` | **신설** — 태백 문서를 그대로 뜨고 재고 0, 로트·거래처연결은 안 가져옴 |

태백 참깨 3,510kg은 안 건드렸다 — 옮기면 태백 재고가 통째로 빈다.

### ④ 비고에 남은 옛 이름 (같은 날 뒷정리)

`partnerName`만 바꾸고 비고(note)를 안 고쳐서, 화면에 **'태백식품 … 태백푸드 수금'**으로
두 이름이 같이 떴다. 풍회 자금기록 9건의 비고에서 태백푸드 → 태백식품.
자동 생성된 문구라 글자만 바뀌고 뜻은 그대로다. 풍회 밖에는 남은 게 0건이었다.

**스크립트:** `scripts/fix-note-taebaek-food.mts` (dry/`--apply`/`--undo`)

### 검증

- 풍회 전표·자금이 가리키는 거래처가 전부 풍회 문서 ✅
- 태백 원본 10개 그대로, 태백 참깨 3,510kg 그대로 ✅

---

## 2026-08-28 — 미발행 목록 정리: 더미 전표로 숨김 + 태영상회 주문 삭제

**작업자:** Claude 에이전트 (사장님 확정)

### ① 숨길 주문 10건 — 더미 전표 한 장에 물림

**사장님 지시:** "invoicePrinted 안쓴다며, 전표 매칭 있는지만 판단기준으로 쓰고
내가 숨기라고 한 애들은 그냥 더미로 가짜 전표 id만 물려놓으라는 얘긴데."

판정 기준은 오직 **그 주문을 가리키는 전표가 있느냐** 하나다(`isVouchered`). 그래서 주문에
새 플래그를 다는 대신, 더미 전표 한 장의 `orderId`에 콤마로 묶었다
(`voucherOrderIds`가 콤마로 갈라 담는다). 코드 변경 없음.

`issuedStatements/stmt-hide-20260828` — docNo `정리-20260828`,
partnerName `(미발행 정리 — 전표 안 끊음)`, **items `[]` · 금액 전부 0 · partnerId 빈값**

| 주문 | 거래처 |
|---|---|
| ORD-1785994142963 | 일성상회 납품 08-10 |
| ORD-1786001299618 | 대성농산 납품 08-10 |
| ORD-1786327189070 | 에덴식당재료마트 납품 08-13 |
| ORD-1786062348224 | 해피유통(쿠팡) 납품 08-14 |
| ORD-1786507262755 | 모란식품 납품 08-17 |
| ORD-1786510380611 | 대왕푸드 납품 08-17 |
| ORD-1786674416669 | 으뜸건어물 납품 08-17 |
| ORD-1787018811386 | 대왕푸드 납품 08-21 |
| ORD-1787192433189 | 해피유통(포천) 납품 08-24 |
| ORD-1787729963250 | 유원식품(시흥) 배송 08-26 |

**회계 무영향 확인:**
- `journalizeStatement` → **null** (items가 비면 분개를 안 만든다)
- 공급 0 · 세액 0 · 총액 0 → 매출 집계 불변(매출전표 총액 346,124,630원 그대로)
- `partnerId` 빈값 → 어느 거래처 잔액에도 안 붙는다 · 세금계산서 발행표시 없음

**스크립트:** `scripts/fix-dummy-stmt-hide-orders.mts` (dry/`--apply`/`--undo`)
되돌리기는 이 문서 하나만 지우면 된다.

### ② 태영상회 08-27 주문 삭제 (잘못 들어간 주문)

`ORD-1787632838910` (시골향참기름/분/1800ml 3박스). **재고는 되돌리지 않았다**(사장님 지시:
"되돌리지마 그냥 삭제해") — 실물은 이미 나갔다. 엔진으로 되돌렸다가 다시 물렸다:
2호박스 438 · 시골향참기름/분/1800ml 낱개 203으로 원위치.

**스크립트:** `scripts/fix-delete-order-taeyoung.mts`(주문 백업 있음, `--undo`로 복원 가능) +
`scripts/fix-taeyoung-keep-stock.mts`(복원된 재고를 다시 물림)

### ③ 청정식품 08-03 연결 (4차)

`260803-11` → `ORD-1785830866346`. 셋째 줄이 주문 `특A/1800ml ×36` vs 전표 `A/1750ml ×30`으로
달랐는데 사장님 확인상 **전표가 맞다**. `fix-relink-statements.mts`에 추가.

### 결과

미발행 목록 **39건 → 7건**. 남은 7건은 전표가 실제로 없고 대부분 아직 진행 중이다:
진보반찬(대기)·일성상회(대기)·해피유통(포천)(작업중)·해피유통(쿠팡)(출고)·
현대유통(원주)·대왕푸드·지구식품(대기).

---

## 2026-08-28 — 전표–주문 연결 9건 복구 (미발행 목록 정리)

**작업자:** Claude 에이전트 (사용자 확정)
**사유:** "예전 주문이여도 전표 연결 안된애들은 미발행에 뜨게해" → 목록을 고쳤더니 39건이 떴고,
"아까처럼 매출전표로 매칭해보고 안된것만 정확하게 남겨".
**스크립트:** `scripts/fix-relink-statements.mts` (dry/`--apply`/`--undo`)

전표는 있는데 `orderId`가 비었거나 **지워진 주문**을 가리켜 그 주문이 미발행으로 남아 있었다.
주문 삭제가 `deleteItem('orders')` 한 줄이라 전표의 orderId는 죽은 id를 문 채 남는다.

| 전표 | 전 | 후 | 근거 |
|---|---|---|---|
| 260818-02 | (빈값) | ORD-1787032928186 | 일성상회 08-18 3품목 수량 일치 |
| 260812-02 | ORD-1786492847819(없음) | ORD-1786507369829 | 세화식품 중간4kg ×1 |
| 260805-01 | ORD-1785727415906(없음) | ORD-1785802585888 | 글로벌유통 원액300ml ×100 |
| 260804-08 | (빈값) | ORD-1785830191902 | 해피유통(포천) 08-04 전품목 일치 |
| 260803-10 | (빈값) | ORD-1785830730345 | 해내음식품 08-03 |
| 260811-02 | (빈값) | ORD-1786320336175 | 엠에스식품 08-11 |
| 260803-08 | ORD-1785718993679(없음) | ORD-1785830984717 | 세화식품 08-03 |
| 260805-10 | ORD-1785912941897(없음) | ORD-1785994168175 | 일성상회 08-06 특A/1750ml ×40 |
| 260818-09 | ORD-1787018171539(없음) | ORD-1787042128510 | 김밥담 08-18 |

**매칭 기준:** 같은 거래처 매출전표 · 날짜 ±4일 · **품목 이름과 낱개수량이 통째로 일치** ·
후보가 딱 하나. 박스 주문은 개입수를 곱해 낱개로 환산하고, 전표의 '시골향 참기름'처럼
공백이 낀 이름은 공백을 지워 비교했다.

**손대지 않은 것 4건(수량이 실제로 다르다 — 전표가 맞고 주문이 틀렸을 수 있다):**
청정식품 08-03(특A/1800ml ×36 vs 전표 A/1750ml ×30) · 엄마랑 빈대떡 08-11(택배비 줄) ·
거산농산 08-19(전표에 볶음검정참깨 −14 반품 줄) · 일성상회 08-26(수량 1·2 vs 10·20).

**결과(1·2차):** 미발행 목록 39건 → 33건.

### 3차 — 스마트스토어 제외 + 여분 줄 허용 매칭 (같은 날)

**사장님 지시:** "스마트스토어주문만 빼고 매칭하면 돼 / 주문단위로 하라고."
(거래처 단위로 숨기는 방안은 만들다 폐기했다 — 주문 단위로 본다.)

- **스마트스토어 주문 12건**은 전표 발행 대상이 아니라 목록에서 뺀다 → 33건 → 21건
- 매칭 조건을 넓혔다: **주문의 모든 줄이 전표에 있으면** 같은 건으로 본다
  (전표에 반품·택배비 같은 여분 줄이 더 있어도 된다)

| 전표 | 전 | 후 | 근거 |
|---|---|---|---|
| 260819-09 | ORD-1787018230541(없음) | ORD-1787124911666 | 거산농산 08-19 — 5줄 수량 전부 일치(`볶음참깨/1kg`↔`볶음참깨-낱개/1kg` 표기차). 전표에 반품 −14 줄만 더 있다 |
| 260811-05 | (빈값) | ORD-1786431682838 | 엄마랑 빈대떡 08-11 — 골드/1800ml ×1 일치, 나머지 둘은 벌크 20kg ×1 두 개로 개수 일치(주문 품목이 삭제돼 이름이 안 읽힌다). 전표에 택배비 줄만 더 있다 |

**못 이은 것 1건 — 청정식품 08-03 `ORD-1785830866346`**
주문 `시골향참기름/특A/1800ml ×36` vs 전표 `260803-11`의 `시골향 참기름/A/1750ml ×30`.
등급·용량·수량이 모두 달라 같은 건이라 단정할 수 없다. 나머지 2줄(볶음참깨 480·들깨가루 20)은 맞는다.

**최종:** 미발행 **19건**. 전부 전표가 실제로 없다.
백업 `scripts/fix-relink-statements-backup.json`(2차 6건분).
1차 3건은 백업이 덮여 `--undo` 대상이 아니다 — 되돌리려면 위 표의 '전' 값을 손으로 넣어야 한다.

---

## 2026-08-28 — 원장·로트 불일치 3종 정정 (깨분참기름·수입들기름·생들기름)

**작업자:** Claude 에이전트 (사용자 확정)
**사유:** "원장잔량이랑 로트가 왜 자꾸 틀어지냐". 정본 계산(`ledgerBalanceKg`, 실사 앵커 반영)으로
재니 14종 중 6종이 갈려 있었다. 큰 셋을 맞췄다.
**판단 기준(사용자 지시):** "누락이 안 된 쪽이 더 정확한 것". 코드상 누락 통로는 **로트 쪽**에 있다 —
`AdminApp:1825`(압착)·`ItemList:3133`(수불부 손입력) 둘 다 원장을 먼저 쓰고 로트 실패를 `catch`로
삼켰다. 원장 줄은 id 고정(`rm-auto-{주문}-{원료}`)이라 덮어써질 뿐 사라지지 않는다.

### ① 깨분참기름 — 로트를 원장에 맞춤 (갭 771.005kg)

로트 −508.183kg. 앱의 실사(commitStockEdit)와 같은 방식으로 로트를 원장 잔량에 맞추고
**원장에 targetKg 앵커 줄**을 남겨 이후 표류가 끊기게 했다.

| | 변경 전 | 변경 후 |
|---|---|---|
| 로트 잔량 | −508.183 kg | **262.822 kg** (`260827-01` 원장맞춤 로트) |
| 원장 | 262.822 kg | 같음 + 앵커 줄 `rm-anchor-깨분참기름-20260827` |

**스크립트:** `scripts/fix-kkaebun-lot-to-ledger.mts` (dry/`--apply`/`--undo`)

### ② 수입들기름 — 원장을 로트(0)에 맞춤, **입고 줄로** (갭 −440.083kg)

원장이 −440.083kg. 사용은 적혔는데 그만큼의 매입이 원장에 안 잡힌 것이라
**앵커로 덮지 않고 입고 줄로 채웠다** — 앵커로 지우면 그 매입이 서류에서 영영 사라진다(사용자 지시).

`rm-fix-수입들기름-20260827` · 2026-08-27 입고 440.083kg · type=manual → 원장 0 = 로트 0

**스크립트:** `scripts/fix-suip-ledger-to-zero.mts` (dry/`--apply`/`--undo`)

### ③ 생들기름 — 겹친 둘을 각각 (겉보기 갭 −41.152kg)

두 오류가 서로 상쇄해 작아 보였다.

| | 원장 | 로트 |
|---|---|---|
| 08-23 실사 앵커 | 3,055.668 | 3,055.668 (일치) |
| 08-24 `ORD-1787192433189` 해피유통(포천) 생들기름300ml 50박스 | −277 | **안 빠짐** |
| 08-25 `ORD-1787543689411` 해피유통(포천) 생들기름300ml 50박스 | −277 | **안 빠짐** |
| 08-27 살림터 | −16.62 | −16.62 |
| 수입들기름 대체 이동(2026-08-27 작업) | **안 씀** | −512.848 |

- 두 주문은 **서로 다른 주문**이고 둘 다 `rawConsumedLots`에 생들기름 277kg이 찍혀 있다 —
  엔진은 뺐다고 기록했는데 로트 쓰기만 날아갔다(`catch` 삼킴의 전형).
  (1,000병 × 0.3L × 0.924 = 277kg — 수량 검산도 맞다)
- 정정: 원장에 `수입들기름 대체 사용 512.848kg` 줄 추가 + 로트에서 554kg 차감
- 결과 **원장 1,972.2 = 로트 1,972.2**

**스크립트:** `scripts/fix-saeng-ledger-sync.mts` (dry/`--apply`/`--undo`)

### 검증 · 남은 것

14종 중 8종이 차이 0. 남은 6종 합 약 232kg:
`볶음들깨 93 · 볶음참깨 −65 · 통깨참기름 34.875 · 볶음검정참깨 21 · 통들깨들기름 18.191 · 탈피들깨가루 8`
볶음 계열 셋은 `lotsAreTotal`이라 축이 달라 따로 봐야 한다(로트=통합, stock=벌크만).

**재발 방지(코드):** 쓰기 직후 되읽어 대조하는 `src/shared/ledgerLotCheck.ts` 도입 — 커밋 참조.

---

## 2026-08-27 — 수입들기름 말통값이 L당 단가로 들어간 것 정정 + 마이너스 재고를 생들기름으로 이관

**작업자:** Claude 에이전트 (사용자 확정)
**사유:** 재고액 합계가 이상하다는 지적. 재고총액이 115,745,103원으로 내려앉아 있었다.
**진단:** `scripts/diag-pack-price.mts`, `scripts/diag-invvalue-consistency.mts` (읽기 전용, 사용 후 삭제)

### ① 원가 — 수입들기름 189,200/L → 10,595.2/L

수입들기름은 **16.5kg 말통 단위로 189,200원**에 산다(현대농업 매입전표). 그 말통 값이
`items.cost`에 통째로 들어갔는데 품목 단위는 **L**이다 → L당 189,200원, **17.9배**.
2026-08 스냅샷엔 11,467(=189,200÷16.5kg)이 찍혀 있어, 원래는 나눠 넣었던 것을
그 뒤에 누가 189,200으로 덮어쓴 것으로 보인다. 다만 11,467도 kg 기준이라 L엔 밀도를 더 곱해야 한다.

| | 값 |
|---|---|
| 바른 단가 | 189,200 ÷ (16.5kg ÷ 0.924kg/L = 17.857L) = **10,595.2원/L** |
| 딸려 틀어졌던 것 | 들기름(=수입들기름×0.8+통들깨들기름×0.2) 154,928/L, 시골향들기름 350ml 50,318원/병, 1750ml 10병박스 2,510,811원 |
| 갱신 건수 | 수입들기름 + 파생 완제품·반제품 **67건** (롤업 재계산) |

**스크립트:** `scripts/fix-suip-deulgireum-unit.mts` (기본 dry, `--apply`, `--undo`)
**되돌리기:** `--undo` (백업 `scripts/fix-suip-deulgireum-unit-backup.json` — 전 품목 원가 통째로)

같은 방식으로 다른 품목도 훑었으나(`diag-pack-price`) **수입들기름 하나뿐**이었다.
통깨참기름은 같은 16.5kg 말통 매입인데 L당 9,373으로 제대로 나눠 넣어져 있었다.

### ② 재고 — 수입들기름 −512.848L을 생들기름에서 차감

수입분이 모자라 **생들기름(자가 착유)을 대신 쓴 것**이라 그 양만큼 옮긴다(사용자 지시).

| 품목 | 변경 전 | 변경 후 |
|---|---|---|
| 수입들기름 stock | −512.848 L | **0** |
| 생들기름 stock | 3,039.048 L | **2,526.200 L** |

`items.stock`만 건드렸다 — 원료수불부(`rawDocEntries`)·원장(`rawMaterialLedger`)은 손대지 않았다.
**스크립트:** `scripts/fix-suip-to-saeng-stock.mts` (기본 dry, `--apply`, `--undo`)
**되돌리기:** `--undo` (백업 `scripts/fix-suip-to-saeng-stock-backup.json`)

### ②-보정 (같은 날) — 로트도 옮기고, 원가 단위를 kg으로 정정

처음에 `items.stock`만 바꾸고 **로트를 안 옮겼다.** 원료 재고의 근거는 로트다
(`lotStockInUnit` = 로트 kg 잔량 합) — 로트를 그대로 두면 화면은 계속 −512.848로 보이고,
다음에 로트를 건드리는 처리가 stock을 로트 합으로 덮어써서 되돌아간다.

| 대상 | 변경 전 | 변경 후 |
|---|---|---|
| 수입들기름 lots (이월 로트) | −512.848 kg | **0** (depleted) — 로트 합 0 = stock 0 |
| 생들기름 lots `260803-01` (FIFO) | 2,789.604 kg | **2,276.756 kg** — 로트 합 2,526.2 = stock 2,526.2 |

**스크립트:** `scripts/fix-suip-to-saeng-lots.mts` (기본 dry, `--apply`, `--undo`)

그리고 **원가 단위가 틀렸다.** 재고·로트·BOM 수량이 전부 **kg**이다
(`lotStockInUnit`=`lotKgRemaining`, BOM 수량도 2026-08-14부터 kg 고정). 품목 `unit`이 'L'인 건
화면 표시용이다. 그런데 밀도를 한 번 더 나눠 원/L로 넣었다.

| | 값 |
|---|---|
| 처음 넣은 값(틀림) | 189,200 × 0.924 ÷ 16.5 = 10,595.2 |
| 맞는 값 | 189,200 ÷ 16.5kg = **11,466.67 원/kg** — 2026-08 스냅샷에 찍혀 있던 11,467이 옳았다 |

**스크립트:** `scripts/fix-suip-cost-kg.mts` (기본 dry, `--apply`, `--undo`) · 67건 재롤업

### 검증

- 재고총액 **115,745,103원 → 199,864,277원** (①·② 직후 값. 위 보정 후 들기름 계열이 8.2% 올라간다)
- 수입들기름 stock 0 = 로트 합 0 ✅ · 생들기름 stock 2,526.2 = 로트 합 2,526.2 ✅
- 카테고리 소계 합 = 총액 ✅ · 박스 원가 = 낱개×개입수+박스자재 전건 일치 ✅ · 낱개/박스 겹쳐 세기 없음 ✅
- 기말재고 스냅샷 4건 모두 적힌값 = 품목합 ✅
- 곁가지로 찾은 **코드** 결함: 스냅샷 상세를 펼칠 때 되읽기 표(`SNAP_TO_TYPE`)에 없는 분류 '비닐'
  4품목 2,960,980원이 부자재가 아니라 '기타'로 새고 있었다 → 품목의 지금 `type`으로 되올리게 수정(코드 커밋).

---

## 2026-08-13 — 완제품 재고 이상 3건 정정 (알찬350 작업완료 미반영 + 박스 음수재고)

**작업자:** Claude 에이전트 (사용자 확정)
**사유:** 재고 현황 '재고'(현재고 − 작업완료) 뷰를 만들며 음수·불일치 3건 발견.
**진단:** `scripts/diag-stock-anomalies.mts` (읽기 전용)
**스크립트:** `scripts/fix-alchan350-dispatched-stock.mts`, `scripts/fix-negative-box-stock.mts` (둘 다 기본 dry, `--apply`, `--undo`)

| 품목 | 변경 전 | 변경 후 | 근거 |
|---|---|---|---|
| `p-1784787834504` 참기름/병/특/알찬/350ml (낱개) | stock 0 | stock **900** | `ORD-1786405645013`(해피유통(쿠팡)) 900개가 2026-08-11 작업완료(DISPATCHED)·미출고. 재고 +900은 정상 반영됐으나 **사용자가 재고 현황에서 휴지통 버튼으로 0으로 지움**(전체 뷰 휴지통은 작업완료분까지 통째로 0). 사용자 확인: 900병 실물 보유 |
| `box-p-165-20` 시골향 들기름/병/특/350ml (20개입) | stock −1 | stock **0** | 출고완료 5박스(엠에스1·진보반찬4)만 있고 박스 재고를 채운 기록 없음 |
| `box-p-1774944582929-10` 들기름/알이네/1800ml (10개입) | stock −2 | stock **0** | 출고완료 2박스(알이네)만 있고 입고 기록 없음 |

- 검증: 알찬350 낱개 stock=900, 작업완료=900 → '재고' 뷰 0으로 정합. 박스 2건 stock=0.
- **되돌리기:** 각 스크립트 `--undo` (백업 `scripts/fix-alchan350-dispatched-stock-backup.json`, `scripts/fix-negative-box-stock-backup.json`).
- **원인 규명(사용자 확인)**: 엔진 버그 아님. 재고 현황에서 **휴지통 버튼으로 재고를 0으로 지운 것**이 발단.
  알찬350은 작업완료 +900이 통째로 지워졌고, 박스 2건은 0이 된 뒤 출고가 더 나가 음수가 됨
  (휴지통은 0까지만 만들 수 있어 음수는 이후 출고로 발생). `orderStockEngine.produceOrder` 점검 불필요.
- **재발 방지(코드)**: 재고 뷰 휴지통은 작업완료분을 보존하도록 변경(`stock = 작업완료분`),
  전체 뷰 휴지통은 "작업완료 N개도 함께 사라집니다" 확인창 경고 추가. 커밋 `d2820f5` 및 후속 커밋 참조.

---

## 2026-08-13 — 볶음참깨 벌크 풀(raw-볶음참깨) 실사 앵커 + 새봄 stale 차감 정정

**작업자:** Claude 에이전트 (사용자 확정)
**사유:** `raw-볶음참깨`(벌크 kg 풀)이 −7,140kg. 원인 = ①청정 벌크 입고를 그동안 로트에 안 넣어옴(운영 누락) ②새봄푸드 stale 과다차감. 소분(80~500g)·벌크판매가 여기서 kg 차감하는 배선은 정상. 사용자 실사값(벌크 0.5개=10kg)으로 앵커.
**스크립트:** `scripts/fix-bokkeum-bulk-anchor.mts` (기본 dry, `--apply`)

| 대상 | 변경 전 | 변경 후 |
|---|---|---|
| items/`raw-볶음참깨` lots+stock | 로트 2개(260811-01 0kg, lot-carry −7140kg), stock −7140 | 실사 로트 1개(260812-실사, 청정벌크 10kg), stock 10 |
| orders/`ORD-1786430165162`(새봄푸드) rawConsumedLots(볶음참깨) | 3,600kg (qty 180일 때 값, 현재 qty=9=180kg) | 180kg (supplierName '실사보정') |
| rawMaterialLedger/`rm-auto-ORD-1786430165162-볶음참깨` used | 3,600kg | 180kg |

- 모란(60kg)·반석(40kg)은 실제 소비와 일치(박스 언팩 20×1kg) — 손대지 않음.
- 검증: 새봄 기록=현재요구=180kg 일치. raw-볶음참깨 로트잔량=stock=10kg.
- **되돌리기:** 앵커 전 값은 위 표 '변경 전'에 기록(자동 백업 없음). 새봄 스냅샷 180kg은 lotId 없이 저장돼 되돌리기 시 +180kg 복원로트 생성(안전).
- **앞으로:** 청정 벌크 입고는 매번 raw-볶음참깨 수동입고로 기록해야 함(안 하면 다시 마이너스). 푸미푸드 가공입고는 완제품(10·20개입·낱개)만 채우는 현행 유지.

---

## 2026-08-05 — 낱개/박스 품목 BOM에서 불필요한 겉박스·테이프 제거

**작업자:** Claude 에이전트 (사용자 요청)
**사유:** 겉박스·테이프는 박스(개입) 품목 BOM에 들어감. 그 구성품인 낱개(또는 별도 박스변형)에도 들어있으면
이중차감/불필요. 특히 p-110은 10개입 박스에 물려 있어 박스 출고 시 낱개 생산분마다 2호박스가 이중 차감됨.
**스크립트:** `scripts/remove-box-tape-from-items.mjs` (dry-run 기본, `--apply`, `--undo`, 백업 `remove-box-tape-from-items-backup.json`)

| 품목 | 제거 | 유지 |
|---|---|---|
| p-110 시골향 참기름/특골드/1750ml (낱개) | 2호박스×1, 테이프-노랑×0 | 참기름·마개·페트병·라벨 |
| p-17 참기름/특A/대왕/1800ml (낱개) | 2호박스×1, 테이프-노랑×0 | 참기름·마개·페트병·라벨 |
| p-1785904023420 볶음참깨/20kg박스/모란 | 2호박스×1, 테이프-투명×1 | 1KG-실링·모란라벨 |

- `updateDoc`으로 submaterials만 교체(나머지 필드 보존). 전체 낱개 159개 중 겉박스/테이프 보유는 이 3개뿐(전수 확인 diag-all-products-box-tape.mjs).
- 되돌리기 `--undo`로 이전 submaterials 복원.

---

## 2026-08-05 — 볶음참깨 10개입/20개입을 10kg/20kg박스와 동일(임가공) 설정

**작업자:** Claude 에이전트 (사용자 요청)
**사유:** 새로 만든 볶음참깨 소분 박스(10개입·20개입)를 기존 10kg/20kg박스와 동일하게 동작시켜
판매 시 원료수불부에 `볶음참깨`가 기록되게 함(볶음참깨=임가공). 새 품목엔 procureType/품목/spec이 비어 있어 안 찍히던 것.
**스크립트:** `scripts/set-bokkeum-gaeip-imgagong.mjs` (dry-run 기본, `--apply`, `--undo`, 백업 이전값 `set-bokkeum-gaeip-imgagong-backup.json`)

| 품목 | procureType | 품목 | spec |
|---|---|---|---|
| 볶음참깨/1kg (10개입) p-1785907900413 | ''→임가공 | ''→시골향볶음참깨 | ''→10kg |
| 볶음참깨/1kg (20개입) p-1785907940315 | ''→임가공 | 시골향볶음참깨(유지) | ''→20kg |

- `updateDoc`(병합) — submaterials 등 다른 필드 보존.
- 검증(sim-bokkeum-imgagong-verify.mts): 개단위/박스단위 입력 시 원료수불부 정확(10개입 48박스=480kg, 20개입=960kg). 기존 박스와 동일.
- ⚠️ **잔여 함정(코드)**: 박스 주문이 "낱개 수량 + 박스플래그 없음"으로 들어가면 `spec×수량` 배수 과다(청정 4800kg 원인). 모든 박스 공통. 코드 가드 별도 필요.
- ⚠️ **stale 데이터**: 청정 ORD-1785830866346 원료수불부 4800kg은 옛 값(현 주문 48박스면 480kg). 미정정.

---

## 2026-08-05 — 볶음참깨 10개입/20개입 신품목에 박스 거래처(out) 복제

**작업자:** Claude 에이전트 (사용자 요청)
**사유:** 볶음참깨 1kg 소분 박스 품목(10개입·20개입)을 새로 만들어, 기존 10kg박스/20kg박스에 물려있던
거래처 판매연결을 동일 방식으로 이어줌. 매핑: 10kg박스(10×1kg)→10개입, 20kg박스(20×1kg)→20개입.
**스크립트:** `scripts/wire-bokkeum-box-partners.mjs` (dry-run 기본, `--apply`, `--undo`, 백업 `wire-bokkeum-box-partners-backup.json`)

| 원본 박스 | 새 품목 | 거래처 수 |
|---|---|---|
| 볶음참깨/10kg박스 (p-1780625531675) | 볶음참깨/1kg (10개입) (p-1785907900413) | 7곳 |
| 볶음참깨/20kg박스 (p-1780625559322) | 볶음참깨/1kg (20개입) (p-1785907940315) | 46곳 |

- `partner_item` **53건 신규 생성**. 구조는 기존 박스 연결과 동일: `{id, itemId, partnerId, Direction:'out'}` (단가 없음 — 박스단가는 낱개×개입수 자동 파생).
- 원본 박스 품목의 연결·데이터는 **변경 없음**(복제만). 되돌리기 `--undo`로 생성분 53건 삭제.
- ⚠️ BOM(낱개×개입수) 배선은 이번 작업 범위 아님 — 두 신품목의 submaterials 구성은 별도 확인 필요.

---

## 2026-07-23 — `manualAdjustment`(구 수동 휴가) → '휴가' 신청 기록 이관 후 필드 제거

**작업자:** Claude 에이전트 (사용자 요청)
**사유:** 단체 휴가를 직원 문서의 숫자 필드로 들고 있어 이력이 없고, 관리자·직원 앱이 서로 다른
이름('수동 차감' / '휴가')으로 불렀다. '휴가' 버튼이 실제 신청 기록을 만들게 되면서 필드가 중복이 됐다.
**스크립트:** `scripts/migrate-manual-to-vacation.mjs` (되돌리기 `--undo`, 백업 `migrate-manual-to-vacation-backup.json`)

| 직원 | 이관 |
|---|---|
| 아브라함 11.5 · 이지영 3 · 박은지 3 · 황준호 3 · 이총제 2 · 태백식품 1 · 남명숙 1 | → `leaveRequests/leave-legacy-vac-{empId}` (type `휴가`, `2026-01-01`, approved) |
| 이은경 · 윤찬호 (값 0) | 필드만 제거 |

- `employees.manualAdjustment` **전원 제거**(0명 남음). 코드에서도 타입·UI 전부 삭제.
- 언제 쓴 건지 기록이 없어 **올해 1월 1일자**로 남겼다(사유에 명시).
- **잔여 일수는 전원 변동 없음** — 이관 전후 검증 완료(박은지 14, 이지영 −6 등).

---

## 2026-07-17 — 외주가공비(540) 계정과목 신설

**작업자:** Claude 에이전트 (사용자 요청)
**사유:** OEM 가공입고 시 가공비 매입전표가 이 계정으로 끊긴다. 없으면 폴백으로 엉뚱한 곳에 집계됨.
**스크립트:** `scripts/add-oem-fee-account.mjs` (dry-run 기본, `--apply`, `--undo`)

| 문서 | 내용 |
|---|---|
| `accountCodes/ac-540` | `540 외주가공비` → `ag-cogs`(총매출원가). 임가공은 제조원가. 과세(세금계산서 수취) |

가공단가 기본값 **500원/kg** (푸미푸드 볶음) — 코드 상수 `OEM_DEFAULT_FEE_PER_KG`, 입고 시 변경 가능.

---

## 2026-07-17 — 볶음참깨 3품목 `완사입` → `임가공`(OEM) 전환

**작업자:** Claude 에이전트 (사용자 요청)
**사유:** 이 3품목은 남의 완제품을 사오는 게 아니라 **우리 참깨를 푸미푸드에 보내 볶아 오는** 것(임가공).
완사입으로 잡혀 있어 참깨가 외주로 나가는 게 기록되지 않고, 가공비와 완제품매입이 구분되지 않았다.
**스크립트:** `scripts/set-bokkeum-oem.mjs` (dry-run 기본, `--apply` 반영, `--undo` 되돌리기)

| 문서 | 변경 |
|---|---|
| `items/PLDhkjOgcPIhO1hhReHm` (볶음참깨-낱개/1kg) | `procureType` 완사입 → **임가공** |
| `items/p-1780625531675` (볶음참깨/10kg박스) | `procureType` 완사입 → **임가공** |
| `items/p-1780625559322` (볶음참깨/20kg박스) | `procureType` 완사입 → **임가공** |

- **판매 동작 변화 없음** — `isGoodsItem`이 완사입·임가공 둘 다 포함하므로 출고 시 자기재고 −N 그대로.
- 카테고리는 `product` 유지. 설계: `docs/OEM-임가공-설계.md`
- 외주공장 = **푸미푸드**. 발주·가공입고 핸들러/UI는 미구현(엔진만 있음).

**향후 방향(사용자 결정):** `category: 'goods'`(상품)를 없애고 `product` + `procureType: '완사입'`으로 통일.
지금은 `goods`와 `완사입`이 `isGoodsItem`에서 같은 동작이라 중복. 급하지 않아 나중에.

---

## 2026-07-14 — 회계 구조 정비 (자금원장 도입 · 계정과목 정리 · receivedAt 오용 수정)

**작업자:** Claude 에이전트 (사용자 요청)
**배경:** 전기세를 어느 전표로 끊느냐에서 출발해 회계 구조 전반을 정리. 상세는 `docs/복식부기-설계.md` 참고.

### 1) `issuedStatements` — 짠지네식품 매출전표 계정과목 채움
`stmt-1782894923495` (2026-07-01, 265만원) 5개 라인에 `accountCode: '800'`(일반매출) 설정.
7/1 이후 계정과목 누락 전표는 이제 0건.

### 2) `issuedStatements` — `receivedAt` → `taxIssuedAt` 이관 (8건)
`receivedAt`은 이름과 달리 **세금계산서 PDF 저장 시각**이 찍히고 있었다
(`TradeStatement.handleTaxPdf`가 `as any`로 잘못된 필드에 기록. 형제 핸들러는 `taxIssuedAt`을 쓰고 있었음).
매입전표 8건의 `receivedAt` → `taxIssuedAt`으로 복사 후 `receivedAt` 필드 삭제.
**진짜 입고 시각은 `PurchaseOrder.receivedAt`이 담당하며 건드리지 않았다.**

### 3) `accountCodes` — 정리
| 코드 | 변경 |
|---|---|
| `605 운임` | `groupId: 'ag-admin'`(판관비) 지정 — 그룹이 없어 폴백으로 엉뚱한 곳에 떨어지고 있었음 |
| `818 감가상각비` | `noncash: true` — 손익엔 잡히되 현금흐름표에서 순이익에 가산 |
| `535 퇴직급여충당금` | **신설** (`ag-cogs` 총매출원가, `noncash: true`). 급여(515)가 제조원가라 같은 그룹 |

**미해결:** `650 카드대금`의 `groupId`가 존재하지 않는 그룹을 가리킴(dangling).
계정과목이 아니라 결제수단이므로 자금원장의 '카드' 계좌로 대체하고 삭제하는 게 맞다.

### 5) `issuedStatements` — `partnerName` 백필 (29건)
4~5월 전표 29건에 `partnerName`이 비어 있었다(`partnerId`는 정상). 거래처원장에서 `(이름없음)`으로
뜨고 잔액이 쪼개져 보였다. `partnerId`로 `partners`를 조회해 이름을 채웠다.
**삭제 없음 — `partnerName` 필드만 채우는 비파괴 백필.** 29건 전부 성공, 건너뛴 것 없음.

가득찬식품 15건, 한국농수산물유통공사 5건, 형제프라콘 3건, 희성실업 2건, 기타 4건.
7월 이후 전표는 문제 없음(0건). 저장 로직은 그 사이 고쳐진 것으로 보인다.

> ⚠️ **주의:** `partners` 문서는 `addItem`이 `id` 필드를 떼고 저장하므로, 227건 중 125건만
> `id` 필드를 갖고 있다. **거래처 조회는 반드시 문서 ID(`doc.id`)로 해야 한다.**
> `p.id`로 조회하면 절반이 `undefined`가 되어 멀쩡한 거래처가 "유령"으로 보인다.

### 4) `cashAccounts` / `cashEntries` — 신규 컬렉션 + 임시 시딩
자금원장(현금출납장) 도입. **테스트용 임시 데이터이므로 앱에서 지우거나 실제 값으로 교체할 것.**

| 문서 | 내용 |
|---|---|
| `cashAccounts/cashacct-temp-main` | "임시 메인통장" (통장), 기초잔액 10,000,000원 / 기준일 2026-07-01 |
| `cashEntries/cash-seed-1` | 2026-07-05 입금 3,000,000 (짠지네식품, 계정 800) — 샘플 |
| `cashEntries/cash-seed-2` | 2026-07-09 출금 1,200,000 (한국전력공사, 계정 520) — 샘플 |

`settlements` 컬렉션도 신설(자금↔전표 매칭)되었으나 데이터는 아직 없음.

---

## 2026-06-17 — 누락 입고 백필 (인천)청정식품 깨분참기름 20캔

**작업자:** Claude 에이전트 (사용자 chdwp 요청)
**사유:** 06-17 선입고 시 Firebase 일일 한도(429)로 로트/수불부 기록 트랜잭션이 실패(전표는 생성됨, 원료는 누락). 한도 회복 후 수동 백필.
**스크립트:** `scripts/backfill-cheongjeong-260617.mts` (되돌리기: `--undo`, 백업 `scripts/backfill-cheongjeong-backup.json`)

| 문서 | 변경 |
|---|---|
| `raw-깨분참기름` | `lots` += (인천)청정식품 330kg(20캔×16.5, lotNo 260617-01), `stock` 2779 → 3140.165 L |
| `rawMaterialLedger/rm-backfill-cheongjeong-20260617` | +330kg 입고 기록 신규 |

**관련 코드 수정(미배포):** 입고 시 원료 로트/수불부 기록 실패가 조용히 삼켜지던 것 → 사용자에게 경고창 표시(ReceivingReturnsManager 스캔·선입고 경로). 재발 시 즉시 인지 가능하도록.

---

## 2026-06-15 — 원료 로트(lot) 시스템 도입

**작업자:** Claude 에이전트 (사용자 chdwp 요청)
**관련 코드:** `RawMaterialLot` 타입, `mutateRawMaterialLots`, 입고 핸들러 로트 연동, 원료재고 로트 UI (로컬 브랜치, 미배포)
**스크립트:** `scripts/lot-migration.mts` (되돌리기: `npx tsx scripts/lot-migration.mts --undo`)
**백업:** `scripts/lot-migration-backup.json` (변경 전 stock/lots 원본)

### 스키마 추가 (items 컬렉션, 코드 레벨 — 기존 문서엔 점진 적용)
- `lots?: RawMaterialLot[]` — 원료(raw) 품목의 입고 로트 배열. 배열 순서 = 선입선출 순서.
- `packageType?: string` / `packageKg?: number` — 매입 SKU 포장 단위(캔/포대/자루)·1개당 kg.
- `RawMaterialLot` 구조: `{ id, supplierId?, supplierName, packageType?, packageKg?, qtyIn?, kgIn, kgRemaining, receivedDate, lotNo?, status: 'active'|'depleted', poId?, createdAt }`. 잔여는 항상 **kg**(canonical), 기름은 화면에서 L로 환산 표시.

### 데이터 변경 (4건, 위 스크립트로 적용)
| 문서 ID | 품목 | 변경 | 비고 |
|---|---|---|---|
| `p-1779251176421` | 참깨 (raw) | `lots` += 이월 5170kg | stock 5170 유지 |
| `raw-깨분참기름` | 깨분참기름 (raw) | `lots` += 이월 1137.672kg | stock 1242 L 유지 (1242×0.916) |
| `raw-생들기름` | 생들기름 (raw) | `lots` += 이월 33.264kg | stock 36 L 유지 |
| `p-1779251603644` | 깨분참기름/16.5kg (wip, 캔 SKU) | `stock` 176 → **0** | 차감 안 되던 누적 카운터 정리 |

- "이월" 로트 = 마이그레이션 시점의 기존 재고를 로트로 보존(수불부 잔량 이월). **재고 수치는 안 바뀜**, 로트로 보이게만 함.
- 캔 SKU(p-1779251603644)는 발주/입고용 SKU로 유지하되, 의미 없는 누적 재고만 0으로 정리. 이후 입고분은 코드가 자동으로 원료(raw) 로트로 기록(매입 SKU stock은 더 이상 누적 안 함).

### 확인 완료
- **깨분참기름 풍회유통 6/15 입고 68캔**: 사용자 확인 결과 이월 1,242L에 **이미 포함**됨 → 별도 풍회 로트 추가 불필요. (이월 1건으로 충분)

### 이전 작업 (읽기 전용 — DB 미변경)
- `scripts/diagnose-punghoe*.mjs`, `scripts/check-raw-purchasing-skus.mjs`, `scripts/lot-migration.mts --dry`, `scripts/verify-lots.mjs` — 전부 **조회만** 함. 쓰기 없음.

## 2026-07-02 — 매입 SKU 원료 연결 (rawMaterialName 지정)

실행: `node scripts/set-raw-material-name.mjs` (사용자 요청). 이름 변경 대신 rawLotTarget이 우선 참조하는 필드 지정.

| 문서 ID | 품목 | 변경 | 비고 |
|---|---|---|---|
| `p-108` | 시골향 볶음검정참깨-벌크/20kg (product) | `rawMaterialName` = "볶음검정참깨" | 입고 시 개수×20kg → 원료 로트 |
| `p-1780646377919` | 볶음참깨-자루/20kg (product) | `rawMaterialName` = "볶음참깨" | 입고 시 개수×20kg → 원료 로트 |

- 6/29~7/1 로트 소실 사고 복구(restore-lots-260702.mjs)는 **사용자 보류 중** — 아직 미실행.

## 2026-07-09 — 들향기름골드 기본 박스 수량 12 → 10

**작업자:** Claude 에이전트 (사용자 chdwp 요청)
**사유:** 들향기름골드 1박스가 실제 10개인데, 품목에 박스 규격이 없어 향미유 코드 폴백(12)이 적용되고 있었음.
**스크립트:** `scripts/set-deulhyang-gold-box10.mjs` (되돌리기: `--undo` → 필드 삭제, 폴백 12 복귀)

| 문서 | 변경 |
|---|---|
| `items/f6` (들향기름골드, goods) | `defaultBoxConfig` 없음(null) → `{ boxType: "", unitsPerBox: 10 }` |

- 거래처별 오버라이드(shipping_rules·partner_item qtyPerBox) 0건 확인 → 전 거래처 일괄 10개 적용.
- 기존 주문 문서는 저장 당시 값(12) 유지 — 과거 이력 불변.

## 2026-07-13 — 안 쓰는 파레트 3종 숨김(hidden 플래그)

**작업자:** Claude 에이전트 (사용자 chdwp 요청 "전체 재고에서 삭제")
**사유:** 목재파렛트는 주문·거래 기록이 있어 문서 삭제 시 거래처 잔량 계산이 깨짐 → 삭제 대신 hidden 플래그로 화면·선택 목록에서만 제거(이력·잔량 계산 유지).
**스크립트:** `scripts/hide-unused-pallets.mjs` (되돌리기: `--undo`)

| 문서 | 변경 |
|---|---|
| `pallets/pal-wood` (목재파렛트) | `hidden: true` |
| `pallets/pal-plastic` (플라스틱파렛트) | `hidden: true` |
| `pallets/pal-black` (검정파렛트) | `hidden: true` |

## 2026-07-13 — 볶음참깨 재고 모델 재편 (박스 카운팅)

**작업자:** Claude 에이전트 (사용자 chdwp 확정안)
**사유:** 확인사항에 "1KG-볶음참깨(-770)"·"볶음참깨(0)" 이중 알림 — 완포장 사입품이 부자재(용기)로 잘못 모델링돼 유령 재고 누적. 박스 수 그대로 카운팅하는 모델로 재편.
**스크립트:** `scripts/migrate-bokkeum-model.mjs` (백업 `migrate-bokkeum-model-backup.json`, 되돌리기 `--undo`)

| 문서 | 변경 |
|---|---|
| `items/p-1780625531675` (볶음참깨/10kg) | stock 120→0(박스), `procureType:완사입`, `unpackTo:{낱개,10}`, spec 1kg→10kg, 옛 BOM 참조 제거 |
| `items/p-1780625559322` (볶음참깨/20kg) | stock 0(박스), 완사입, `unpackTo:{낱개,20}`, spec 1kg→20kg, 참조 제거 |
| `items/PLDhkjOgcPIhO1hhReHm` (낱개/1kg) | stock 20→0(팩), 완사입, 참조 제거 |
| `items/p-1779069467512` (스마트스토어/1kg) | 완사입 지정 |
| `items/p-1783918260605` (350g) | raw-볶음참깨 직접참조 제거(원료식으로 대체) |
| `items/p-1781770117874` (80g) | spec 140g→80g 오타 수정 |
| `items/p-1781739328648` | 이름 "볶음참깨"→"볶음참깨/500g" |
| `item_formula/formula-시골향볶음참깨-볶음참깨` | 신규 — 80/140/200/350/500g이 벌크(raw-볶음참깨)에서 spec 기준 kg 차감 |
| `items/PLYZ-S-1000` (1KG-볶음참깨) | archived, stock -770→0 (유령 재고 폐기) |
| `adjustmentRequests` 2건 | 유령 발주필요 알림 삭제 (1KG-볶음참깨 810개 / 볶음참깨 20개) |

- 재고 전부 0 시작 — 실사값은 사용자가 직접 입력 예정.

## 2026-07-13 — 볶음참깨 마무리 배선 (#2, 사용자 품목 정리 후)

**작업자:** Claude 에이전트 (사용자 요청)
**스크립트:** `scripts/wire-bokkeum-final.mjs` (백업 `wire-bokkeum-final-backup.json`, `--undo`)

| 문서 | 변경 |
|---|---|
| `items/PLDhkjOgcPIhO1hhReHm` (낱개/1kg) | `isSmartStore: true` — 삭제된 스마트스토어/1kg 품목 대체(낱개 재고로 통합) |
| `items/p-1779937553909` (볶음참깨(벌크)/20kg) | `rawMaterialName: 볶음참깨`, 자루 20kg — 자루 SKU 삭제로 끊긴 벌크 입고→로트 경로 복구 |
| `items/p-1780625531675`·`p-1780625559322` | spec 10kg/20kg 정정(표시용) |

## 2026-08-24 — 볶음참깨 자루(벌크) 재고 -45kg 확정

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-bokkeum-bulk-minus45.mts` (기본 `--dry`, 적용 `--apply`)

`lotsAreTotal` 원료는 두 숫자가 따로다 — 로트 합계는 **통틀어** 몇 kg(자루+낱개+박스),
`items.stock`은 그중 **자루로 남은 것**. 여태 로트를 건드릴 때마다 stock이 로트합으로
덮여 두 숫자가 같아져 있었다(201 = 201).

통합 대상은 낱개 1kg · 10개입 · 20개입 셋. 200g은 별개 BOM(`볶음참깨 ×0.2 + 200G-페트
+ 페트 마개`)이라 뺀다.

```
낱개 1kg    -4개  × 1kg  =   -4 kg
10개입 박스  -1박스 × 10kg =  -10 kg
20개입 박스  13박스 × 20kg =  260 kg
                  포장분  =  246 kg
                로트 통합  =  201 kg
              → 자루(벌크) =  -45 kg
```

| 문서 | 변경 |
|---|---|
| `items/raw-볶음참깨` (볶음참깨 벌크, type=wip) | `stock` 201 → **-45**. 로트 4개(합 201kg)는 그대로 |

- 자루가 음수인 건 포장분이 로트 통합을 넘어선다는 뜻 — 창고 실사로 맞출 자리다.
- 음수 재고 `볶음참깨-낱개/1kg` -4, `볶음참깨/1kg (10개입)` -1은 아직 안 건드렸다.
- 되돌리기: `items/raw-볶음참깨`의 `stock`을 201로.
- ⚠️ `firebaseService.mutateRawMaterialLots`의 `lotsAreTotal` 가드가 **배포되기 전까지는**
  로트를 건드리는 순간 stock이 로트합(201)으로 도로 덮인다.

## 2026-08-24 — 볶음참깨-낱개/1kg BOM의 유령 부자재 교체

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-bokkeum-1kg-vinyl.mts` (기본 `--dry`, 적용 `--apply`)

옛 `1KG-볶음참깨`(PLYZ-S-1000)는 2026-07-13에 archived 처리됐는데 BOM에는 그대로
남아 있었다. 보관된 품목은 화면·원가 롤업에서 빠지므로 1kg 낱개의 비닐 원가가 통째로
빠졌다. 사장님이 새로 만든 같은 이름의 비닐 품목으로 옮긴다.

| 문서 | 변경 |
|---|---|
| `item_bom/bom-PLDhkjOgcPIhO1hhReHm-PLYZ-S-1000` | 삭제 (보관 품목 참조) |
| `item_bom/bom-PLDhkjOgcPIhO1hhReHm__p-1787560459683` | 신규 — `1KG-볶음참깨`(category=비닐, 원가 110) ×1 |

- 옛 품목을 물고 있던 BOM은 이 한 줄뿐이었다.
- 되돌리기: 새 줄을 지우고 `{parent_id:'PLDhkjOgcPIhO1hhReHm', child_id:'PLYZ-S-1000', quantity:1}` 복원.

## 2026-08-24 — items에서 옛 `submaterials`·`unpackTo` 필드 제거

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-drop-item-submaterials.mts` (기본 `--dry`, 적용 `--apply`)

구성(BOM)은 `item_bom`이 유일 원천이 됐다(shared/bomIndex). 품목 문서에 남아 있던 옛
배열은 아무도 안 읽는데 남아서, 진단할 때마다 "어느 쪽이 참이냐"를 되묻게 만들었다.

| 문서 | 변경 |
|---|---|
| `items` 50건 | `submaterials` · `unpackTo` 필드 제거 |

- 지우기 전 대조: 옛 배열과 item_bom이 **차이 0건**으로 일치했다(구성·수량 모두).
- `unpackTo`는 이미 전 품목에서 0건이었다.
- 되돌리기: 지운 배열은 복구 안 된다. `item_bom`이 같은 내용을 그대로 들고 있다.

## 2026-08-24 — 검정참깨 · 탈피들깨 카테고리로 품목 이동

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-recat-검정탈피.mts` (기본 `--dry`, 적용 `--apply`)

분류 관리에 새로 만든 `검정참깨`·`탈피들깨`가 비어 있고, 해당 품목들이 상위 분류
(참깨·들깨)에 섞여 있었다.

| 변경 | 건수 |
|---|---|
| `category` 참깨 → **검정참깨** | 10건 (완제품 8 · 원료 1 · 반제품 1) |
| `category` 들깨 → **탈피들깨** | 12건 (완제품 11 · 원료 1) |

- **제품 계열(product/wip/raw)만** 옮겼다. 부자재는 category가 '무슨 물건이냐'
  (라벨·비닐·테이프)라서 안 건드렸다 — '모란 검정참깨'는 라벨이고 '테이프-검정'의 검정은 색이다.
  건너뛴 부자재 8건.
- 되돌리기: 각 품목의 `category`를 참깨/들깨로.

## 2026-08-24 — 완제품 서브타입 `배송` → `박스`

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-subtype-배송-to-박스.mts` (기본 `--dry`, 적용 `--apply`)

분류 관리에서 완제품 서브타입 이름을 배송→박스로 바꿨는데 품목은 옛 값을 들고 있어,
새 이름으로 거르면 한 건도 안 나왔다. **분류표를 고쳐도 이미 붙은 값은 안 따라온다.**

| 문서 | 변경 |
|---|---|
| `items` 130건 (전부 type=product) | `subtype` 배송 → **박스** |

- 되돌리기: `subtype`을 '배송'으로.
- 코드에서 `subtype !== '배송'`으로 박스를 가려내던 네 자리는 **구성 기반 판정**으로 바꿨다
  (완제품 구성품 하나 × 수량 2 이상). 이름을 또 바꿔도 안 깨진다.

## 2026-08-24 — 수율 방향 정정 후 원가 전면 재계산

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-recost-yield.mts` (기본 `--dry`, 적용 `--apply`, 되돌리기 `--undo`)
**백업:** `scripts/fix-recost-yield-backup.json` (미보관 전 품목의 적용 전 원가)

사장님 지적 — "통들깨들기름 원가 다시 계산해봐", "37%라는 거는 들깨 1kg로 기름이 370g
나온다는 거지". 두 가지가 겹쳐 있었다:

① **원가 롤업이 수율을 곱했다.** 기름 1kg엔 들깨 1/0.37 = 2.7kg이 드는데 0.37kg으로 잡혔다.
② **제조 반제품은 저장 cost가 있으면 그걸 그대로 썼다.** 원료값이 올라도 안 따라와서
   통들깨들기름이 들깨 5,795원 시절 값을 들고 있었다(실제 6,700원).

| 변경 | 건수 |
|---|---|
| `items.cost` 재계산 | **226건** (반제품 10 · 완제품 216) |

주요 반제품:

| 반제품 | 전 | 후 | 근거 |
|---|---:|---:|---|
| 통들깨들기름 | 15,663 | **18,108** | 들깨 6,700 ÷ 수율 0.37 |
| 통깨참기름 | 10,029 | **8,552** | 참깨 4,105 ÷ 0.48 |
| 깨분참기름 | 7,884 | **6,600** | 깨분 2,970 ÷ 0.45 |
| 참기름특/A/골드… 6종 | 9,075~10,364 | **6,990~8,064** | 위 둘의 배합 |
| 들기름 | 13,185 | **12,795** | 수입들기름 0.8 + 통들깨들기름 0.2 |

- 참기름 계열 완제품이 약 −22%. 저장돼 있던 반제품 원가가 지금 원료값보다 높았기 때문이다.
- 깻묵(부산물) 차감은 넣지 않았다(사장님 지시).
- 원료식이 없는 매입 반제품(깨분참기름/16.5kg·수입들기름·생들기름 등)은 저장값 그대로.
- 되돌리기: `npx tsx scripts/fix-recost-yield.mts --undo`

## 2026-08-24 — 면세 원료 지정 + 원가 재계산 (2차)

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-taxtype-면세원료.mts`, `scripts/fix-recost-yield.mts`

사장님 지적 — "통깨는 참깨가 면세고 통깨참기름은 과세여서 참깨에 1.1곱해서 해야할걸",
"검정참깨, 탈피들깨도 면세", "나머지는 과세", "들깨 단가 6000원으로 수정했으니 다시 계산".

**면세 원료로 과세품을 만들면 매입세액을 못 뺀다** — 그만큼이 그대로 원가에 얹힌다(×1.1).

| 문서 | 변경 |
|---|---|
| `items` 4건 (참깨·검정참깨·들깨·탈피들깨가루) | `taxType: '면세'` |
| `items` 210건 | `cost` 재계산 (면세 가산 + 들깨 6,000원 반영) |

| 반제품 | 값 | 근거 |
|---|---:|---|
| 통깨참기름 | **9,407** | 참깨 4,105 × 1.1 ÷ 0.48 |
| 통들깨들기름 | **17,838** | 들깨 6,000 × 1.1 ÷ 0.37 |
| 깨분참기름 | 6,600 | 깨분 2,970 ÷ 0.45 (과세라 그대로) |
| 들기름 | 12,741 | 수입 11,467×0.8 + 통들깨 17,838×0.2 |

- 원료 이름이 `검정깨` → `검정참깨`로 바뀌어 있어 그 이름으로 잡았다.
- 나머지(깨분·깻묵 등)는 과세 — `taxType` 미지정이 곧 과세다.
- 되돌리기: `npx tsx scripts/fix-recost-yield.mts --undo` (백업은 이번 적용 직전 값)

## 2026-08-24 — 금빛 350ml BOM 수량을 L → kg로 정정

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-bom-금빛-kg.mts` (기본 `--dry`, 적용 `--apply`)

BOM 수량은 **언제나 kg**으로 저장한다(items.stock·로트·원료수불부와 같은 단위).
품목 수정 화면이 kg 숫자에 'L' 딱지만 붙여 용량이 모자라 보였고(300ml 병이 0.277L로),
그걸 맞추려다 이 한 줄만 L로 들어갔다.

| 문서 | 변경 |
|---|---|
| `item_bom` 1줄 (참기름/병/특A(금빛)/350ml ← 참기름특A) | `quantity` 0.35 → **0.3206** (0.35L × 0.916) |

- 밀도 있는 구성품이 걸린 BOM 117줄 중 **116줄은 kg으로 정상**이었다. 이 한 줄만 어긋났다.
- 표시 쪽도 같이 고쳤다 — 밀도로 나눠 L로 보여준다(품목 수정·품목정보관리 칩).
- 되돌리기: `quantity`를 0.35로.

## 2026-08-24 — 급여 지급(미지급) 전표 템플릿 추가

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-tpl-salary-payout.mts` (기본 `--dry`, 적용 `--apply`, 되돌리기 `--undo`)

| 문서 | 내용 |
|---|---|
| `fixedCostTemplates/fct-salary-payout` | 신규 — 출금 · 계정 263 미지급급여 · 묶음 '급여·상환' |

분개: **(차) 263 미지급급여 / (대) 통장**

급여는 말일에 발생으로 잡혀 비용(515)이 이미 섰고 갚을 돈이 263에 남아 있다. 지급은 그 263을
터는 것이라 비용 계정을 다시 세우면 안 된다. 옛 '급여' 템플릿(mode=급여, 출금)은 515를 차변에
세우는 현금주의용이라 여기 쓰면 비용이 두 번 잡힌다.

- 되돌리기: `npx tsx scripts/fix-tpl-salary-payout.mts --undo`

## 2026-08-24 — 옛 '급여' 템플릿 숨김

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-tpl-hide-old-salary.mts` (기본 `--dry`, 적용 `--apply`, 되돌리기 `--undo`)

| 문서 | 변경 |
|---|---|
| `fixedCostTemplates/fct-builtin-salary` | `hidden` false → **true** |

`mode='급여'`라 515 급여를 차변에 세우는 **현금주의**용이다. 급여를 말일 발생으로 잡는 지금
방식에선 지급 때 이걸 쓰면 비용이 두 번 잡힌다. 지급은 `fct-salary-payout`(263을 턴다)을 쓴다.
기본 템플릿(builtin)이라 지울 수는 없고 숨기기만 된다.

- 되돌리기: `npx tsx scripts/fix-tpl-hide-old-salary.mts --undo`

## 2026-08-24 — 미광팩 기초 미지급 전표 모양 정정

**작업자:** Claude 에이전트 (사용자 지시)
**스크립트:** `scripts/fix-opening-mikwang.mts` (기본 `--dry`, 적용 `--apply`, 되돌리기 `--undo`)

| 문서 | 변경 |
|---|---|
| `issuedStatements/stmt-open-c-migwang-1773134199090-매입` (기초260731-35) | items: `505 부자재매입` 한 줄 → **(차) 375 이월이익잉여금 / (대) 251 외상매입금** |

기초 전표 61건 중 이것 하나만 모양이 달랐다(다른 미지급 15건은 전부 375/251 두 줄).
`autoJournal`은 `side` 없는 줄을 만나면 **짐작하지 않고 분개를 통째로 안 만든다**(일부러 그렇게
해 뒀다 — 짐작으로 메우면 조용히 틀린 방향으로 서고 시산표는 맞아 보인다). 그래서 이
4,391,800원이 시산표에도 미광팩 잔액에도 안 잡혀 있었다.

고친 뒤 기초 전표 61건 전부 분개가 선다(side 없는 것 0건).

| 계정 | 기초 합계 |
|---|---:|
| 108 외상매출금(차) | 162,002,060 |
| 251 외상매입금(대) | **134,871,506** (미광팩 4,391,800 포함) |
| 375 이월이익잉여금 | 296,873,566 |
| 505 부자재매입 | 0 |

- 되돌리기: `npx tsx scripts/fix-opening-mikwang.mts --undo`

## 2026-09-01 · 선수금·선급금 오기입 4건 정정

**무엇** — 거래명세서를 끊으면서 같은 클릭으로 수금·지불한 건이 통째로 259 선수금 /
131 선급금으로 앉아 있었다. 미수·미지급이 그만큼 안 깎이고 양쪽에 살아 있었다.

**왜** — `recordPayment`가 초과분을 가를 때 `partnerBalances`를 보는데, 그 순간
화면 상태가 아직 안 돌아 **방금 끊은 전표를 모른다.** 받을 돈이 0으로 보여 전액이
초과수금이 됐다. 코드는 `src/shared/paymentSplit.ts`로 고쳤다(아직 잔액에 안 잡힌
전표를 더해 센다). 이 정정은 이미 들어간 것을 맞춘 것이다.

**고친 것** (`scripts/fix-advance-misbooked.mts`, 백업·`--undo` 있음)

| 날짜 | 거래처 | 앞 | 뒤 |
|---|---|---|---|
| 09-01 | 한국농수산물유통공사 | 선급금 20,216,853 | 미지급 상계 20,216,853 |
| 09-01 | 피쉬메이져 | 선수금 632,000 | 미수 상계 632,000 |
| 09-01 | 대왕푸드 | 상계 389,850 / 선수금 774,150 | 미수 상계 1,164,000 |
| 09-01 | 기타 | 선급금 44,000 | 미지급 상계 44,000 |

**안 건드린 것** — 08-20 웨일스드림 선급금 20,000,000. 그날 갚을 것이 정말 없었으니
선급금이 맞다(물건 미도착).

**결과** — 태백·풍회 259 선수금 잔액 0. 분개 527건, 못 만든 것 1건(더미)으로 그대로.

## 2026-09-01 · 박스 품목 규격 6건을 BOM 에 맞춤

**무엇** — 박스 품목의 규격(`350ml * 20`)은 낱개 규격 + 개입수를 따라 적는 글자인데,
여섯 품목이 안 따라가 있었다.

| 품목 | 앞 | 뒤 |
|---|---|---|
| 들기름/병/특/가득찬/350ml | `300ml * 20` | `350ml * 20` (**용량이 틀렸었다**) |
| 참기름/병/분/엘생명/180ml | `180ml` | `180ml * 40` |
| 참기름/병/원액/엘생명/180ml | `180ml` | `180ml * 40` |
| 시골향참기름/원액/1750ml | `1750ml` | `1750ml * 10` |
| 시골향참기름/A/흰정사각/1750ml | `1750ml` | `1750ml * 10` |
| 시골향참기름/특A/흰정사각/1750ml | `1750ml` | `1750ml * 10` |

**왜** — 규격을 다시 만드는 코드가 품목 편집창의 '개입수' 칸 안에만 있어서, 그 칸을
손으로 건드릴 때만 돌았다. 낱개 용량을 고쳐도 박스는 옛 글자 그대로 남았다.

**앞으로** — 낱개를 저장할 때 그 낱개를 문 박스도 같이 맞춘다
(`src/shared/boxSpec.ts` · AdminApp 의 품목 저장 경로). 규칙은 한 곳에 있고
이 스크립트도 같은 함수를 쓴다.

`scripts/fix-box-spec-tail.mts` · 백업·`--undo` 있음. 개입수 판단은 BOM 이 하므로
동작은 안 바뀌었다 — 화면에 찍히는 글자만 맞췄다.

## 2026-09-02 — 박은지 2026-09-02 연차 1건 삭제

잘못 들어간 신청(사장님 확인). `leaveRequests/lv-rry4ztnki` (2026-09-02, 연차, approved, 1일).

사용일수는 직원 문서에 저장돼 있지 않다 — `getApprovedLeaveDays` 가 승인된 신청을
그때그때 더해서 낸다. 그래서 이 줄을 지우면 사용이 1일 줄고 잔여가 1일 는다.
따로 고칠 칸이 없다.

    npx tsx scripts/fix-leave-eunji-0902.mts          # 무엇을 지울지 보기
    npx tsx scripts/fix-leave-eunji-0902.mts --apply  # 지우기(백업 남김)
    npx tsx scripts/fix-leave-eunji-0902.mts --undo   # 되돌리기

백업: `scripts/fix-leave-eunji-0902-backup.json`

## 2026-09-03 — 거래일 고친 뒤 안 따라간 시각 도장 1건 (9월분만)

`issuedStatements/stmt-…` `260902-03` (홍인농산, 거래일 2026-09-03, 9/2 에 미리 끊음).
`issuedAt` 이 만든 시각(16:23:55)으로 남아 있었다 — 예약 전표라 `00:00:00` 이어야 한다.

**8월은 손대지 않았다.** 사장님이 날짜를 손으로 다 맞춰 둔 것이라, 오류로 보여도 그대로 둔다.
스크립트도 `--from` 기본이 `2026-09` 이고 그 이전은 거절한다.

    npx tsx scripts/fix-stamp-after-dateedit.mts          # 미리보기
    npx tsx scripts/fix-stamp-after-dateedit.mts --apply  # 고치기(백업 남김)
    npx tsx scripts/fix-stamp-after-dateedit.mts --undo   # 되돌리기

백업: `scripts/fix-stamp-after-dateedit-backup.json`

손대지 않고 남겨 둔 것 — 태백↔풍회 입금 4줄이 `18:00:00` 으로 찍혀 있다.
코드에도 문서에도 그렇게 찍는 자리가 없어 사람이 일부러 넣은 값으로 보고 건드리지 않았다.

## 2026-09-03 — 아직 안 끝난 9월의 '월말' 재고 스냅샷 삭제

`inventorySnapshots/inv-snap-2026-09` (219,518,832원, 회사 taebaek, 기록 2026-09-01 10:03).
**9월이 안 끝났는데 월말 실사액 자리에 8월 값이 그대로 들어가 있었다.**

그냥 두면 스케줄러가 9/30에 돌아도 "이미 있네" 하고 건너뛴다. 지웠으니 9/30 23:00에
그날 재고로 제대로 찍힌다. 7·8월 스냅샷은 안 건드렸다 — 지난 달 장부가 그걸 딛고 서 있다.

    npx tsx scripts/fix-drop-sep-invsnap.mts          # 미리보기
    npx tsx scripts/fix-drop-sep-invsnap.mts --apply  # 지우기(백업 남김)
    npx tsx scripts/fix-drop-sep-invsnap.mts --undo   # 되돌리기

백업: `scripts/fix-drop-sep-invsnap-backup.json`

같은 날 `firebase deploy --only functions` 로 스케줄러도 올렸다 —
재고평가 규칙을 앱과 맞추고(회사별·음수 포함) 전표번호를 새 형식으로 바꾼 것.

## 2026-09-03 — 풍회 자금줄 넷의 시각 도장

`createdAt` 이 `2026-08-04T09:00:00.000Z` 처럼 **UTC 09시**로 박혀 있어 로컬로 읽으면
전부 `18:00:00` 이었다. 스크립트로 넣으면서 시각을 대충 채운 자국이다(사장님 확인).

    260804-03  8/04  6,200,000원  18:00:00 → 23:59:59   (8/28 에 소급 입력)
    260811-03  8/11  3,200,000원  18:00:00 → 23:59:59
    260824-02  8/24  3,110,000원  18:00:00 → 23:59:59
    260831-02  8/31    500,000원  18:00:00 → 16:52:49   (그날 넣었으니 당일 시각)

**금액도 날짜도 안 바뀐다.** 원장에서 그 줄이 그날 안에서 서는 자리만 바뀐다.
기준은 `stampFor` — 소급이면 그날 맨 뒤, 당일이면 그때 시각. 그 줄을 만든 순간(id 의 ms)으로 판정한다.

    npx tsx scripts/fix-punghoe-1800.mts          # 미리보기
    npx tsx scripts/fix-punghoe-1800.mts --apply  # 고치기(백업 남김)
    npx tsx scripts/fix-punghoe-1800.mts --undo   # 되돌리기

백업: `scripts/fix-punghoe-1800-backup.json`

## 2026-09-03 — 합쳐 발행한 전표에 주문 이어 붙임 · 자금 전표번호 채움 · 문서함 중복

### 합쳐 발행한 주문 2건
여러 주문을 한 전표로 묶어 발행했는데 전표의 `orderId` 에 하나만 적혀서,
나머지가 계속 **미발행 주문**으로 떴다.

    260827-07  ← ORD-1787819105308  현대유통(원주) 볶음참깨-낱개/1kg ×30
    260831-02  ← ORD-1788162025970  해피유통(포천) 시골향들기름/특/1750ml 2박스(=20낱개)

**지우지 않고 이었다.** 둘 다 이미 출고돼 재고가 빠졌고 8월 서류(원료수불부·
생산판매기록부)에 들어가 있다 — 지우면 관청에 낸 서류와 안 맞게 된다.
목록 판정이 `invoicePrinted` 가 아니라 **전표 실물**이라, 이으면 목록에서 사라지고
기록은 남는다. **돈은 안 움직인다** — `orderId` 칸만 는다.

    npx tsx scripts/fix-link-merged-orders.mts --apply / --undo

### 자금 전표번호 220개
246개 중 220개가 비어 있었다(전부 8월). 원장에서 번호로 전표를 찾아가는데 누를 게 없었다.
`nextDocNo` 규칙(그날 최대+1)으로, 만든 순서대로, 이미 있는 번호는 피해서 채웠다.
날짜·금액·계정은 안 바뀐다.  `scripts/fix-cash-docno.mts --undo`

### 문서함 '서류관리' 중복 7개 + 중분류 1개
09-02 08:11~08:18 사이에 화면을 켤 때마다 하나씩 생겼다. 가장 오래된 것만 남겼다.
`scripts/fix-cabinet-dedup2.mts --undo`

## 2026-09-03 — 이지영 2026-09-04 연차 취소

안 쉬기로 했다(사장님). **화면에 승인된 연차를 무르는 길이 없어** 스크립트로 지웠다.
`leaveRequests/lv-4ikuei2h1` (연차, approved, 1일).

사용·예정은 저장된 값이 아니라 승인된 신청에서 세므로, 지우면 '예정'에서 1일이 빠지고
잔여가 1일 는다 — 따로 고칠 칸이 없다. 아직 안 온 날이라 '사용'이 아니라 '예정'이었다.

    npx tsx scripts/fix-leave-cancel-0904.mts --undo

취소 기능을 어떻게 만들지는 할일에 적어 뒀다(지우기 vs `status: 'cancelled'`).
