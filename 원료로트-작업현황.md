# 원료 로트(Lot) 시스템 — 작업 현황

> 작성: 2026-06-15 (Claude 에이전트, 사용자 chdwp 요청). 사람/다른 에이전트가 현재 상태·변경·남은 일을 한눈에 보기 위한 문서.
> DB 직접 변경 내역은 [DB-CHANGELOG.md](DB-CHANGELOG.md) 참조.

## 한 줄 요약
기름·가루를 캔/포대로 입고하면 원료(kg/L)로 자동 환산해 **로트별로 추적**하고, 납품완료 시 **선입선출로 자동 차감**한다.
**진행: 1단계 ✅ · 2단계 ✅ · 3단계(혼합 5:5) ⬜ · 배포 ✅(2026-06-15, 커밋 484d65b)**

---

## 배경 / 목표
- 기름은 캔(16.5kg), 가루는 포대(20kg)/자루(25kg)로 입고됨. 이걸 원료 재고(kg/L)에 연동하고 싶음.
- 거래처별 입고분을 **로트**로 쌓고, 선입선출(앞 로트 먼저)로 쓰되 ▲▼로 순서 변경 가능.
- 발견된 문제: `깨분참기름`이 **캔 SKU(반제품, 개 단위)** 와 **원료(raw, L 단위)** 두 품목으로 분리돼 있어 입고가 원료에 반영 안 됨. 게다가 품목명 일괄변경(`/16.5kg` 접미사)으로 수불부 자동기록 매칭까지 깨져 있었음.

## 핵심 동작 (구현됨)
1. **입고 → 로트 자동 생성**: 거래처 입고(또는 수동 입고) 시, 품목명에서 base 원료명을 뽑고(`깨분참기름/16.5kg`→`깨분참기름`) 규격에서 캔당 kg을 파싱해 **캔수×kg**으로 환산한 로트를 원료에 추가 + 수불부(kg) 기록.
2. **단위**: 로트 잔량은 항상 **kg(canonical)**, 기름은 화면에서 **L 병기** 표시. 원료는 캔이 아니라 kg/L로 관리.
3. **선입선출 + 순서변경**: 원료(또는 연결된 반제품) 클릭 → 로트 목록. ▲▼로 사용 순서 변경, 🗑로 삭제(관리자).
4. **로트번호**: 입고 시 `YYMMDD-순번` 자동 부여(예 260615-01), 패널에서 수동 수정 가능(캔 라벨 제조사 번호).
5. **납품완료 시 자동 차감**: 주문 납품완료 → 배합비로 원료 사용량 합산 → **선입선출 차감** + 수불부에 로트별 분배 표기(`자동: 고객 ▸ 청양 30kg + 청정 20kg`).

---

## 변경한 파일 (코드 — git으로 추적·되돌리기 가능)
| 파일 | 변경 내용 |
|---|---|
| `src/shared/types.ts` | `RawMaterialLot` 타입 추가. `Item`에 `lots`/`packageType`/`packageKg`. `Order`에 `rawLotsDeducted`. |
| `src/constants/formula.ts` | `baseRawName`, `parsePackageKg`, `kgToUnit`/`unitToKg`, `lotKgRemaining`, `lotStockInUnit` 추가. |
| `src/shared/lotUtils.ts` (신규) | `withCarryOverLot`, `buildReceiveLot`, `nextLotNo`, `deductFromLots`, `receiptToKg`. |
| `src/shared/services/firebaseService.ts` | `mutateRawMaterialLots`(트랜잭션, undefined strip). |
| `components/ReceivingReturnsManager.tsx` | 입고 2경로(스캔·선입고)에서 `recordRawMaterialReceipt`로 로트 생성+수불부. 매입 SKU 재고 누적 중단. |
| `components/RawMaterialLotPanel.tsx` (신규) | 로트 목록 UI(▲▼/🗑/로트번호 편집/L·kg 표시/소진 숨김). |
| `components/ItemList.tsx` | 원료·연결 반제품 클릭 시 로트 패널 표시. 수동 입고도 로트 생성형으로. |
| `src/features/admin/AdminApp.tsx` | `deductSubmaterialsForOrder`에 원료별 합산 + 선입선출 차감 + 부족 알림. |

## 새 스크립트 (`scripts/`, 로컬 진단·마이그레이션)
- `lot-migration.mts` — 이월 로트 시드 + 캔 SKU 정리. `--dry`(미리보기)/`--undo`(되돌리기). 백업: `lot-migration-backup.json`.
- `test-lot-logic.mts` — 순수 로직 테스트 34건(`npx tsx`). DB 미접속.
- `diagnose-punghoe*.mjs`, `check-raw-purchasing-skus.mjs`, `verify-lots.mjs` — 읽기 전용 진단.

## 고친 버그
1. **수불부 자동기록 누락**: 입고 매칭이 `RM_LIST.includes(item.name)`인데 품목명이 `깨분참기름/16.5kg`으로 바뀌어 매칭 실패 → `baseRawName`으로 접미사 떼고 매칭하도록 수정.
2. **Firestore 쓰기 거부(잠재)**: 로트 객체의 미입력 옵션필드(undefined)를 Firestore가 거부 → `mutateRawMaterialLots`에서 쓰기 전 `stripUndefined`. (마이그레이션 이월 로트는 우연히 깨끗해 통과했지만 실제 캔/수동 입고는 터졌을 것)

## DB 변경 (운영 Firestore, 적용 완료)
[DB-CHANGELOG.md](DB-CHANGELOG.md) 참조. 요약: 참깨·깨분참기름·생들기름 이월 로트 시드(재고 불변), 캔 SKU(`p-1779251603644`) 재고 176→0. 전부 `lot-migration.mts --undo`로 원복 가능.

## 결정 사항 (사용자 확정)
- 캔 SKU는 발주/입고용으로 **유지**, 입고 시 원료에 자동 로트 생성.
- 시작 재고는 **수불부 잔량 이월**.
- 매입 SKU 있는 원료는 **참깨·깨분참기름 둘뿐** → 수동 입고 버튼 **유지**(나머지 10개 원료의 유일 입고 경로), 로트 생성형으로 전환.
- 로트번호 **자동(YYMMDD-순번)+수동수정**.
- 혼합 비율 기본 **5:5**(3단계).
- 풍회 6/15 68캔은 이월 1,242L에 **이미 포함**(추가 안 함).

## 검증 상태
- `npx tsc --noEmit`: 0 에러
- `npx tsx scripts/test-lot-logic.mts`: 34/34 통과
- `npx vite build --config vite.admin.config.ts`: 성공
- 로컬 dev(`npx vite --config vite.admin.config.ts`, :3001/3002) HMR 정상

## 남은 일 (TODO)
- [ ] **3단계 — 기름 혼합(5:5)**: 상위 2개 로트를 비율대로 동시 차감(기본 50/50), 비율 입력 UI.
- [ ] 반제품(캔 SKU) 재고를 WIP 탭에서 숨길지 결정(현재 0으로 표시됨, 무해).
- [ ] 거래처 입고 SKU 연결 보강(현재 참깨·깨분참기름만) — 나머지 원료도 거래처 물리면 수동 입고 줄일 수 있음.
- [ ] 차감 배합비가 하드코딩 `PRODUCT_FORMULA` 기준 — Firestore `item_formula`와 일치 여부 점검(현재 수불부 자동기록과 동일 소스라 상호 일관).
- [x] **배포 완료(2026-06-15)**: `npm run deploy:all` → https://taebaek-staff.web.app(admin), https://taebaek-3abe4.web.app(staff). GitHub main 푸시됨(484d65b, cf51c14 위 리베이스, 충돌 없음).

## 실행 / 되돌리기
- 로컬 실행(원료 UI=admin): `npx vite --config vite.admin.config.ts` → http://localhost:3001 (점유 시 3002)
- 마이그레이션 되돌리기: `npx tsx scripts/lot-migration.mts --undo`
- 코드 되돌리기: 커밋 안 했으므로 `git checkout -- <파일>` 또는 GitHub 최신(`1398ae6`)에서 복원
- ⚠️ **운영 Firestore에 씀** — 입고/납품 시 실제 로트가 바뀜.
- 2026-06-15부터 **라이브 배포됨** — 라이브 사이트에서 입고하면 로트 생성, 납품완료하면 선입선출 차감이 실제로 작동.
