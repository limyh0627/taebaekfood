# 진행사항

Codex와 Claude Code가 현재 작업을 주고받는 기록이다. 영구 규칙은 `인수인계.md`, 미완료 목록은
`할일.md`가 기준이며, 이 파일에는 **지금 진행 중인 한 작업**만 적는다.

## 작성 규칙

- 작업을 시작할 때 담당자·목표·대상 파일·완료 조건을 적는다.
- 조사하거나 수정한 사실은 추측과 구분해 바로 적는다.
- 실행한 시험 명령과 결과를 적는다. 실행하지 못했으면 이유를 적는다.
- 커밋·푸시·배포·운영 DB 적용은 사장님 지시 전까지 하지 않는다.
- 작업이 끝나면 결과와 남은 일을 적고, 다음 담당자가 현재 코드와 다시 대조한다.

## 현재 작업

- 담당: Claude Code 구현 → Codex 검수
- 목표: `partner_item` 생성·동기화 시 품목과 거래처가 로그인 회사 소유인지 쓰기 전에 검증한다.
- 현재 확인: 회사 조건 없는 기존 연결 조회는 `b85bc8d`에서 로그인 회사 조건으로 제한됐다.
- 남은 문제: 다른 회사의 `itemId` 또는 `partnerId`를 넘겨도 같은 회사 연결인지 검증하지 않는다.
- 금지: Git 명령, 커밋, 푸시, 배포, 운영 DB 접근·수정, 마이그레이션 실행.
- 완료 조건:
  1. 교차회사 품목·거래처 연결을 쓰기 전에 거절한다.
  2. 기존 동일회사 연결·해제는 유지한다.
  3. 실제 동작 회귀시험을 추가하고 관련 Vitest와 타입검사를 통과한다.
  4. Claude가 아래 기록에 수정 파일·시험 결과·남은 일을 적는다.

## 작업 기록

- 2026-09-17 Codex: 작업 범위와 완료 조건을 작성함.

## 긴급 작업 — 오피스톡

- 담당: Claude Code 구현 → Codex 검수
- 증상: 멤버 초대 실패, 메시지 전송 실패, 모바일 키보드의 줄바꿈 Enter가 메시지를 전송함.
- 현재 확인:
  - `OfficeTalk.tsx`의 textarea가 모바일 여부와 무관하게 `Enter && !shiftKey`를 전송으로 처리한다.
  - 새 메시지는 `addItem`을 거쳐 claim 회사가 붙지만, 방 수정의 `updateItem`은 공통 회사 쓰기 경계를 거치지 않는다.
  - Firestore 규칙은 방의 `participantIds`와 `participantCompanies`가 정확히 맞고 모두 방 회사여야 하며,
    메시지의 회사와 부모 방 회사도 같아야 한다.
- 완료 조건:
  1. 모바일 Enter는 줄바꿈이고 전송은 전송 버튼으로만 한다. 데스크톱 Enter 전송 정책은 기존대로 유지한다.
  2. 초대와 메시지 전송이 강화된 회사 보안 규칙을 만족한다.
  3. 실패 시 입력·선택 상태를 잃지 않고 사용자에게 원인을 표시한다.
  4. 회귀시험과 타입검사를 통과한다.
- 금지: 기존 다른 작업 되돌리기, Git 명령, 커밋·푸시·배포, 운영 DB 접근·수정.
