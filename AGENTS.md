# 태백푸드 — 코드 고치는 규칙

**Codex 가 일 시작하기 전에 읽는 파일이다.** Claude Code 는 [CLAUDE.md](CLAUDE.md) 를 읽는다 —
둘은 같은 것을 가리킨다. **규칙 자체는 [인수인계.md](인수인계.md) 한 곳에 있다.**
규칙을 고칠 일이 있으면 여기 말고 거기를 고쳐라.

## 먼저 읽을 것

- [할일.md](할일.md) — 지금 안 하지만 잊으면 안 되는 것 (**일 시작할 때 훑고, 끝난 건 지운다**)
- [인수인계.md](인수인계.md) — 지켜야 할 규칙(화면·BOM·재고·원가·전표·데이터 수정)
  - **0장이 제일 중요하다: 배포와 커밋은 사장님이 말할 때까지 안 한다.**
  - **그 다음: "같은 일에는 같은 함수를 쓴다"** — 셈을 새로 짜기 전에 그 절의 목록부터 본다.
    이 앱은 같은 셈이 여러 벌로 갈라져서 난 사고가 제일 많았다(박스 단가 ×10 이 네 벌 중 세 벌만
    고쳐져 있었던 적이 있다).
- [DB-CHANGELOG.md](DB-CHANGELOG.md) — DB를 건드린 이력과 되돌리는 법

## 말투

**사장님은 한국어로 묻고 한국어로 읽는다.** 답도, 주석도, 커밋 메시지도 한국어로 쓴다.
주석은 "무엇을 하는지"가 아니라 **"왜 이렇게 됐는지"**를 적는다 — 이 앱 주석은 대부분
사고 기록이다.

## 기술 스택

React + TypeScript + Vite / Firebase(Firestore·Hosting·Auth·Storage·FCM) / PWA(vite-plugin-pwa)
한 벌의 코드가 **관리자 앱과 직원 앱** 둘을 만든다.

## 고치고 나서 반드시 돌릴 것

```bash
npx tsc --noEmit -p tsconfig.json    # 타입
npx vitest run                        # 시험 (1,390여 개, 1분쯤 걸린다)
```

**시험이 이 앱의 안전장치다.** 화면을 눈으로 확인할 수 없는 셈(원가·전표·재고)이 대부분이라,
시험이 없으면 틀린 걸 알 방법이 없다. 새 셈을 짜면 시험도 같이 짠다.

**시험을 짰으면 일부러 코드를 망가뜨려서 시험이 잡는지 확인한다.** 안 잡으면 그 시험은
아무것도 안 지키고 있는 것이다.

## 배포 — 사장님이 말하기 전에는 하지 않는다

```bash
npm run deploy:all      # 둘 다
```

⚠ **호스팅 이름이 서로 뒤바뀌어 있다. 헷갈리기 쉬우니 그대로 외워라.**

| 무엇 | 명령 | 주소 |
|---|---|---|
| 직원 앱 | `npm run deploy:staff` | https://taebaek-3abe4.web.app |
| 관리자 앱 | `npm run deploy:admin` | https://taebaek-staff.web.app |

## DB 를 건드릴 때

화면에서 못 고치는 것을 손볼 때는 **스크립트로만** 한다 — 콘솔에서 손으로 고치면 기록이 안 남는다.

- `scripts/fix-*.mts` — `--dry`(기본) / `--apply` / `--undo` 를 다 갖춘다
- 고치기 전 값을 **백업 JSON 으로 남긴다.** 백업이 이미 있으면 두 번 돌지 않게 막는다
  (실제로 두 번 돌아서 원가가 10% 더 깎인 적이 있다)
- 끝나면 [DB-CHANGELOG.md](DB-CHANGELOG.md) 에 적는다
- 읽기만 하는 조사는 `scripts/diag-*.mts` 로 짜고 다 쓰면 지운다

## 손대지 않는 것

- `.env`, `scripts/serviceAccountKey.json` — Firebase 키
- `로컬전용/` — 엑셀 서류·일지 (git 에 안 올라간다)
- `functions/` — 서버 코드. 배포가 따로다(`npm run deploy:functions`)

## 두 도구를 같이 쓸 때

Claude Code 와 Codex 가 **같은 폴더를 동시에 고치면 서로의 수정을 덮어쓴다.**
한 번에 하나만 돌리거나, 갈래(branch)를 나눠서 각자 다른 데를 맡긴다.
