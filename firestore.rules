rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Firebase 익명 인증을 통과한 요청만 허용 (비인증 외부 접근 차단)
      allow read, write: if request.auth != null;
    }
  }
}
